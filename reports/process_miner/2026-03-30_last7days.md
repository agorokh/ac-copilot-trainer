# Process Improvement Miner Report

**Repository:** agorokh/ac-copilot-trainer
**Analysis period:** last 7 day(s) (since 2026-03-30 UTC, through 2026-04-06 UTC)
**Generated:** 2026-04-06T09:40:07.818273+00:00

---

## Executive Summary

- **PRs Analyzed:** 32
- **Total Comments:** 1015
- **Average Comments per PR:** 31.7
- **Total Files Changed:** 211
- **Total Additions:** 12,819
- **Total Deletions:** 1,618
- **CI Failures:** 0
- **Comment Clusters Found:** 74

---

## Analyzed PRs

- **PR #67:** [fix(#66): CSP textColored signature reversal + checkbox semantics + em dash (P0 hotfix)](https://github.com/agorokh/ac-copilot-trainer/pull/67)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 39
  - Files: 11
  - CI: success

- **PR #65:** [docs: SAVE vault for issue #57 closure](https://github.com/agorokh/ac-copilot-trainer/pull/65)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 7
  - Files: 2
  - CI: success

- **PR #64:** [Phase 5 Part E: Active suggestion panel (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/64)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 51
  - Files: 5
  - CI: success

- **PR #63:** [Phase 5 Part D: Real-time per-corner coaching engine (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/63)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 59
  - Files: 7
  - CI: success

- **PR #62:** [Phase 5 Part C: Polished approach telemetry panel (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/62)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 37
  - Files: 6
  - CI: success

- **PR #51:** [Add session journal export under ScriptConfig](https://github.com/agorokh/ac-copilot-trainer/pull/51)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 41
  - Files: 7
  - CI: success

- **PR #42:** [feat(#41): coaching HUD, diag gate, BMW font, brake walls](https://github.com/agorokh/ac-copilot-trainer/pull/42)
  - Author: agorokh
  - Merged: 2026-04-03
  - Comments: 21
  - Files: 8
  - CI: success

- **PR #40:** [Rendering pipeline: shaderedQuad, sim clock, diagnostics (#39)](https://github.com/agorokh/ac-copilot-trainer/pull/40)
  - Author: agorokh
  - Merged: 2026-04-03
  - Comments: 51
  - Files: 13
  - CI: success

- **PR #34:** [Visual overhaul: market-quality 3D overlays and HUD (#33)](https://github.com/agorokh/ac-copilot-trainer/pull/34)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 19
  - Files: 5
  - CI: success

- **PR #30:** [debug(#24): enable Draw3D coordinate diagnostics](https://github.com/agorokh/ac-copilot-trainer/pull/30)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 6
  - Files: 1
  - CI: success

- **PR #61:** [Add design conformance test suite (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/61)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 29
  - Files: 2
  - CI: success

- **PR #60:** [Phase 5 Part B: Settings window (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/60)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 26
  - Files: 6
  - CI: success

- **PR #58:** [Phase 5 Part A: ac.storage, corner names, structured approach data (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/58)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 23
  - Files: 4
  - CI: success

- **PR #56:** [Add focus practice mode for corner targets and marker emphasis (#44)](https://github.com/agorokh/ac-copilot-trainer/pull/56)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 32
  - Files: 7
  - CI: success

- **PR #55:** [Add Ollama post-lap debrief and HUD fallback (#46)](https://github.com/agorokh/ac-copilot-trainer/pull/55)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 32
  - Files: 14
  - CI: success

- **PR #54:** [Phase 3c: sidecar lap features + improvement ranking (#49)](https://github.com/agorokh/ac-copilot-trainer/pull/54)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 64
  - Files: 12
  - CI: success

- **PR #53:** [WebSocket sidecar protocol v1 + Lua inbound (fixes #45)](https://github.com/agorokh/ac-copilot-trainer/pull/53)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 26
  - Files: 9
  - CI: success

- **PR #52:** [Coaching UX: configurable hint display cap and WARP docs (#43)](https://github.com/agorokh/ac-copilot-trainer/pull/52)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 16
  - Files: 7
  - CI: success

- **PR #50:** [fix(#9): coaching hold timer, Coaching window idle, strip layout (v0.4.2)](https://github.com/agorokh/ac-copilot-trainer/pull/50)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 28
  - Files: 5
  - CI: success

- **PR #38:** [feat(#37): 3D brake walls, speed diagnostics, coaching overlay fix](https://github.com/agorokh/ac-copilot-trainer/pull/38)
  - Author: agorokh
  - Merged: 2026-04-03
  - Comments: 39
  - Files: 6
  - CI: success

- **PR #36:** [feat(#35): Mavil-quality 3D line, coaching overlay, brake marker upgrade](https://github.com/agorokh/ac-copilot-trainer/pull/36)
  - Author: agorokh
  - Merged: 2026-04-02
  - Comments: 43
  - Files: 6
  - CI: success

- **PR #31:** [fix(#24): filled geometry for brake markers + racing line](https://github.com/agorokh/ac-copilot-trainer/pull/31)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 27
  - Files: 4
  - CI: success

- **PR #29:** [debug(#24): log world coordinates for brake markers + racing line](https://github.com/agorokh/ac-copilot-trainer/pull/29)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 12
  - Files: 1
  - CI: success

- **PR #28:** [fix(#24): line-based brake markers — pcall isolation](https://github.com/agorokh/ac-copilot-trainer/pull/28)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 28
  - Files: 2
  - CI: success

- **PR #27:** [fix(#24): visible 3D markers, thicker racing line, coaching hints](https://github.com/agorokh/ac-copilot-trainer/pull/27)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 28
  - Files: 9
  - CI: success

- **PR #26:** [fix(#24): CSP render fixes + Draw3D diagnostics](https://github.com/agorokh/ac-copilot-trainer/pull/26)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 12
  - Files: 4
  - CI: success

- **PR #25:** [Fix CSP struct field access crash in persistence, spline_parser, setup_reader](https://github.com/agorokh/ac-copilot-trainer/pull/25)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 29
  - Files: 4
  - CI: success

- **PR #22:** [Coaching hints and optional AI WebSocket sidecar (Refs #9)](https://github.com/agorokh/ac-copilot-trainer/pull/22)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 41
  - Files: 10
  - CI: success

- **PR #21:** [Corner analysis, consistency, spline, racing line, tires, setup (#8)](https://github.com/agorokh/ac-copilot-trainer/pull/21)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 59
  - Files: 12
  - CI: success

- **PR #20:** [fix(pr20): lap clock init + trace validation](https://github.com/agorokh/ac-copilot-trainer/pull/20)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 57
  - Files: 10
  - CI: success

- **PR #10:** [Telemetry engine, brake detection, persistence, and HUD (#6)](https://github.com/agorokh/ac-copilot-trainer/pull/10)
  - Author: agorokh
  - Merged: 2026-03-30
  - Comments: 33
  - Files: 11
  - CI: success

- **PR #1:** [chore(deps): Bump actions/upload-artifact from 4 to 7 in the actions group](https://github.com/agorokh/ac-copilot-trainer/pull/1)
  - Author: dependabot[bot]
  - Merged: 2026-03-30
  - Comments: 0
  - Files: 1
  - CI: success

---

## Top Recurring Comment Clusters

These clusters represent patterns of repeated feedback across PRs.

### 1. details / summary / code

- **Count:** 195 comments
- **Severity:** reliability
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/track_markers.lua, docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟠 Major_

**Session-exit persistence never runs, and the next session reuses stale state.**

On the first main-menu frame, `script.update()` calls `persistSnapshot()`, but `per...

2. _⚠️ Potential issue_ | _🟠 Major_

**Guard no-car frames before touching HUD, telemetry, or lap state.**

`tryLoadDisk()` and `persistSnapshot()` already treat `not car` as a valid runtime state, but `...

3. _⚠️ Potential issue_ | _🟠 Major_

**Duration tracking misses first-frame time, causing false negatives.**

Current logic starts timing at `0` and only accumulates on later frames, so brake holds can b...

---

### 2. code / color / prefers

- **Count:** 182 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/racing_line.lua

**Representative Examples:**

1. ### Persist on session exit always exits early, losing data

**High Severity**

<!-- DESCRIPTION START -->
`persistSnapshot` is called inside the `sim.isInMainMenu` branch of `script.update`, but `per...

2. ### Silent fallback discards data when JSON unavailable

**Medium Severity**

<!-- DESCRIPTION START -->
`jsonEncode` silently returns `"{}"` when the `JSON` global is missing, causing `persistence.sa...

3. ### Session state not reset when car/track changes

**High Severity**

<!-- DESCRIPTION START -->
With `LAZY=PARTIAL`, the script persists across session changes. When transitioning to the main menu, ...

---

### 3. details / summary / code

- **Count:** 92 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Affected Files:** src/ac_copilot_trainer/modules/persistence.lua, tests/test_focus_practice.py

**Representative Examples:**

1. _🧹 Nitpick_ | _🔵 Trivial_

**Parameters `car` and `sim` are now unused in `sessionKey`.**

The function signature still accepts `car` and `sim` parameters, but the implementation now derives all ident...

2. _🧹 Nitpick_ | _🔵 Trivial_

**Consider splitting compound assertion for clearer failure messages.**

When this assertion fails, it's unclear which part failed. Splitting improves debuggability:

```dif...

3. <!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...

---

### 4. code / copilot / instructions

- **Count:** 67 comments
- **Severity:** nit
- **Preventability:** typecheck

**Representative Examples:**

1. ## Pull request overview

Implements Issue #7 feature set for the AC Copilot Trainer Lua app: full-lap telemetry traces, live delta + sector splits, on-track 3D brake markers, approach HUD, and thrott...

2. ## Pull request overview

Copilot reviewed 10 out of 10 changed files in this pull request and generated 2 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...

3. ## Pull request overview

Copilot reviewed 10 out of 10 changed files in this pull request and generated no new comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/ins...

---

### 5. code / summary / details

- **Count:** 29 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, tools/ai_sidecar/session.py

**Representative Examples:**

1. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Unguarded ui.getcursor deref <code>🐞 Bug</code> <code>☼ Reliability</code>

<pre>
...

2. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>coachingholdseconds</i></b> set to 30s <code>📎 Requirement gap</code> <code>...

3. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

2\. Timer freezes when car nil <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
<b...

---

### 6. codex / your / usage

- **Count:** 28 comments
- **Severity:** nit
- **Preventability:** guideline

**Representative Examples:**

1. You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).

2. You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).

3. You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).

---

### 7. code / window / code_block

- **Count:** 27 comments
- **Severity:** maintainability
- **Preventability:** typecheck
- **Affected Files:** src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/hud.lua

**Representative Examples:**

1. `bestCornerFeatures` is now persisted/loaded in `ac_copilot_trainer.lua`, but `normalizeLoaded()` doesn’t validate/coerce it like the other new v3 fields. For schema hygiene (and to avoid surprising t...

2. `guessSetupIniPath()` flattens the track identifier with `gsub("[^%w%.%-_]+", "_")`, which can break setup resolution for tracks with layout subfolders (e.g. `track/layout`), since the actual setups p...

3. With `lastTry` initialized to 0 and the reconnect guard `t - lastTry < RECONNECT_SEC`, the first connection attempt won’t happen until ~5s after session start (when `sim.time` reaches 5). If you want ...

---

### 8. code_block / code / script

- **Count:** 27 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Affected Files:** src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/persistence.lua

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cat -n src/ac_copilot_trainer/modules/persistence.lua | head -100
```

Repository: agorokh...

2. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, let's understand the file structure and the sessionKey usage
fd -t f "persistence...

3. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, let's check the file exists and examine the context around lines 204-209
fd ac_co...

---

### 9. code / this / comments

- **Count:** 25 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Affected Files:** scripts/check_csp_ui_safety.py, src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/modules/track_markers.lua

**Representative Examples:**

1. `snapToTrack()` accesses `hit.position.y` directly when `hit` is a table. If `hit.position` is a userdata (common for vec3-like types) or has a metamethod that throws on `.y`, this will error outside ...

2. This test name is misleading: `tools.ai_sidecar.server` can be imported even when `websockets` is missing (the import happens inside `_run`). Either rename the test to reflect what it checks, or chang...

3. `lastTry` starts at 0, so with `sim.time` starting near 0 this prevents the first connection attempt for ~5 seconds (`t - lastTry < RECONNECT_SEC`). If the goal is to connect ASAP when configured, ini...

---

### 10. code / summary / details

- **Count:** 24 comments
- **Severity:** maintainability
- **Preventability:** typecheck
- **Affected Files:** tests/test_lua_runtime_smoke.py, src/ac_copilot_trainer/modules/delta.lua, tools/ai_sidecar/__init__.py

**Representative Examples:**

1. `analyzeTrace()` documents `trace` elements as only `{ throttle, brake }`, but the implementation assumes `eMs` exists (used for dt estimation and segment timing). This mismatch can cause runtime erro...

2. Lua annotations for `LapTraceSample` in this module only declare `spline` and `eMs`, but `bestSpeedKmhAtSpline()` interpolates the `speed` field. This mismatch makes the contract unclear for callers a...

3. `LapTraceSample.speed` is annotated as optional (`number|nil`), but `interpAtSpline()` does arithmetic on the chosen field without guarding against `nil` (`va + t * (vb - va)`), which will raise at ru...

---

### 11. code / details / summary

- **Count:** 19 comments
- **Severity:** perf
- **Preventability:** test
- **Affected Files:** src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/telemetry.lua, src/ac_copilot_trainer/modules/coaching_hints.lua

**Representative Examples:**

1. _🧹 Nitpick_ | _🔵 Trivial_

**`getTrace()` returns a mutable reference to internal buffer.**

The comment warns "do not mutate," but returning the internal `lapBuf` table directly allows callers to acc...

2. _🧹 Nitpick_ | _🔵 Trivial_

**Defensive trim is unreachable given early break.**

The `while `#out` > 3` loop at lines 73-75 can never execute because the loop at line 35 breaks when `#out >= 3`, and t...

3. _🛠️ Refactor suggestion_ | _🟠 Major_

**Consolidate ID/sanitization logic through `csp_helpers` to avoid drift.**

`M.sessionKey` currently reimplements guarded `ac.get*` access and local sanitization...

---

### 12. code / that / alpha

- **Count:** 14 comments
- **Severity:** maintainability
- **Preventability:** typecheck
- **Affected Files:** tests/test_design_conformance.py, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**Representative Examples:**

1. **suggestion:** Function name/doc suggests scrollable content but implementation is only wrapped text with no scroll region.

The docstring promises a "Scrollable debrief", but this only renders wrapp...

2. **issue (testing):** Docstring mentions title text alpha, but the test currently only verifies background alpha

The assertions only check the background rgbm alpha, so the title text alpha requiremen...

3. This docstring claims the test checks both (1) background alpha < 1 and (2) title text alpha == 1 “before fade”, but the assertions only validate the background alpha. Either add an assertion that the...

---

### 13. code / sourcery / review

- **Count:** 13 comments
- **Severity:** maintainability
- **Preventability:** architecture

**Representative Examples:**

1. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Adds a gated render-diagnostics config, improves coaching HUD visibility (including a new BMW-based font and a main-win...

2. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Refactors coaching timers to use a wall-clock-style countdown, fixes alpha/fade behavior so coaching is visible for the...

3. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements session journal export when returning to the AC main menu after a driving stint with at least one completed ...

---

### 14. code / summary / details

- **Count:** 12 comments
- **Severity:** bug
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/racing_line.lua

**Representative Examples:**

1. `setBlendMode`/`setDepthMode` are set but never restored. Render state may persist across frames, so this can inadvertently affect `trackMarkers.draw` in the next frame (markers are drawn before the l...

2. <!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...

3. <!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...

---

### 15. code / fallback / this

- **Count:** 10 comments
- **Severity:** bug
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/hud_settings.lua, src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/setup_reader.lua

**Representative Examples:**

1. `ac.getCarID(0)` / `ac.getTrackID()` are invoked directly. Elsewhere in this module you already defensively handle CSP API variability (e.g., `ac.getTrackLayout and ac.getTrackLayout()`). If these fun...

2. `ac.getTrackID()` is called unguarded. Since this function is used during initialization and this PR’s goal is to avoid CSP API-related startup crashes, consider guarding this call similarly to `ac.ge...

3. The new gating logic returns early unless `render.debugLine` exists (or legacy `drawSphere` fallback). This regresses CSP builds where `render.debugSphere` and/or `render.debugCross` exist but `render...

---

### 16. code / summary / details

- **Count:** 10 comments
- **Severity:** nit
- **Preventability:** architecture
- **Affected Files:** src/ac_copilot_trainer/modules/tire_monitor.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/racing_line.lua

**Representative Examples:**

1. `lockupFlash()` is unlikely to ever return true for a real lockup: in `update()` the moment `slipT` reaches `LOCKUP_HOLD` it is immediately reset to 0 after logging, so the HUD will never see `slipT` ...

2. `math.floor(bestLineN / 2)` can produce index `0` when `bestLineN == 1` (Lua arrays are 1-based), and for odd lengths it doesn’t select the true midpoint (e.g., N=3 gives 1). Use a 1-based midpoint ca...

3. LuaDoc type annotation for `speedColor()` says `---@return table`, but it returns an `rgbm(...)` color object. Updating the return type improves editor/tooling accuracy and matches how the value is us...

---

### 17. code / segment / brake

- **Count:** 8 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Refresh hint on corner-to-brake transitions**

In `realtime_coaching.tick`, the brake-phase branch only calls `s...

2. `findNextBrakeOrCorner()` linearly scans all segments every frame while on straights (`for i = 1, #segments do ...`). That makes the “approaching” detection O(n) per tick and undermines the stated O(1...

3. `cornerLabelForBrake()` uses `math.abs(s.brakeSpline - brakeSeg.s0)` to match a brake segment to its corner. This breaks for wrap-around near spline 0/1 (e.g., brakeSpline=0.001, s0=0.989 → abs≈0.988)...

---

### 18. details / summary / code

- **Count:** 8 comments
- **Severity:** maintainability
- **Preventability:** automation
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_hints.lua, tools/ai_sidecar/server.py

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟠 Major_

**Coaching hint logic appears inverted for high entry speed.**

When `en > bn + 5` (driver entering faster than reference), the hint suggests "try braking slightly la...

2. _🧹 Nitpick_ | _🔵 Trivial_

**Minor: Docstring says "echo server" but the server only logs, doesn't echo.**

The server receives and logs messages but doesn't send responses back. Consider updating the...

3. _🧹 Nitpick_ | _🔵 Trivial_

**Clarify helper scope to avoid over-constraining valid direct field reads.**

Line 20 currently implies all `sim`/`car` accesses should be helper-wrapped. In practice, this...

---

### 19. code / missing / keys

- **Count:** 7 comments
- **Severity:** maintainability
- **Preventability:** typecheck
- **Affected Files:** tests/test_session_journal.py, src/ac_copilot_trainer/modules/hud_settings.lua, src/ac_copilot_trainer/modules/throttle_detection.lua

**Representative Examples:**

1. `analyzeTrace()` assumes each sample has `eMs` (used for `dtEst`, `dtMs`, lap duration, etc.), but the parameter annotation only mentions `{ throttle, brake }[]` and there’s no guard for missing/inval...

2. `denseArray()` claims to rebuild from “sparse / string-keyed” decoded JSON arrays, but it only considers numeric keys. If `JSON.parse` yields string keys like "1", "2" (common in some Lua JSON libs / ...

3. **suggestion (testing):** Add companion tests for extra/unknown keys and for missing required nested keys like `car.id` and `track.id`

This currently only asserts behavior for missing top-level keys....

---

### 20. code / removing / consider

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/track_markers.lua, tests/test_design_conformance.py

**Representative Examples:**

1. `sim` is no longer used in this function after removing the billboard/rectangle path. To make the unused parameter intentional (and quiet linters), consider renaming it to `_sim` (or removing it if th...

2. `state.coachingUntil` is now effectively unused for visibility/remaining time (coaching uses `coachingRemainSec`), but it’s still kept in state and updated on lap completion. Consider removing `coachi...

3. `state.coachingUntil` is now only initialized/reset and updated on lap completion, but no longer used for any visibility logic (search shows no other reads). Keeping it updated alongside `coachingRema...

---

## Most Churned Files

Files with the most review comments:

- `src/ac_copilot_trainer/ac_copilot_trainer.lua`: 114 comments
- `src/ac_copilot_trainer/modules/track_markers.lua`: 52 comments
- `src/ac_copilot_trainer/modules/racing_line.lua`: 45 comments
- `src/ac_copilot_trainer/modules/hud.lua`: 42 comments
- `tests/test_design_conformance.py`: 31 comments
- `src/ac_copilot_trainer/modules/coaching_overlay.lua`: 30 comments
- `src/ac_copilot_trainer/modules/realtime_coaching.lua`: 25 comments
- `src/ac_copilot_trainer/modules/persistence.lua`: 22 comments
- `src/ac_copilot_trainer/modules/coaching_hints.lua`: 20 comments
- `tools/ai_sidecar/features.py`: 17 comments

---

## Suggested Next Actions

### Proposed Cursor Rule Updates

**Automation Gates (lint/format/pre-commit):**

- Add check for: details / summary / code
- Add check for: code / summary / details
- Add check for: code / label / empty
- Add check for: code / thanks / test
- Add check for: code / uses / different

**Type Checking:**

- Add type check for: details / summary / code
- Add type check for: code / copilot / instructions
- Add type check for: code / summary / details
- Add type check for: code / window / code_block
- Add type check for: code_block / code / script

**Repository Guidelines:**

- Document pattern: details / summary / code
- Document pattern: code / color / prefers
- Document pattern: codex / your / usage
- Document pattern: code / summary / details
- Document pattern: code / fallback / this

**Architecture/Agent Rules:**

- Add agent rule: code / sourcery / review
- Add agent rule: code / summary / details
- Add agent rule: code / ranking / fastest
- Add agent rule: code / with / review
- Add agent rule: code / thanks / review_comment_addressed

### Proposed CI/Pre-commit Changes

- Add automated checks for common formatting/import issues
- Add type checking to CI pipeline
- Add pre-commit hooks for common issues

### Proposed Documentation Updates

- Update AGENTS.md with common patterns
- Add examples to documentation
- Create troubleshooting guide

---

## Appendix: All Comment Clusters

### Cluster 5: details / summary / code

- Count: 195
- Severity: reliability
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/track_markers.lua, docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**Session-exit persistence never runs, and the next session reuses stale state.**

On the first main-menu frame, `script.update()` calls `persistSnapshot()`, but `per...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Guard no-car frames before touching HUD, telemetry, or lap state.**

`tryLoadDisk()` and `persistSnapshot()` already treat `not car` as a valid runtime state, but `...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Duration tracking misses first-frame time, causing false negatives.**

Current logic starts timing at `0` and only accumulates on later frames, so brake holds can b...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Don't cap a time-based buffer by an assumed 120 Hz sample rate.**

`bufferSeconds` is part of the module contract, but `maxSamples = bufferSeconds * 120` drops data...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Clear lap-scoped brake points when `lapCount` goes backwards.**

This logic only handles `lc > state.lastLapCount`. If the player restarts or returns to pits and `l...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Don't silently drop failed persistence writes.**

`persistence.save()` returns `false` on encode/open/write failure, but both save helpers ignore that result. On th...
```

---

### Cluster 7: code / color / prefers

- Count: 182
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
### Persist on session exit always exits early, losing data

**High Severity**

<!-- DESCRIPTION START -->
`persistSnapshot` is called inside the `sim.isInMainMenu` branch of `script.update`, but `per...
```

```
### Silent fallback discards data when JSON unavailable

**Medium Severity**

<!-- DESCRIPTION START -->
`jsonEncode` silently returns `"{}"` when the `JSON` global is missing, causing `persistence.sa...
```

```
### Session state not reset when car/track changes

**High Severity**

<!-- DESCRIPTION START -->
With `LAZY=PARTIAL`, the script persists across session changes. When transitioning to the main menu, ...
```

```
### Session restart causes stale brake data contamination

**Medium Severity**

<!-- DESCRIPTION START -->
When a player restarts a session without going through the main menu, `car.lapCount` drops fr...
```

```
### Brake event spanning lap boundary lost from completed lap

**Low Severity**

<!-- DESCRIPTION START -->
`Detector:update` only emits a `BrakeEvent` when the brake is released. If a driver is braki...
```

```
### Shell injection risk via unsanitized path in `ensureDir`

**Medium Severity**

<!-- DESCRIPTION START -->
`ensureDir` passes string-concatenated paths directly to `os.execute` without sanitizing t...
```

---

### Cluster 11: details / summary / code

- Count: 92
- Severity: bug
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/persistence.lua, tests/test_focus_practice.py

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**Parameters `car` and `sim` are now unused in `sessionKey`.**

The function signature still accepts `car` and `sim` parameters, but the implementation now derives all ident...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Consider splitting compound assertion for clearer failure messages.**

When this assertion fails, it's unclear which part failed. Splitting improves debuggability:

```dif...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
No actionable comments were generated in the recent review. 🎉

<details>
<summary>ℹ️ Recent review info</summary>

<details>
<sum...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
No actionable comments were generated in the recent review. 🎉

<details>
<summary>ℹ️ Recent review info</summary>

<details>
<sum...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: failure by coderabbit.ai -->

> [!CAUTION]
> ## Review failed
> 
> The pull request is cl...
```

---

### Cluster 10: code / copilot / instructions

- Count: 67
- Severity: nit
- Preventability: typecheck
- Affected Files: N/A

**All Examples:**

```
## Pull request overview

Implements Issue #7 feature set for the AC Copilot Trainer Lua app: full-lap telemetry traces, live delta + sector splits, on-track 3D brake markers, approach HUD, and thrott...
```

```
## Pull request overview

Copilot reviewed 10 out of 10 changed files in this pull request and generated 2 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...
```

```
## Pull request overview

Copilot reviewed 10 out of 10 changed files in this pull request and generated no new comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/ins...
```

```
## Pull request overview

Copilot reviewed 10 out of 10 changed files in this pull request and generated no new comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/ins...
```

```
## Pull request overview

Copilot reviewed 10 out of 10 changed files in this pull request and generated 2 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...
```

```
## Pull request overview

Copilot reviewed 10 out of 10 changed files in this pull request and generated 3 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...
```

---

### Cluster 36: code / summary / details

- Count: 29
- Severity: bug
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, tools/ai_sidecar/session.py, src/ac_copilot_trainer/modules/session_journal.lua, src/ac_copilot_trainer/modules/corner_names.lua

**All Examples:**

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Unguarded ui.getcursor deref <code>🐞 Bug</code> <code>☼ Reliability</code>

<pre>
...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>coachingholdseconds</i></b> set to 30s <code>📎 Requirement gap</code> <code>...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

2\. Timer freezes when car nil <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
<b...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>coachingmaxvisiblehints</i></b> change untested <code>📘 Rule violation</code...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Ranking on new pb <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
LapComparis...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Debrief not journaled <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
<b><i>s...
```

---

### Cluster 9: codex / your / usage

- Count: 28
- Severity: nit
- Preventability: guideline
- Affected Files: N/A

**All Examples:**

```
You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).
```

```
You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).
```

```
You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).
```

```
You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).
```

```
You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).
```

```
You have reached your Codex usage limits for code reviews. You can see your limits in the [Codex usage dashboard](https://chatgpt.com/codex/settings/usage).
```

---

### Cluster 18: code / window / code_block

- Count: 27
- Severity: maintainability
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/session_journal.lua

**All Examples:**

```
`bestCornerFeatures` is now persisted/loaded in `ac_copilot_trainer.lua`, but `normalizeLoaded()` doesn’t validate/coerce it like the other new v3 fields. For schema hygiene (and to avoid surprising t...
```

```
`guessSetupIniPath()` flattens the track identifier with `gsub("[^%w%.%-_]+", "_")`, which can break setup resolution for tracks with layout subfolders (e.g. `track/layout`), since the actual setups p...
```

```
With `lastTry` initialized to 0 and the reconnect guard `t - lastTry < RECONNECT_SEC`, the first connection attempt won’t happen until ~5s after session start (when `sim.time` reaches 5). If you want ...
```

```
`math.floor(bestLineN / 2)` can be 0 when `bestLineN` is 1, so `line[mid]` won’t log even though data exists. It’s also not the true midpoint for odd lengths (e.g., 3 → 1). Consider using a 1-based mi...
```

```
`manifest.ini` declares two windows using the same section header `[WINDOW_...]`. INI parsers typically treat duplicate section names as overwriting/merging, which can cause the original main window d...
```

```
Per-segment coloring treats `segSpeed == 0` as “missing speed data” and falls back to `fallbackColor`. But 0 km/h is a valid speed and should map to the red/yellow range per `speedColor()`. Consider c...
```

---

### Cluster 20: code_block / code / script

- Count: 27
- Severity: bug
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/persistence.lua, src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cat -n src/ac_copilot_trainer/modules/persistence.lua | head -100
```

Repository: agorokh...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, let's understand the file structure and the sessionKey usage
fd -t f "persistence...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, let's check the file exists and examine the context around lines 204-209
fd ac_co...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
set -euo pipefail

python - <<'PY'
import re
import urllib.request

html = url...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cat -n src/ac_copilot_trainer/modules/track_markers.lua | head -70
```

Repository: agorok...
```

```
_⚠️ Potential issue_ | _🔴 Critical_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, let's verify the code at line 820 of ac_copilot_trainer.lua
cat -n src/ac_copi...
```

---

### Cluster 0: code / this / comments

- Count: 25
- Severity: bug
- Preventability: typecheck
- Affected Files: scripts/check_csp_ui_safety.py, src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/modules/track_markers.lua, tests/test_ai_sidecar.py, src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
`snapToTrack()` accesses `hit.position.y` directly when `hit` is a table. If `hit.position` is a userdata (common for vec3-like types) or has a metamethod that throws on `.y`, this will error outside ...
```

```
This test name is misleading: `tools.ai_sidecar.server` can be imported even when `websockets` is missing (the import happens inside `_run`). Either rename the test to reflect what it checks, or chang...
```

```
`lastTry` starts at 0, so with `sim.time` starting near 0 this prevents the first connection attempt for ~5 seconds (`t - lastTry < RECONNECT_SEC`). If the goal is to connect ASAP when configured, ini...
```

```
`configure()` clears `sock` but doesn’t reset `lastTry`, so changing `wsSidecarUrl` at runtime can delay reconnect attempts for up to `RECONNECT_SEC`. Reset `lastTry` in `configure()` (or trigger an i...
```

```
In `drawLineStrip`, the new comment documents `render.debugLine(from, to, color, colorEnd)` but the call only passes a single color argument. If CSP expects a `colorEnd`, this will throw (and the surr...
```

```
`ui.windowSize()` can return nil (or `vec2` can be unavailable), but the code unconditionally indexes `sz.x`/`sz.y`. This can raise an error and break the overlay draw. Consider defaulting to fixed di...
```

---

### Cluster 2: code / summary / details

- Count: 24
- Severity: maintainability
- Preventability: typecheck
- Affected Files: tests/test_lua_runtime_smoke.py, src/ac_copilot_trainer/modules/delta.lua, tools/ai_sidecar/__init__.py, src/ac_copilot_trainer/modules/throttle_detection.lua, src/ac_copilot_trainer/modules/spline_parser.lua

**All Examples:**

```
`analyzeTrace()` documents `trace` elements as only `{ throttle, brake }`, but the implementation assumes `eMs` exists (used for dt estimation and segment timing). This mismatch can cause runtime erro...
```

```
Lua annotations for `LapTraceSample` in this module only declare `spline` and `eMs`, but `bestSpeedKmhAtSpline()` interpolates the `speed` field. This mismatch makes the contract unclear for callers a...
```

```
`LapTraceSample.speed` is annotated as optional (`number|nil`), but `interpAtSpline()` does arithmetic on the chosen field without guarding against `nil` (`va + t * (vb - va)`), which will raise at ru...
```

```
`guessFastLanePath()` sanitizes the track string by replacing non-[%w%.%-_] characters with “_”. That will break tracks with layouts stored as subfolders (e.g. `track/layout`), causing `content/tracks...
```

```
`__all__` exports `main`, but the package doesn’t define or import a `main` symbol (it only exists as `tools.ai_sidecar.server.main`). This makes `from tools.ai_sidecar import main` fail despite being...
```

```
`__all__` advertises `main`, but the package doesn’t define or import `main` (it’s in `server.py`). This makes `from tools.ai_sidecar import main` / star-import behavior inconsistent. Either import/re...
```

---

### Cluster 6: code / details / summary

- Count: 19
- Severity: perf
- Preventability: test
- Affected Files: src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/telemetry.lua, src/ac_copilot_trainer/modules/coaching_hints.lua, src/ac_copilot_trainer/modules/persistence.lua, scripts/check_csp_api.py

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**`getTrace()` returns a mutable reference to internal buffer.**

The comment warns "do not mutate," but returning the internal `lapBuf` table directly allows callers to acc...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Defensive trim is unreachable given early break.**

The `while `#out` > 3` loop at lines 73-75 can never execute because the loop at line 35 breaks when `#out >= 3`, and t...
```

```
_🛠️ Refactor suggestion_ | _🟠 Major_

**Consolidate ID/sanitization logic through `csp_helpers` to avoid drift.**

`M.sessionKey` currently reimplements guarded `ac.get*` access and local sanitization...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Redundant nested pcall structure.**

The outer `pcall` on line 111 wraps an anonymous function that contains additional `pcall` calls for each render primitive. If any inn...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Minor: `upDir` could be hoisted to module scope.**

The `upDir` vector is constant `(0, 1, 0)` and is recreated on every `draw()` call. Hoisting it to module scope avoids ...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**String stripping doesn't handle escaped quotes or long-string literals.**

Lua strings with escaped quotes (`"foo\"bar"`) or long-string syntax (`[[...]]`) aren't fully st...
```

---

### Cluster 15: code / that / alpha

- Count: 14
- Severity: maintainability
- Preventability: typecheck
- Affected Files: tests/test_design_conformance.py, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
**suggestion:** Function name/doc suggests scrollable content but implementation is only wrapped text with no scroll region.

The docstring promises a "Scrollable debrief", but this only renders wrapp...
```

```
**issue (testing):** Docstring mentions title text alpha, but the test currently only verifies background alpha

The assertions only check the background rgbm alpha, so the title text alpha requiremen...
```

```
This docstring claims the test checks both (1) background alpha < 1 and (2) title text alpha == 1 “before fade”, but the assertions only validate the background alpha. Either add an assertion that the...
```

```
**suggestion (testing):** Extend speedColor test to validate actual color branching (green/red/white) and edge thresholds, not just the presence of the threshold constant.

PC-02 currently only verifi...
```

```
Design brief/PR description says text should be fully opaque (background only is transparent), but COLOR_BRAND is defined with alpha 0.70 and used for footer text. Consider making footer text alpha 1....
```

```
**suggestion (testing):** Add an assertion for 100% text opacity to match the acceptance criterion.

PE-03 currently only validates the background alpha and some token names. Please also assert that t...
```

---

### Cluster 28: code / sourcery / review

- Count: 13
- Severity: maintainability
- Preventability: architecture
- Affected Files: N/A

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Adds a gated render-diagnostics config, improves coaching HUD visibility (including a new BMW-based font and a main-win...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Refactors coaching timers to use a wall-clock-style countdown, fixes alpha/fade behavior so coaching is visible for the...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements session journal export when returning to the AC main menu after a driving stint with at least one completed ...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Adds a configurable cap for how many post-lap coaching hints are visibly rendered, threads this setting through the HUD...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements WebSocket protocol v1 between the Lua app and Python sidecar, adds structured bidirectional messaging (inclu...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements per-lap corner telemetry feature extraction and delta-based improvement ranking in the AI sidecar, wires the...
```

---

### Cluster 42: code / summary / details

- Count: 12
- Severity: bug
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
`setBlendMode`/`setDepthMode` are set but never restored. Render state may persist across frames, so this can inadvertently affect `trackMarkers.draw` in the next frame (markers are drawn before the l...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...
```

---

### Cluster 3: code / fallback / this

- Count: 10
- Severity: bug
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/hud_settings.lua, src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/setup_reader.lua, src/ac_copilot_trainer/modules/spline_parser.lua, src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
`ac.getCarID(0)` / `ac.getTrackID()` are invoked directly. Elsewhere in this module you already defensively handle CSP API variability (e.g., `ac.getTrackLayout and ac.getTrackLayout()`). If these fun...
```

```
`ac.getTrackID()` is called unguarded. Since this function is used during initialization and this PR’s goal is to avoid CSP API-related startup crashes, consider guarding this call similarly to `ac.ge...
```

```
The new gating logic returns early unless `render.debugLine` exists (or legacy `drawSphere` fallback). This regresses CSP builds where `render.debugSphere` and/or `render.debugCross` exist but `render...
```

```
Primitive budget calculation in the non-line branch can undercount when `render.debugSphere` exists but has been marked unusable (`debugSphereUsable == false`) and you fall back to `render.drawSphere`...
```

```
The implementation doesn’t currently match the stated fallback chain (quad → glBegin → debugLine): if `render.quad` exists but its `pcall` fails, the code does not attempt the GL or debugLine paths fo...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `ui.textWrapped` function might not be available in all versions of CSP/ImGui. It is safer to check for its existence or use...
```

---

### Cluster 4: code / summary / details

- Count: 10
- Severity: nit
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/tire_monitor.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/racing_line.lua, tools/ai_sidecar/features.py, tools/ai_sidecar/session.py

**All Examples:**

```
`lockupFlash()` is unlikely to ever return true for a real lockup: in `update()` the moment `slipT` reaches `LOCKUP_HOLD` it is immediately reset to 0 after logging, so the HUD will never see `slipT` ...
```

```
`math.floor(bestLineN / 2)` can produce index `0` when `bestLineN == 1` (Lua arrays are 1-based), and for odd lengths it doesn’t select the true midpoint (e.g., N=3 gives 1). Use a 1-based midpoint ca...
```

```
LuaDoc type annotation for `speedColor()` says `---@return table`, but it returns an `rgbm(...)` color object. Updating the return type improves editor/tooling accuracy and matches how the value is us...
```

```
> Guard telemetry floats against NaN/inf in `_as_float`.

Addressed: `_as_float` returns `None` when `not math.isfinite(f)`; added `test_as_float_rejects_non_finite`. `17b1241`.
```

```
> Add boolean guard before integer coercion for `lapTimeMs` (`True` → 1 ms PB).

Addressed: `_positive_lap_time_ms` returns `None` when `isinstance(raw, bool)`; added `test_lap_time_true_is_ignored_fo...
```

```
In `drawProgressBar`, the guard `if not ui.drawRectFilled ...` will throw if `ui` itself is nil (attempt to index a nil value). Even though the current caller checks `ui` first, this helper is safer a...
```

---

### Cluster 25: code / segment / brake

- Count: 8
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Refresh hint on corner-to-brake transitions**

In `realtime_coaching.tick`, the brake-phase branch only calls `s...
```

```
`findNextBrakeOrCorner()` linearly scans all segments every frame while on straights (`for i = 1, #segments do ...`). That makes the “approaching” detection O(n) per tick and undermines the stated O(1...
```

```
`cornerLabelForBrake()` uses `math.abs(s.brakeSpline - brakeSeg.s0)` to match a brake segment to its corner. This breaks for wrap-around near spline 0/1 (e.g., brakeSpline=0.001, s0=0.989 → abs≈0.988)...
```

```
`EXIT_WINDOW_SPLINE` is defined but never used, and the “exiting” phase currently lasts at most one tick (corner→straight sets exiting, next straight tick sets straight+clears hint). If exit-phase coa...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The spline distance calculation does not account for the track wrap-around at the start/finish line. If a brake segment starts a...
```

```
`findSegment()` returns whatever segment index is stored in the bucket without verifying that the spline position is actually inside that segment’s [s0,s1) range (with wrap handling). Because buckets ...
```

---

### Cluster 34: details / summary / code

- Count: 8
- Severity: maintainability
- Preventability: automation
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_hints.lua, tools/ai_sidecar/server.py, docs/01_Vault/AcCopilotTrainer/01_Decisions/csp-api-field-safety.md, src/ac_copilot_trainer/modules/csp_helpers.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**Coaching hint logic appears inverted for high entry speed.**

When `en > bn + 5` (driver entering faster than reference), the hint suggests "try braking slightly la...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Minor: Docstring says "echo server" but the server only logs, doesn't echo.**

The server receives and logs messages but doesn't send responses back. Consider updating the...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Clarify helper scope to avoid over-constraining valid direct field reads.**

Line 20 currently implies all `sim`/`car` accesses should be helper-wrapped. In practice, this...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Keep Draw3D diagnostics opt-in by default.**

With this set to `true`, all sessions emit periodic debug logs, which can quickly flood AC logs outside targeted debug...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🌐 Web query:

`For Assetto Corsa CSP Lua `ac.StateSim`, what are the units of `sim.gameTime` and `sim.time`? Are both s...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cat -n tests/test_scripts/test_check_csp_api.py | sed -n '40,75p'
```

Repository: agorokh...
```

---

### Cluster 32: code / missing / keys

- Count: 7
- Severity: maintainability
- Preventability: typecheck
- Affected Files: tests/test_session_journal.py, src/ac_copilot_trainer/modules/hud_settings.lua, src/ac_copilot_trainer/modules/throttle_detection.lua, src/ac_copilot_trainer/modules/persistence.lua, tests/test_focus_practice.py

**All Examples:**

```
`analyzeTrace()` assumes each sample has `eMs` (used for `dtEst`, `dtMs`, lap duration, etc.), but the parameter annotation only mentions `{ throttle, brake }[]` and there’s no guard for missing/inval...
```

```
`denseArray()` claims to rebuild from “sparse / string-keyed” decoded JSON arrays, but it only considers numeric keys. If `JSON.parse` yields string keys like "1", "2" (common in some Lua JSON libs / ...
```

```
**suggestion (testing):** Add companion tests for extra/unknown keys and for missing required nested keys like `car.id` and `track.id`

This currently only asserts behavior for missing top-level keys....
```

```
**suggestion (testing):** Extend coverage to numeric fields (`conditions.track_grip`, `summary.*`, `lap_history[*].lap_ms`) and ensure invalid types are rejected while `None` is allowed

The numeric-t...
```

```
**suggestion (testing):** Add more `filter_coaching_hints` scenarios, including empty hints, empty focus map, and non-matching text

Current tests only cover a basic focus match, inactive focus, and t...
```

```
**suggestion:** Clarify semantics of config booleans when using `config[key] == true` to derive checkbox state.

This treats any non-`true` value (including `nil`) as unchecked, which aligns with your...
```

---

### Cluster 12: code / removing / consider

- Count: 6
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/track_markers.lua, tests/test_design_conformance.py, docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md

**All Examples:**

```
`sim` is no longer used in this function after removing the billboard/rectangle path. To make the unused parameter intentional (and quiet linters), consider renaming it to `_sim` (or removing it if th...
```

```
`state.coachingUntil` is now effectively unused for visibility/remaining time (coaching uses `coachingRemainSec`), but it’s still kept in state and updated on lap completion. Consider removing `coachi...
```

```
`state.coachingUntil` is now only initialized/reset and updated on lap completion, but no longer used for any visibility logic (search shows no other reads). Keeping it updated alongside `coachingRema...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

Using `re.match` with a `^` anchor prevents matching EmmyLua tags that are indented. Switching to `re.search` (and removing the ...
```

```
This line hardcodes a specific `main` commit hash and claims all `feat/issue-57-*` branches are deleted on origin. Those details will become stale quickly and can be misleading for future sessions; co...
```

```
Pinning the branch state to a specific `main` commit hash in the "Current focus" header is likely to go stale immediately as `main` advances. Consider either omitting the hash or qualifying it as "as ...
```

---

### Cluster 30: speed / code / when

- Count: 6
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/coaching_hints.lua, src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
This condition fires when the *current* entry speed is higher than the reference (`en > bn + 5`), but the hint tells the driver to brake later—which would typically increase entry speed further. The c...
```

```
The min-speed coaching lines omit units (“km/h”), while the entry-speed line includes them. For readability/consistency in the HUD, include units in these formatted strings too.
```suggestion
        ...
```

```
`traceToLine()` now preserves `speed=nil` (line 71) to distinguish “no data” from 0 km/h, but the downsampling branch in the same function still coerces missing speed to `0` (via `or 0`). This makes b...
```

```
Per-segment speed coloring treats a missing speed value as `0` when only one of `a.speed`/`b.speed` is available (`(a.speed or 0) + (b.speed or 0)`), which will skew the average toward red even if the...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The logic for calculating `apexSpeedKmh` as an average of entry and exit speed is questionable. In racing telemetry, the apex sp...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The fixed horizontal offset of 70 pixels for the "km/h" unit label may cause overlap when the speed value is three digits (e.g.,...
```

---

### Cluster 59: code / ranking / fastest

- Count: 6
- Severity: nit
- Preventability: architecture
- Affected Files: tools/ai_sidecar/session.py, src/ac_copilot_trainer/ac_copilot_trainer.lua, docs/10_Development/12_WS_Sidecar_Protocol.md

**All Examples:**

```
The fallback is shown whenever `remaining <= 0`, which means after coaching hints expire the coaching window will continuously show “Complete a lap…” for the rest of the session (until next lap), rath...
```

```
LapComparisonState computes and returns an improvement ranking *before* updating the PB when the current lap is faster than the stored best. That means a new PB lap can still return regressions vs the...
```

```
> Ranking on new pb — computes improvement ranking against the previous PB before updating PB, so a new fastest lap could still get improvementRanking.

Addressed: `improvement_ranking_for` now treats...
```

```
> LapComparisonState computes ranking before updating PB when the current lap is faster than the stored best.

Addressed: Same as Qodo thread — new PB path updates reference first and returns an empty...
```

```
LapComparisonState only updates the stored PB when `telemetry.corners` parses to a non-empty table. If a faster lap arrives without `telemetry.corners`, `_best_time_ms` won’t update; a later slower la...
```

```
The protocol doc says ranking is computed vs “the fastest lap seen on that WebSocket connection”, but the current implementation only considers laps where `telemetry.corners` is present (empty/missing...
```

---

### Cluster 22: code / with / review

- Count: 5
- Severity: maintainability
- Preventability: architecture
- Affected Files: N/A

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements the final Phase 5 Part E HUD redesign by replacing the debug-heavy main HUD window with a polished active su...
```

```
## Code Review

This pull request implements a multi-window layout by moving detailed telemetry, statistics, and configuration toggles from the main HUD into a new dedicated Settings window. It also i...
```

```
## Code Review

This pull request introduces a design conformance checklist and a corresponding Python test suite to ensure the implementation aligns with Figma design requirements. The test suite use...
```

```
## Code Review

This pull request introduces a polished approach telemetry panel to the coaching overlay, providing real-time speed comparisons and distance-to-braking-point data when approaching corn...
```

```
## Code Review

This pull request introduces a new 'Active Suggestion' panel to the HUD, replacing legacy debug displays with a polished, fading UI component. It also integrates focus practice indicat...
```

---

### Cluster 35: code / summary / details

- Count: 5
- Severity: maintainability
- Preventability: automation
- Affected Files: N/A

**All Examples:**

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: review paused by coderabbit.ai -->

> [!NOTE]
> ## Reviews paused
> 
> It looks like this...
```

```
<h3>Review Summary by Qodo</h3>

Add session journal export with schema v1 validation

<code>✨ Enhancement</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.svg" height=...
```

```
## Code Review

This pull request introduces a session journal system that exports driving stint data as JSON files when returning to the main menu. The changes include a new Lua module for generating...
```

```
## Code Review

This pull request introduces a `coachingMaxVisibleHints` configuration setting to limit the number of coaching hints displayed in the HUD to a range of 1–3. The changes include a norma...
```

```
## Code Review

This pull request implements the WebSocket sidecar protocol (v1), enabling the Lua application to receive coaching hints from a Python sidecar. Key updates include inbound message poll...
```

---

### Cluster 60: code / thanks / review_comment_addressed

- Count: 5
- Severity: nit
- Preventability: architecture
- Affected Files: tests/test_ai_sidecar_improvement.py, tools/ai_sidecar/features.py, tools/ai_sidecar/session.py, tools/ai_sidecar/server.py

**All Examples:**

```
`@agorokh`, thanks for the confirmation! The explicit non-empty assertions before `data[0]` / `ranking[0]` indexing will now surface a clear `AssertionError` (with a meaningful message) instead of an ...
```

```
`@agorokh`, thanks for the fix and the dedicated test — adding `math.isfinite(f)` guard and covering it with `test_as_float_rejects_non_finite` is exactly the right approach. 

<!-- <review_comment_ad...
```

```
`@agorokh`, thanks for the confirmation! The boolean guard and the accompanying test `test_lap_time_true_is_ignored_for_pb` are exactly the right approach here — rejecting `True`/`False` before the `i...
```

```
`@agorokh`, thanks for the thorough fix! The implemented handling is actually more complete than my original suggestion — the `PermissionError` catch and the `{e.filename!r}` / `at char {e.pos}` addit...
```

```
`@agorokh`, thanks for running the formatter and pushing the fix — the long line in `test_extract_corner_table_ignores_unknown_and_malformed` should now be properly wrapped and `ruff format --check` s...
```

---

### Cluster 1: code / callback / function

- Count: 4
- Severity: maintainability
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, tests/test_design_conformance.py, tests/test_lua_runtime_smoke.py

**All Examples:**

```
_⚠️ Potential issue_ | _🔴 Critical_

<details>
<summary>🧩 Analysis chain</summary>

🌐 Web query:

`Does CSP (Content Manager Shaders Patch) Assetto Corsa Lua API ui.treeNode accept a callback function...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The logic in `visibleHintCount` for normalizing `maxVisible` (including `tonumber` conversion, NaN checks, rounding, and clampin...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The regex for `push` is inconsistent with `pop` and overly restrictive by requiring empty parentheses. Since `push` is called as...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

This test only verifies that the module loads. Expanding it to call `draw(vm)` would exercise the UI logic and help catch issues...
```

---

### Cluster 13: code / details / summary

- Count: 4
- Severity: security
- Preventability: architecture
- Affected Files: N/A

**All Examples:**

```
Hey - I've found 1 security issue, and left some high level feedback:

**Security issues**:
- Insecure WebSocket Detected. WebSocket Secure (wss) should be used for all WebSocket connections. ([link](...
```

```
Hey - I've found 1 issue, and left some high level feedback:

- `ensureCornerIniLoaded` takes a `sim0` parameter that is never used; consider removing the argument (and corresponding call-site paramet...
```

```
Hey - I've found 1 issue

<details>
<summary>Prompt for AI Agents</summary>

~~~markdown
Please address the comments from this code review:

## Individual Comments

### Comment 1
<location path="src/a...
```

```
Hey - I've reviewed your changes and they look great!

***

<details>
<summary>Sourcery is free for open source - if you like our reviews please consider sharing them ✨</summary>

- [X](https://twitte...
```

---

### Cluster 17: font / code / brand

- Count: 4
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_font.lua, tests/test_design_conformance.py

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The current regex `[%w_%-]+` used to extract the font filename does not support spaces, which are common in Assetto Corsa font f...
```

```
**suggestion (testing):** Add assertions for the brand font (Syncopate) and Windows fallbacks to match the multi-font system described in the PR.

PC-06 already validates `Michroma` and `Montserrat`, ...
```

```
Same font-stack issue here: `pushNamed()` result isn’t captured, and `fontMod.pop()` is called without the required kind token, so the font won’t be popped.
```suggestion
  local titleFont = fontMod.p...
```

```
Same font-stack issue here: capture the return value of `pushNamed("brand", ...)` and pass it into `fontMod.pop(kind)`; otherwise the font stack will grow each draw.
```suggestion
    local brandFontK...
```

---

### Cluster 21: code / this / consider

- Count: 4
- Severity: bug
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/persistence.lua, src/ac_copilot_trainer/modules/setup_reader.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
`ac.getTrackFullID("/")` is called unguarded; if this API isn’t present in a given CSP build, this will throw “attempt to call field … (a nil value)” and reintroduce the startup crash this PR is tryin...
```

```
Same as above: `ac.getCarID(0)` / `ac.getTrackID()` are called unguarded when building the operator-facing message. If these APIs are unavailable in some CSP builds, this helper can still throw during...
```

```
`enableDraw3DDiagnostics` is documented as “troubleshooting only”, but it’s now enabled by default. That will emit `[COPILOT]` logs every ~2s for all users and can add noise/perf overhead; consider ke...
```

```
`ui.deltaTime()` is called unconditionally. Elsewhere in the codebase UI APIs are treated as optional (guarded with `type(...)=="function"`/`pcall`), so this can crash on CSP builds where `ui.deltaTim...
```

---

### Cluster 26: current / code / speed

- Count: 4
- Severity: nit
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_hints.lua

**All Examples:**

```
`realtimeCoaching.tick()` is fed `lastLapCornerFeats` (features from the previous completed lap) rather than any current-lap telemetry, so “real-time” hints will reflect last lap’s entry/min speeds in...
```

```
`buildRealTime()` only compares **last-lap** corner features vs **best-lap** features; it doesn’t use any current-lap telemetry (e.g., current speed on approach). That means the “real-time” hint won’t...
```

```
The realtime coaching tick currently generates hints solely from `state.lastLapCornerFeats` vs `state.bestCornerFeatures`. That means hints during lap N are based on lap N-1 performance and won’t reac...
```

```
## Code Review

This pull request implements a real-time per-corner coaching engine, utilizing a state machine and a bucket-based lookup system for efficient track segment tracking. It adds real-time ...
```

---

### Cluster 8: code / textcolored / pull

- Count: 3
- Severity: maintainability
- Preventability: typecheck
- Affected Files: N/A

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Fixes CSP Lua UI misuse by standardizing ui.textColored(text, color) across HUD modules, correcting ui.checkbox semanti...
```

```
## Code Review

This pull request corrects the argument order for ui.textColored calls across multiple Lua modules to comply with the Custom Shaders Patch (CSP) API, which uses a (text, color) signatu...
```

```
## Pull request overview

P0 hotfix addressing CSP UI API mismatches that broke in-game HUD rendering, plus adding checks/tests intended to prevent regressions.

**Changes:**
- Swap argument order acr...
```

---

### Cluster 16: code / string / dash

- Count: 3
- Severity: nit
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_font.lua

**All Examples:**

```
The EmmyLua/LuaLS type annotation syntax here isn’t valid: `---@param role string "numbers"|"labels"|...` will be parsed as a description, not a string-literal union. Use a proper string-literal union...
```

```
`formatLapMs()` is returning a mojibake string ("â") instead of an actual em dash. This will render incorrectly in-game; use a proper UTF-8 em dash ("—") or construct it via `utf8.char`/byte sequenc...
```

```
`formatLapMs()` returns a mojibake string ("â") instead of an actual em dash. This will render incorrectly in-game and in screenshots; use a real em dash ("—") or construct it via UTF-8 bytes (e.g.,...
```

---

### Cluster 24: test / phases / phase

- Count: 3
- Severity: maintainability
- Preventability: architecture
- Affected Files: tests/test_design_conformance.py

**All Examples:**

```
**issue (testing):** PD-02 description says "6 phases" but tests and implementation only cover 5, which makes the test misleading.

The docstring mentions “all 6 phase string literals,” but the test a...
```

```
Test name/docstring says “six phases” (PD-02), but the assertion loop checks only 5 phase literals (and the state machine comment lists 5 states including straight). Please align the wording with what...
```

```
PD-02 comment says the state machine has "6 phase string literals", but the test only checks for 5 phases (straight/approaching/braking/corner/exiting). Update the docstring to match the actual assert...
```

---

### Cluster 29: code / that / progress

- Count: 3
- Severity: bug
- Preventability: test
- Affected Files: tests/test_session_journal.py, tests/test_design_conformance.py, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
**suggestion (testing):** Also test that a mismatched `schema_version` is rejected with the expected error message

This currently only checks the constant value. Please also add a test that modifies ...
```

```
**suggestion (testing):** Strengthen progress bar test to cover behavior around pct (0, 1, and out-of-range values), not just the existence of tokens.

PC-03 currently only checks that `drawProgressBa...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The minimum height of 180px for the approach panel is insufficient to prevent overlap between the progress bar and the branding ...
```

---

### Cluster 31: code / medium / being

- Count: 3
- Severity: bug
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/session_journal.lua, tools/ai_sidecar/improvement_ranking.py, src/ac_copilot_trainer/modules/hud_settings.lua

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

If `io.open` fails (e.g., due to permission issues or the directory not being created correctly), the function returns `false` s...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `float(regret)` call is redundant as `regret` is already a float.

```suggestion
                "priority": round(regret, 4...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `ui.textWrapped` function might not be available in all versions of CSP/ImGui. It is safer to check for its existence or use...
```

---

### Cluster 37: code / reset / ac_copilot_trainer

- Count: 3
- Severity: bug
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/render_diag.lua

**All Examples:**

```
`segDrawn` is set to `true` before the render call result is known, but it’s later used to decide whether to reset `prevTiltH` (to prevent tilt leaking across gaps). If drawing fails (`okDraw` stays f...
```

```
Diagnostic state is module-global and never resets. After `diagActive` becomes false at 60s, there’s no way to restart diagnostics for a new session (the main script resets its own state on leaving tr...
```

```
## Code Review

This pull request updates AC Copilot Trainer to version 0.4.2, transitioning the coaching countdown timer from simulation time to wall-clock time to resolve visibility issues. Key chan...
```

---

### Cluster 43: push / function / code

- Count: 3
- Severity: bug
- Preventability: typecheck
- Affected Files: tests/test_design_conformance.py, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**Style color push/pop mismatch on early returns or exceptions.**

The `ui.pushStyleColor` at line 25 may succeed, but if any code between lines 28-46 throws an exception or...
```

```
**suggestion (testing):** Font push/pop balancing is checked globally, not per draw function as the requirement states

The docstring requires each draw function to bracket fontMod.push()/fontMod.pop(...
```

```
The docstring says this verifies “Every draw function … brackets font push/pop”, but the implementation only checks total `fontMod.push()`/`fontMod.pop()` counts across the whole file. This can produc...
```

---

### Cluster 48: code / coaching / logic

- Count: 3
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/hud_settings.lua

**All Examples:**

```
Fixed: fallback now gates on `lapsCompleted == 0` so it only shows before the first lap. Once coaching fires and expires, the window returns empty (hides).
```

```
**suggestion:** Focus label derivation logic is duplicated instead of reusing `focusLabelMap`.

In `script.update`, the focus filter map is rebuilt with the same manual/auto logic already encapsulated...
```

```
The `hudEnabled` toggle here is labeled as controlling only the “main HUD window”, but `config.hudEnabled` is also used to early-return from `script.windowCoaching` (and likely other UI paths). This m...
```

---

### Cluster 54: code / rules / debrief

- Count: 3
- Severity: maintainability
- Preventability: typecheck
- Affected Files: tests/test_llm_coach.py, tools/ai_sidecar/coaching/llm_coach.py, tools/ai_sidecar/protocol.py

**All Examples:**

```
**issue (testing):** Add a test for compose_debrief preferring LLM output over the rules fallback

Currently this only exercises the failure path where `call_ollama_generate` returns `None` and we fal...
```

```
`compose_debrief()` does a blocking HTTP call to Ollama (default timeout 45s) before returning a debrief. Because the sidecar builds/sends `coaching_response` only after this returns, a slow/unreachab...
```

```
`prepare_outbound_message()` calls `compose_debrief()` inline while constructing the `coaching_response`. When Ollama is enabled, this couples protocol handling to potentially slow network I/O and del...
```

---

### Cluster 55: code / server / send

- Count: 3
- Severity: reliability
- Preventability: guideline
- Affected Files: tools/ai_sidecar/coaching/llm_coach.py

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

While `urllib.request.urlopen` is used here with a timeout, it is a synchronous blocking call. In the context of a WebSocket ser...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `_sanitize_debrief` function collapses multiple spaces into one but does not handle potential markdown-like characters or ot...
```

```
Follow-up from PR resolution pass (commits \ccd0cb1\, \0cb02a\):

- **\	ools.ai_sidecar\**: \rom .server import main\ so \__all__\ matches exports.
- **\ws_bridge\**: first connect without 5s delay;...
```

---

### Cluster 57: code / call / redundant

- Count: 3
- Severity: nit
- Preventability: typecheck
- Affected Files: tools/ai_sidecar/features.py, tests/test_ai_sidecar_improvement.py

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `aliases` dictionary is static and should be defined as a module-level constant to avoid re-allocation on every call to `_no...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `str(k)` call is redundant as keys from a JSON-loaded dictionary are already strings.

```suggestion
            nk = _norma...
```

```
> Remove redundant local import of `extract_corner_table` (already imported at module level).

Addressed: Dropped the inner import in `test_extract_corner_table_ignores_unknown_and_malformed`. Pushed ...
```

---

### Cluster 64: code / flag / still

- Count: 3
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, tools/ai_sidecar/server.py

**All Examples:**

```
`script.windowCoaching()` ignores `config.hudEnabled` (unlike `script.windowMain()`), so disabling the HUD will still render the coaching overlay (and its placeholder text). Consider honoring the same...
```

```
`ui.popStyleColor()` is executed whenever it exists, even if `ui.pushStyleColor()` wasn’t called (missing) or its pcall failed. This can underflow the style stack. Gate the pop on a local `pushed` fla...
```

```
`--no-reply` help text says “Log lap_complete only; do not send coaching_response”, but the handler still sends `analysis_error` frames on invalid JSON / non-object root even when `--no-reply` is set....
```

---

### Cluster 69: heading / brake / nearby

- Count: 3
- Severity: nit
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/track_markers.lua

**All Examples:**

```
By design: brake events are sparse point samples without stored heading data. Using car.look is the best available proxy since only nearby walls are rendered and the driver is typically heading the sa...
```

```
Acknowledged as a known limitation. Brake events are sparse point samples with no stored heading — car.look is the best available proxy. Only nearby walls are rendered (FADE_FAR culling), minimizing t...
```

```
`@agorokh`, understood — thanks for the context. Using `car.look` as a proxy makes sense given that brake point records don't carry heading data and only nearby walls (where the car's facing direction...
```

---

### Cluster 14: code / review / issue

- Count: 2
- Severity: perf
- Preventability: architecture
- Affected Files: N/A

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

<details>
<summary>Reviewer's guide (collapsed on small PRs)</summary>

## Reviewer's Guide

Updates AcCopilotTrainer system docs to reflect...
```

```
## Code Review

This pull request updates the project's internal documentation to reflect the completion and closure of Issue #57, marking the full delivery of the Phase 5 HUD architecture redesign. T...
```

---

### Cluster 19: code / label / empty

- Count: 2
- Severity: nit
- Preventability: automation
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
The HUD label says "AI line lateral (XY)", but the distance computation uses `dx = px - q.x` and `dz = pz - q.z` (XZ/ground plane), not X/Y. This is misleading for users and makes debugging offsets ha...
```

```
`focusPracticeLabel = state.focusPracticeHudSummary or nil` does not nil out an empty string (Lua treats "" as truthy), so the `or nil` is redundant and can still pass an empty label downstream. If th...
```

---

### Cluster 23: code / braking / comparisons

- Count: 2
- Severity: maintainability
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/coaching_hints.lua

**All Examples:**

```
**suggestion:** Trail-braking threshold checks can be simplified to avoid redundant comparisons.

The outer `math.abs(tb - tt) > 0.15` check is implied by `tb < tt - 0.15` and `tb > tt + 0.15`, so it ...
```

```
Hey - I've found 2 issues, and left some high level feedback:

- The `EXIT_WINDOW_SPLINE` constant in `realtime_coaching.lua` is currently unused and the `exiting` phase just piggybacks on segment kin...
```

---

### Cluster 27: code / return / annotation

- Count: 2
- Severity: nit
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/coaching_font.lua, tests/test_design_conformance.py

**All Examples:**

```
**nitpick:** The return type annotation does not match the documented kind values.

The `---@return string|nil kind: "dwrite" | "builtin"` annotation mixes a general type with a specific union. Since ...
```

```
In this test file, all existing `test_*` methods are annotated with `-> None`, but the newly added PD-01..PD-08 tests omit the return type. Please add `-> None` to these new test methods to match the ...
```

---

### Cluster 33: code / line / uses

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/session_journal.lua

**All Examples:**

```
Already addressed in commit bbfb358 — the condition at line 140 uses `line[1].speed ~= nil and line[2].speed ~= nil` (not truthiness). The line reference may have shifted after the latest push.
```

```
`journal_index.jsonl` is intended to be JSONL (one JSON object per line), but `persistence.encodeJson()` uses `JSON.stringify(..., true)` (pretty/indented output). If `JSON.stringify` includes newline...
```

---

### Cluster 38: code / docstring / says

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: tools/ai_sidecar/server.py, scripts/check_csp_api.py

**All Examples:**

```
Module docstring says “echo server”, but the handler never echoes messages back to the client (it only logs). Update the description to match actual behavior (e.g., “logging server” / “receiver”) or i...
```

```
The docstring says the script exits with code 2 for unknown `render.*` calls, but the implementation currently exits 0 when only warnings are present. Please align the documentation with actual behavi...
```

---

### Cluster 39: code / seconds / units

- Count: 2
- Severity: bug
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/csp_helpers.lua

**All Examples:**

```
`simSeconds()` is documented/used as returning seconds, but the fallback currently returns `sim.time` without converting from milliseconds. That reintroduces the timer drift/units bug when `sim.gameTi...
```

```
`simSeconds()` is still used throughout the app as a seconds-based clock (e.g., multiplying deltas by 1000 for ms and adding second-based hold constants), but this updated header comment says units ca...
```

---

### Cluster 40: values / code / invalid

- Count: 2
- Severity: nit
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/coaching_hints.lua, WARP.md

**All Examples:**

```
The EmmyLua annotation for `kind` isn’t valid syntax (`string|nil brake|throttle|...`). If you want a restricted set of values, use a quoted-literal union (e.g., '"brake"|"throttle"|...') or keep it a...
```

```
**issue (typo):** Consider rephrasing “invalid values clamp to **1** or **3**” for grammatical correctness.

The wording makes it sound like the values do the clamping. Consider “invalid values are cl...
```

---

### Cluster 41: code / details / summary

- Count: 2
- Severity: reliability
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/setup_reader.lua, src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🌐 Web query:

`In Assetto Corsa / CSP, how can a Lua app determine the currently loaded setup INI? Are active setups li...
```

```
_⚠️ Potential issue_ | _🔴 Critical_

<details>
<summary>🧩 Analysis chain</summary>

🌐 Web query:

`Does Assetto Corsa CSP render.DepthMode support ReadOnlyLessEqual enum value?`

💡 Result:

No, Assett...
```

---

### Cluster 44: code / node / class

- Count: 2
- Severity: maintainability
- Preventability: test
- Affected Files: tests/design_conformance_checklist.yaml

**All Examples:**

```
The `test:` node IDs here omit the test class names (e.g., `TestInformationArchitecture`). For pytest, class-based tests need fully-qualified node IDs like `tests/test_design_conformance.py::TestInfor...
```

```
The `test:` node IDs in this checklist don’t match the actual pytest node IDs for tests defined inside classes (e.g. `TestInformationArchitecture`). Pytest will report them as `tests/test_design_confo...
```

---

### Cluster 45: code / sections / this

- Count: 2
- Severity: maintainability
- Preventability: test
- Affected Files: tests/test_design_conformance.py

**All Examples:**

```
This test claims the manifest defines *exactly* WINDOW_0..WINDOW_2, but it only asserts those three sections exist and are non-empty. If additional windows (e.g., WINDOW_3) are added later, this would...
```

```
This test/docstring says the manifest defines *exactly* `WINDOW_0`, `WINDOW_1`, `WINDOW_2`, but the assertions only verify those three sections exist (it would still pass if `WINDOW_3`+ are present). ...
```

---

### Cluster 46: right / window / code

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, tests/test_design_conformance.py

**All Examples:**

```
The coaching window is documented as “transparent background, top-right”, but `script.windowCoaching()`/`coachingOverlay.draw()` don’t set any window background alpha/flags or position. If the intent ...
```

```
The docstring for this test says it verifies WINDOW_1 has the “correct default position (top-right)”, but the assertions only check that `DEFAULT_POSITION` is present (not its value). If the position ...
```

---

### Cluster 47: code / frame / medium

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

If a single frame in the buffer is malformed JSON, `pollInbound` currently stops processing all subsequent frames in that tick. ...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The check `if not sim.isInMainMenu then` is redundant here because `script.update` already returns early at line 1023 if `sim.is...
```

---

### Cluster 49: code / selection / consider

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/hud_settings.lua

**All Examples:**

```
Focus label map selection logic is duplicated: `focusLabelMap()` implements the manual-vs-auto selection, but the lap-boundary coaching filter reimplements the same branching to build `fmForFilter`. T...
```

```
Similarly, if `ui.combo` is not available (or errors), the Racing line source/style settings disappear entirely. Consider adding a read-only fallback (e.g., `ui.text`) so users can at least see the cu...
```

---

### Cluster 50: code / section / keys

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: tools/ai_sidecar/features.py, tests/test_manifest_phase5.py

**All Examples:**

```
> The `str(k)` call is redundant as keys from a JSON-loaded dictionary are already strings.

Addressed: Iterate with `isinstance(k, str)` guard and pass `k` straight into `_normalize_metric_key`.
```

```
This test only checks that the three strings appear somewhere in `manifest.ini`, which can produce false positives (e.g., if `[WINDOW_2]` exists but `NAME=Settings`/`FUNCTION_MAIN=windowSettings` are ...
```

---

### Cluster 51: code / parameter / unused

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: tools/ai_sidecar/features.py, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
`@agorokh`, thanks for cleaning that up — removing the unused `brakedistancem` / `brake_distance_m` aliases keeps `_METRIC_ALIASES` accurate and reduces noise until brake distance is actually promoted...
```

```
**nitpick:** The `sim0` parameter to `ensureCornerIniLoaded` is never used and can be removed.

Accepting an unused parameter is misleading and may encourage incorrect future use. Please either remove...
```

---

### Cluster 52: code / trace / this

- Count: 2
- Severity: reliability
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
`persistPayload()` intentionally drops `bestLapTrace` when `bestReferenceLapMs ~= bestLapMs`, but `bestReferenceLapMs` is not persisted. If a new PB time is accepted while the span guard rejects the n...
```

```
## Code Review

This pull request introduces a new module, `corner_names`, which enables the display of human-readable corner labels (e.g., "T1 Left" or track-specific names like "Variante") by parsin...
```

---

### Cluster 53: code / this / function

- Count: 2
- Severity: nit
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
If a CSP build lacks `render.circle`, `render.debugText`, and `render.rectangle`, this function will still sort/cull markers, raycast/snap, and enter a `pcall` per marker but render nothing. Consider ...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

This block of code duplicates logic already present in the `focusLabelMap` function. To improve maintainability and reduce redun...
```

---

### Cluster 56: code / review / this

- Count: 2
- Severity: nit
- Preventability: typecheck
- Affected Files: N/A

**All Examples:**

```
## Code Review

This pull request implements corner-level improvement ranking for the AI sidecar, enabling the system to provide specific coaching suggestions by comparing lap telemetry against the se...
```

```
## Code Review

This pull request introduces an optional Ollama-powered post-lap debrief feature for the AI sidecar, allowing users to receive LLM-generated feedback alongside rules-based coaching. Th...
```

---

### Cluster 58: code / metrics / ranking

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: tools/ai_sidecar/improvement_ranking.py

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The list of metrics used for ranking is hardcoded here, which duplicates logic from `features.py`. It would be better to use a s...
```

```
> The list of metrics used for ranking is hardcoded here, which duplicates logic from `features.py`.

Addressed: Ranking now iterates `sorted(CORNER_SPEED_METRICS)` imported from `features.py`.
```

---

### Cluster 61: code / this / document

- Count: 2
- Severity: nit
- Preventability: architecture
- Affected Files: docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, tools/ai_sidecar/features.py

**All Examples:**

```
The `last_updated` field was changed from `2026-03-31` to `2026-03-30`, which appears inconsistent with the current update to this document. If this metadata is meant to reflect the most recent edit d...
```

```
> `brake_distance_m` aliases are parsed but filtered by `CORNER_SPEED_METRICS` — document or remove.

Addressed: Removed the unused `brake_distance_m` / `brakedistancem` alias entries until brake dist...
```

---

### Cluster 62: code / thanks / test

- Count: 2
- Severity: maintainability
- Preventability: automation
- Affected Files: tests/test_ai_sidecar_improvement.py, tools/ai_sidecar/features.py

**All Examples:**

```
`@agorokh`, thanks for the quick fix — removing the redundant local import keeps the test file clean. ✅

<!-- <review_comment_addressed> -->

<!-- This is an auto-generated reply by CodeRabbit -->
```

```
`@agorokh`, thanks for confirming! The boolean guard and the accompanying test `test_extract_corner_table_skips_bool_corner_id` look like exactly the right fix. 

<!-- <review_comment_addressed> -->

...
```

---

### Cluster 63: websocket / security / insecure

- Count: 2
- Severity: security
- Preventability: guideline
- Affected Files: tools/ai_sidecar/server.py

**All Examples:**

```
**security (javascript.lang.security.detect-insecure-websocket):** Insecure WebSocket Detected. WebSocket Secure (wss) should be used for all WebSocket connections.

*Source: opengrep*

✅ Addressed in...
```

```
**security (javascript.lang.security.detect-insecure-websocket):** Insecure WebSocket Detected. WebSocket Secure (wss) should be used for all WebSocket connections.

*Source: opengrep*

✅ Addressed in...
```

---

### Cluster 65: code / color / every

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/track_markers.lua

**All Examples:**

```
Per-segment coloring currently allocates a new `rgbm` for every quad every frame (`speedColor(segSpeed)`), potentially up to `MAX_QUADS` per strip (and two strips in `both` mode). This can add GC chur...
```

```
drawWallQuad creates identical rgbm colors multiple times per wall (bottom color set twice, top color set twice). Since this runs every frame for up to MAX_MARKERS, it adds avoidable allocations/calls...
```

---

### Cluster 66: loop / code / render

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/track_markers.lua

**All Examples:**

```
`pcall` inside the per-segment render loop adds significant overhead (up to ~499 pcalls per frame when the line is in range). Prefer checking `render.line` once outside the loop and either call direct...
```

```
render.glBegin() is started before the marker loop, but if any error occurs inside the loop the surrounding pcall will abort before render.glEnd(), potentially leaving GL in an open-batch state and br...
```

---

### Cluster 67: diagnostic / this / flag

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
The new Draw3D diagnostic `ac.log` runs continuously (every ~2s) in all sessions. This can significantly spam CSP logs and add overhead in a per-frame render callback. Consider gating this behind a co...
```

```
speedDiagLogged is module-level, so the diagnostic will only ever log for the first drawLineStrip call (e.g., best OR last), even though the comment says “per strip”. If you want diagnostics for both ...
```

---

### Cluster 68: diagnostic / capped / iterations

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
Addressed: the periodic 5s diagnostic replaced the one-shot approach in a prior commit. Now also capped to 12 iterations (60s) to avoid logging indefinitely.
```

```
Fixed: diagnostic is now capped to 12 iterations (~60s of game time). After that, `state._coachDiagCount >= 12` prevents further logging.
```

---

### Cluster 70: segment / segments / code

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
The new multi-offset rendering draws 5× more debugLine segments and allocates new vec3 objects for every offset on every segment each frame. This can noticeably increase GC pressure / frame time on lo...
```

```
The tilt continuity state (`prevTiltH`) is carried across skipped segments (e.g., segments outside the cull radius). If rendering resumes at a non-adjacent segment (common on tracks with close/overlap...
```

---

### Cluster 71: code / comment / edge

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
Comment says “Vertical pillar (3 m tall)” but the code uses `sy + 3.5`. Either adjust the comment or the height constant so they match (helps avoid confusion when tuning marker visibility).
```suggest...
```

```
The comment says the “trailing edge (a, behind car) rises”, but the code applies the newly computed tilt to the `b` edge (`v3/v4` use `backTiltH` from `calcTiltHeight(sA, sB, len)`). Please either adj...
```

---

### Cluster 72: code / uses / different

- Count: 2
- Severity: nit
- Preventability: automation
- Affected Files: src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/persistence.lua

**All Examples:**

```
`markerCacheKey()` uses `%g`, which rounds to ~6 significant digits and can cause different marker coordinates to collide into the same cache key (reusing a snapped Y from a different location). Using...
```

```
`M.sessionKey(car, sim)` no longer uses `car` or `sim`. To avoid confusion and make intent clear, consider renaming the parameters to `_car, _sim` (or removing them if the signature isn’t part of a pu...
```

---

### Cluster 73: trace / code / first

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
Lap trace clock never starts until after the first lap boundary (only `tel:beginLapClock()` is inside the `lc > state.lastLapCount` block). This means lap #1 always finalizes an empty trace, and delta...
```

```
The PB trace acceptance guard uses only `spanMs >= lastMs * 0.45`. This can still accept a partial trace if lap recording started mid-lap (e.g., user enables the app during an ongoing lap), producing ...
```

---

## Hook suggestions (not applied)

The following are **suggested** `settings.json` hook snippets for `preventability: automation` patterns.
Review and merge manually if appropriate.

### details / summary / code
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: details / summary / code (8 occurrences)
```

### code / summary / details
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / summary / details (5 occurrences)
```

### code / label / empty
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / label / empty (2 occurrences)
```

### code / thanks / test
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / thanks / test (2 occurrences)
```

### code / uses / different
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / uses / different (2 occurrences)
```
