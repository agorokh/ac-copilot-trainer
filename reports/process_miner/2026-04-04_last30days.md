# Process Improvement Miner Report

**Repository:** agorokh/ac-copilot-trainer
**Analysis period:** last 30 day(s) (since 2026-04-04 UTC, through 2026-05-04 UTC)
**Generated:** 2026-05-04T09:57:16.524649+00:00

---

## Executive Summary

- **PRs Analyzed:** 30
- **Total Comments:** 1586
- **Average Comments per PR:** 52.9
- **Total Files Changed:** 476
- **Total Additions:** 41,588
- **Total Deletions:** 4,854
- **CI Failures:** 8
- **Comment Clusters Found:** 79

---

## Bot vs human review comments

Counts are over review and issue thread comments for this run (excludes the PR body).

- **bot:** 1436
- **human:** 150

---

## Per-bot comment severity (heuristic)

- **bugbot:** bug=51, maintainability=30, nit=135, perf=1, reliability=10
- **chatgpt_codex_connector_bot:** bug=41, maintainability=17, nit=199, perf=1, reliability=8
- **coderabbit:** bug=118, maintainability=172, nit=35, perf=3, reliability=47, security=5
- **copilot:** bug=32, maintainability=50, nit=189, perf=6, reliability=9, security=4
- **gemini:** bug=11, maintainability=13, nit=58, perf=10, reliability=2, security=4
- **github_actions:** nit=2
- **qodo_code_review_bot:** bug=15, maintainability=8, nit=8, perf=1, reliability=3, security=4
- **sourcery:** bug=25, maintainability=42, nit=50, perf=4, reliability=4, security=9

---

## Multi-bot agreement (PRs with ≥2 distinct bots)

- **PRs in window with ≥2 bots:** 29

Per-PR summaries (pair co-occurrence counts use agreement locations: file+line when present, else path+comment id, else comment id):

- **PR #95** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #94** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 2
  top pairs: bugbot|chatgpt_codex_connector_bot=1, bugbot|gemini=1, chatgpt_codex_connector_bot|gemini=1, copilot|gemini=1

- **PR #91** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 2
  top pairs: bugbot|coderabbit=1, bugbot|gemini=1

- **PR #92** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #90** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #89** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #88** — bots: chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: copilot|gemini=1

- **PR #87** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 2
  top pairs: bugbot|copilot=2, bugbot|chatgpt_codex_connector_bot=1, chatgpt_codex_connector_bot|copilot=1

- **PR #85** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: chatgpt_codex_connector_bot|copilot=1

- **PR #84** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #83** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, github_actions, qodo_code_review_bot, sourcery; locations with multiple bots: 4
  top pairs: coderabbit|copilot=2, bugbot|coderabbit=1, chatgpt_codex_connector_bot|coderabbit=1

- **PR #80** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, github_actions, qodo_code_review_bot, sourcery; locations with multiple bots: 4
  top pairs: bugbot|coderabbit=2, chatgpt_codex_connector_bot|coderabbit=1, chatgpt_codex_connector_bot|gemini=1

- **PR #78** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 3
  top pairs: bugbot|chatgpt_codex_connector_bot=1, bugbot|copilot=1, chatgpt_codex_connector_bot|copilot=1, coderabbit|copilot=1, coderabbit|gemini=1

- **PR #75** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: coderabbit|copilot=1

- **PR #74** — bots: coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: copilot|gemini=2

- **PR #73** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: bugbot|coderabbit=1, chatgpt_codex_connector_bot|coderabbit=1

- **PR #70** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: coderabbit|copilot=2, bugbot|coderabbit=1, bugbot|copilot=1

- **PR #68** — bots: coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

- **PR #67** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

- **PR #65** — bots: coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

- **PR #64** — bots: bugbot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: bugbot|coderabbit=1, bugbot|copilot=1, coderabbit|copilot=1, coderabbit|gemini=1

- **PR #63** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: bugbot|copilot=1, coderabbit|copilot=1

- **PR #62** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

- **PR #61** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #60** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

---

## Analyzed PRs

- **PR #95:** [fix(vault): strip Obsidian + stray draft after PR94 merge](https://github.com/agorokh/ac-copilot-trainer/pull/95)
  - Author: agorokh
  - Merged: 2026-04-29
  - Comments: 7
  - Files: 5
  - CI: failure

- **PR #94:** [docs(vault): post-merge handoff for PR #91](https://github.com/agorokh/ac-copilot-trainer/pull/94)
  - Author: agorokh
  - Merged: 2026-04-29
  - Comments: 16
  - Files: 7
  - CI: failure

- **PR #91:** [feat(screen): Phase-2 LVGL launcher + AC Copilot + Pocket Technician (issue #86 Parts A-D)](https://github.com/agorokh/ac-copilot-trainer/pull/91)
  - Author: agorokh
  - Merged: 2026-04-29
  - Comments: 290
  - Files: 45
  - CI: failure

- **PR #92:** [docs(vault): session SAVE — PR #91 Phase-2 Part A+B in flight](https://github.com/agorokh/ac-copilot-trainer/pull/92)
  - Author: agorokh
  - Merged: 2026-04-25
  - Comments: 9
  - Files: 5
  - CI: failure

- **PR #90:** [docs(vault): post-merge handoff for PR #89](https://github.com/agorokh/ac-copilot-trainer/pull/90)
  - Author: agorokh
  - Merged: 2026-04-25
  - Comments: 6
  - Files: 1
  - CI: failure

- **PR #89:** [fix(gitattributes): pin *.sh and *.bash to eol=lf](https://github.com/agorokh/ac-copilot-trainer/pull/89)
  - Author: agorokh
  - Merged: 2026-04-25
  - Comments: 10
  - Files: 1
  - CI: failure

- **PR #88:** [docs(vault): post-merge handoff for PR #87](https://github.com/agorokh/ac-copilot-trainer/pull/88)
  - Author: agorokh
  - Merged: 2026-04-24
  - Comments: 11
  - Files: 3
  - CI: failure

- **PR #87:** [chore: sync template to template-repo@061d9ab (template-2026.04)](https://github.com/agorokh/ac-copilot-trainer/pull/87)
  - Author: agorokh
  - Merged: 2026-04-24
  - Comments: 68
  - Files: 200
  - CI: failure

- **PR #85:** [docs(vault): enrich with PR #70-#84 context + EPIC #59 + debugging journey + Figma design](https://github.com/agorokh/ac-copilot-trainer/pull/85)
  - Author: agorokh
  - Merged: 2026-04-23
  - Comments: 28
  - Files: 15
  - CI: success

- **PR #84:** [docs(vault): post-merge handoff for PR #83](https://github.com/agorokh/ac-copilot-trainer/pull/84)
  - Author: agorokh
  - Merged: 2026-04-22
  - Comments: 9
  - Files: 2
  - CI: success

- **PR #83:** [feat(sidecar,bridge): external WS client support (issue #81)](https://github.com/agorokh/ac-copilot-trainer/pull/83)
  - Author: agorokh
  - Merged: 2026-04-22
  - Comments: 178
  - Files: 47
  - CI: success

- **PR #80:** [feat(post-merge): roll out deterministic steward contract](https://github.com/agorokh/ac-copilot-trainer/pull/80)
  - Author: agorokh
  - Merged: 2026-04-22
  - Comments: 178
  - Files: 12
  - CI: success

- **PR #78:** [feat(#77): sidecar auto-launch + per-lap archive (with car setup capture)](https://github.com/agorokh/ac-copilot-trainer/pull/78)
  - Author: agorokh
  - Merged: 2026-04-21
  - Comments: 127
  - Files: 11
  - CI: success

- **PR #76:** [chore(deps-dev): bump the python group with 14 updates](https://github.com/agorokh/ac-copilot-trainer/pull/76)
  - Author: dependabot[bot]
  - Merged: 2026-04-19
  - Comments: 0
  - Files: 1
  - CI: success

- **PR #75:** [fix(#75): in-game smoke test — CSP runtime fixes + Ollama coaching pipeline](https://github.com/agorokh/ac-copilot-trainer/pull/75)
  - Author: agorokh
  - Merged: 2026-04-14
  - Comments: 120
  - Files: 19
  - CI: success

- **PR #74:** [docs(vault): SAVE — PR #73 merged, refresh handoff + current focus](https://github.com/agorokh/ac-copilot-trainer/pull/74)
  - Author: agorokh
  - Merged: 2026-04-12
  - Comments: 9
  - Files: 2
  - CI: success

- **PR #73:** [fix(#72): Phase 5 HUD rebuild — live-frame engine + bundled fonts + FIXED_SIZE windows + always-visible tiles](https://github.com/agorokh/ac-copilot-trainer/pull/73)
  - Author: agorokh
  - Merged: 2026-04-07
  - Comments: 67
  - Files: 16
  - CI: success

- **PR #70:** [fix(#69): visual design rewrite matching Figma spec](https://github.com/agorokh/ac-copilot-trainer/pull/70)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 48
  - Files: 6
  - CI: success

- **PR #68:** [docs: SAVE vault after PR #67 merge](https://github.com/agorokh/ac-copilot-trainer/pull/68)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 6
  - Files: 2
  - CI: success

- **PR #67:** [fix(#66): CSP textColored signature reversal + checkbox semantics + em dash (P0 hotfix)](https://github.com/agorokh/ac-copilot-trainer/pull/67)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 39
  - Files: 11
  - CI: success

- **PR #65:** [docs: SAVE vault for issue #57 closure](https://github.com/agorokh/ac-copilot-trainer/pull/65)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 7
  - Files: 2
  - CI: success

- **PR #64:** [Phase 5 Part E: Active suggestion panel (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/64)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 51
  - Files: 5
  - CI: success

- **PR #63:** [Phase 5 Part D: Real-time per-corner coaching engine (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/63)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 59
  - Files: 7
  - CI: success

- **PR #62:** [Phase 5 Part C: Polished approach telemetry panel (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/62)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 37
  - Files: 6
  - CI: success

- **PR #61:** [Add design conformance test suite (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/61)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 29
  - Files: 2
  - CI: success

- **PR #60:** [Phase 5 Part B: Settings window (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/60)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 26
  - Files: 6
  - CI: success

- **PR #58:** [Phase 5 Part A: ac.storage, corner names, structured approach data (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/58)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 23
  - Files: 4
  - CI: success

- **PR #56:** [Add focus practice mode for corner targets and marker emphasis (#44)](https://github.com/agorokh/ac-copilot-trainer/pull/56)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 32
  - Files: 7
  - CI: success

- **PR #55:** [Add Ollama post-lap debrief and HUD fallback (#46)](https://github.com/agorokh/ac-copilot-trainer/pull/55)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 32
  - Files: 14
  - CI: success

- **PR #54:** [Phase 3c: sidecar lap features + improvement ranking (#49)](https://github.com/agorokh/ac-copilot-trainer/pull/54)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 64
  - Files: 12
  - CI: success

---

## Top Recurring Comment Clusters

These clusters represent patterns of repeated feedback across PRs.

### 1. code / code_block / ensure

- **Count:** 61 comments
- **Severity:** bug
- **Preventability:** test
- **Dominant author type:** bot
- **Dominant bot:** coderabbit
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/modules/hud.lua

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟡 Minor_

**Handle fixture read/parse failures with a clean CLI error path.**

`_run_compare_laps` currently crashes with a traceback on missing files or invalid JSON. For an o...

2. _🧹 Nitpick_ | _🔵 Trivial_

**Remove redundant local import.**

`extract_corner_table` is already imported at line 10. The local import at line 70 is redundant.


<details>
<summary>♻️ Proposed fix</su...

3. _⚠️ Potential issue_ | _🟡 Minor_

**Run `ruff format` to unblock CI.**

`ruff format --check` is currently failing on this file; formatting must be applied before merge.



<details>
<summary>💡 Exampl...

---

### 2. code / code_block / agorokh

- **Count:** 30 comments
- **Severity:** maintainability
- **Preventability:** automation
- **Dominant author type:** bot
- **Dominant bot:** coderabbit
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, scripts/_build_pr80_ledger.py, firmware/screen/src/main.cpp

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify current coercion path and absence/presence of finite-value guards/tes...

2. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify lapTimeMs coercion behavior is currently unguarded and check test cov...

3. _⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify whether boolean corner IDs are explicitly guarded before int conversi...

---

### 3. code / commit / unstaged

- **Count:** 19 comments
- **Severity:** maintainability
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** chatgpt_codex_connector_bot
- **Affected Files:** scripts/check_vault_follow_up.sh

**Representative Examples:**

1. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Detect commit -a changes in vault follow-up guard**

This guard exits when `git diff --cached` is empty, but the...

2. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Detect pathspec/include commits in vault follow-up guard**

`FILES_TO_CHECK` only includes unstaged tracked file...

3. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Parse long commit options before scanning short flags**

In `commit_may_include_unstaged_tracked`, any token sta...

---

### 4. program / files / review

- **Count:** 12 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** human

**Representative Examples:**

1. C:/Program Files/Git/gemini review

2. C:/Program Files/Git/review

3. C:/Program Files/Git/gemini review

---

### 5. code / only / code_block

- **Count:** 11 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** copilot
- **Affected Files:** tools/ai_sidecar/features.py, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. > `_as_float` should reject booleans like `_positive_lap_time_ms` so JSON `true` is not treated as `1.0`.

Addressed: Added `isinstance(v, bool)` guard in `_as_float` and `test_as_float_rejects_bool`....

2. `findNextBrakeOrCorner()` forces `d` to 1 when `entry.s0 == sp` (`if d < 1e-9 then d = 1 end`). That makes the “next” segment appear a full lap away exactly at a brake/corner boundary, which can preve...

3. `script.update` calls `wsBridge.tick()` and then `wsBridge.startSidecarIfNeeded()` every frame. Since `wsBridge.tick()` already attempts `tryOpen()` on its 5s cadence when `sock` is nil, `startSidecar...

---

### 6. code / font / stack

- **Count:** 7 comments
- **Severity:** bug
- **Preventability:** guideline
- **Dominant author type:** bot
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, tests/test_design_conformance.py

**Representative Examples:**

1. ![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

This function contains a critical logic error and a redundant calculation:

1. **Font Stack Corruption**: `fontMod.pop()` is called ...

2. Same font-stack issue here: `pushNamed()` result isn’t captured, and `fontMod.pop()` is called without the required kind token, so the font won’t be popped.
```suggestion
  local titleFont = fontMod.p...

3. Same font-stack issue here: `pushNamed()` must be paired with `pop(kind)` using the returned token; `pop()` without args won’t undo the font push.
```suggestion
  local titleFont = fontMod.pushNamed("...

---

### 7. code / that / test

- **Count:** 7 comments
- **Severity:** maintainability
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/hud.lua, tests/test_design_conformance.py

**Representative Examples:**

1. ### Exported `M.activeHint()` function is never called

**Low Severity**

<!-- DESCRIPTION START -->
`M.activeHint()` is exported but never called anywhere in the codebase. The active hint is already ...

2. ### Exported `M.phase()` function is never called

**Low Severity**

<!-- DESCRIPTION START -->
`M.phase()` is exported from `realtime_coaching.lua` but never called anywhere in the runtime codebase. ...

3. ### Dead code kept solely for test string matching

**Low Severity**

<!-- DESCRIPTION START -->
The `_pe07Touch` variable reads `vm.debriefText` into a local that is never used, followed by a no-op `...

---

### 8. audit / security / subprocess

- **Count:** 6 comments
- **Severity:** security
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** sourcery
- **Affected Files:** scripts/_build_pr80_ledger.py, firmware/screen/tools/long_cmd_fix_post.py

**Representative Examples:**

1. **security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'run' without a static string. If this data can be controlled by a malicious actor, it may be an ...

2. **security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'run' without a static string. If this data can be controlled by a malicious actor, it may be an ...

3. **security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'check_output' without a static string. If this data can be controlled by a malicious actor, it m...

---

### 9. code / corner / label

- **Count:** 6 comments
- **Severity:** bug
- **Preventability:** architecture
- **Dominant author type:** bot
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. ![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

There is a logic error in the corner label priority. Currently, if `nextBrake` is found (which is almost always true as it wraps aro...

2. _⚠️ Potential issue_ | _🟠 Major_

**In-corner coaching should override the next-corner label.**

`cornerLabel` is resolved from the next brake point first, and Line 235 only fills the current segment ...

3. _⚠️ Potential issue_ | _🟠 Major_

**Keep the queried corner label aligned with the telemetry.**

When `topLabel` comes from `inCornerLabel`, this block still sends `targetSpeed`/`distToBrakeM` from `n...

---

### 10. code / frontmatter / yaml

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Affected Files:** docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md

**Representative Examples:**

1. ![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The YAML frontmatter is structurally invalid. It should not contain Markdown headers (`##`) or leading empty lines within the bl...

2. ![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The closing `---` for the YAML frontmatter block is missing. This is required for the metadata section to be recognized and pars...

3. The YAML frontmatter in `Current Focus.md` is missing its closing `---` delimiter before the Markdown body (`# Current focus`). As written, the entire document is treated as frontmatter, which can bre...

---

### 11. code / reset / stale

- **Count:** 6 comments
- **Severity:** maintainability
- **Preventability:** architecture
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. ### Rolling reset skips clearing `_cachedRealtimeView` state

**Medium Severity**

<!-- DESCRIPTION START -->
Both `resetDrivingState` and `resetRollingDrivingState` clear `state.realtimeActiveHint = ...

2. ### Stale recv queue survives reset, repopulates cleared state

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` clears `cornerAdvisories` and resets `currentSimT` to 0, but does not clear ...

3. ### Reset clears launch flag, preventing sidecar relaunch after crash

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` sets `sidecarChildEverLaunched = false` while leaving `spawnedAlive` ...

---

### 12. code / this / severity

- **Count:** 6 comments
- **Severity:** maintainability
- **Preventability:** test
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, tools/ai_sidecar/features.py, src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. ### Missing boolean guard in `_as_float` unlike `_positive_lap_time_ms`

**Low Severity**

<!-- DESCRIPTION START -->
`_as_float` accepts Python booleans (`True` → `1.0`, `False` → `0.0`) because `boo...

2. ### Unused `COLOR_LABEL` variable is dead code

**Low Severity**

<!-- DESCRIPTION START -->
`COLOR_LABEL` is declared on line 15 but never referenced anywhere in `hud.lua`. All other `COLOR_*` consta...

3. ### Several HUD fields computed every frame but never rendered

**Low Severity**

<!-- DESCRIPTION START -->
The diff removes all rendering of `vm.sectorMessage`, `vm.coastWarn`, `vm.tireLockupFlash`,...

---

### 13. code / severity / which

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** tools/ai_sidecar/session.py, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**Representative Examples:**

1. ### `_positive_lap_time_ms` misses `OverflowError` from infinity

**Medium Severity**

<!-- DESCRIPTION START -->
`_positive_lap_time_ms` catches `(TypeError, ValueError)` but `int(float('inf'))` rais...

2. ### Fallback clock `monoClock` is never incremented

**Low Severity**

<!-- DESCRIPTION START -->
New debounce code uses `opts.simT or monoClock` as the time reference, but `monoClock` is initialized ...

3. ### `drawSidecarDebrief` is now dead code with no callers

**Low Severity**

<!-- DESCRIPTION START -->
The `drawSidecarDebrief` function in `coaching_overlay.lua` is now completely unused. Its only c...

---

### 14. code / this / template

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** human
- **Affected Files:** .github/workflows/vault-automerge.yml, .github/workflows/post-merge-notify.yml, scripts/hook_protect_main_impl.py

**Representative Examples:**

1. Valid observation, but deferring to an upstream `template-repo` issue rather than changing it in this sync PR. The workflow comes in verbatim from `template-repo@061d9ab` and reverting the unlabeled-t...

2. The `ref: merge_commit_sha` removal is from the upstream template and the classifier script (`scripts/post_merge_classify.py`) reads PR diff via `gh api` rather than the local checkout, so it does not...

3. Valid robustness suggestion, but the file is unchanged from upstream `template-repo@061d9ab` in this sync PR. Deferring to an upstream issue so the fix lands once and propagates to every child repo ra...

---

### 15. sorry / unable / review

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** sourcery

**Representative Examples:**

1. Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines

2. Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines

3. Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines

---

### 16. ac_copilot_trainer / with / modules

- **Count:** 5 comments
- **Severity:** reliability
- **Preventability:** typecheck
- **Dominant author type:** bot
- **Dominant bot:** qodo_code_review_bot

**Representative Examples:**

1. <h3>Review Summary by Qodo</h3>

Add Ollama post-lap debrief with HUD display and rules fallback (issue #46)

<code>✨ Enhancement</code> <code>🧪 Tests</code>

<img src="https://www.qodo.ai/wp-content/...

2. <h3>Code Review by Qodo</h3>

<code>🐞 Bugs (1)</code>  <code>📘 Rule violations (3)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...

3. <h3>Code Review by Qodo</h3>

<code>🐞 Bugs (1)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...

---

### 17. code / brake / corner

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** copilot
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. `cornerLabelForBrake()` uses `math.abs(s.brakeSpline - brakeSeg.s0)` to match a brake segment to its corner. This breaks for wrap-around near spline 0/1 (e.g., brakeSpline=0.001, s0=0.989 → abs≈0.988)...

2. ![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The spline distance calculation does not account for the track wrap-around at the start/finish line. If a brake segment starts a...

3. ### Wrap-around spline distance breaks label lookup near start/finish

**Medium Severity**

<!-- DESCRIPTION START -->
`cornerLabelForBrake` computes spline distance as `math.abs(s.brakeSpline - brake...

---

### 18. code / code_block / remove

- **Count:** 5 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** coderabbit
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**Representative Examples:**

1. _🧹 Nitpick_ | _🔵 Trivial_

**Remove unused color constants.**

`COLOR_RED_LIGHT` and `PANEL_PAD_X` are defined but never used, as flagged by static analysis. Remove them to reduce noise.

<details>
<s...

2. _🧹 Nitpick_ | _🔵 Trivial_

**Remove unused `carSp` parameter from `placeholderView`.**

The static analysis correctly flags that `carSp` is never used in this function. Since the placeholder view does...

3. _🧹 Nitpick_ | _🔵 Trivial_

**Remove unused dedupe variables left over from the removed time-based suppression.**

Static analysis correctly flags `DEDUP_HOLD_SEC` (line 30) as unused and `lastEmittedA...

---

### 19. ranking / code / computes

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** qodo_code_review_bot
- **Affected Files:** tools/ai_sidecar/session.py

**Representative Examples:**

1. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Ranking on new pb <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
LapComparis...

2. LapComparisonState computes and returns an improvement ranking *before* updating the PB when the current lap is faster than the stored best. That means a new PB lap can still return regressions vs the...

3. > Ranking on new pb — computes improvement ranking against the previous PB before updating PB, so a new fastest lap could still get improvementRanking.

Addressed: `improvement_ranking_for` now treats...

---

### 20. code / json / guard

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** tools/ai_sidecar/protocol.py, tools/ai_sidecar/external_protocol.py, tools/ai_sidecar/coaching/llm_coach.py

**Representative Examples:**

1. ### Unguarded `.get()` on possibly non-dict JSON response crashes connection

**Medium Severity**

<!-- DESCRIPTION START -->
`json.loads(raw)` can return any JSON type (list, string, number, `None`, ...

2. ### Missing bool guard on float() for JSON values

**Medium Severity**

<!-- DESCRIPTION START -->
The `corner_query` handler coerces untrusted JSON values `cur`, `ref`, and `dist` via `float(inbound....

3. ### Protocol field lacks bool guard on int coercion

**Low Severity**

<!-- DESCRIPTION START -->
The `int(proto_raw)` coercion at the protocol validation entry point has no `isinstance(proto_raw, boo...

---

## Recurring CI Failures

- **PR #95:** [fix(vault): strip Obsidian + stray draft after PR94 merge](https://github.com/agorokh/ac-copilot-trainer/pull/95)
  - Failed jobs: build

- **PR #94:** [docs(vault): post-merge handoff for PR #91](https://github.com/agorokh/ac-copilot-trainer/pull/94)
  - Failed jobs: build

- **PR #91:** [feat(screen): Phase-2 LVGL launcher + AC Copilot + Pocket Technician (issue #86 Parts A-D)](https://github.com/agorokh/ac-copilot-trainer/pull/91)
  - Failed jobs: score

- **PR #92:** [docs(vault): session SAVE — PR #91 Phase-2 Part A+B in flight](https://github.com/agorokh/ac-copilot-trainer/pull/92)
  - Failed jobs: build, guard-and-automerge, guard-and-automerge

- **PR #90:** [docs(vault): post-merge handoff for PR #89](https://github.com/agorokh/ac-copilot-trainer/pull/90)
  - Failed jobs: guard-and-automerge, build

- **PR #89:** [fix(gitattributes): pin *.sh and *.bash to eol=lf](https://github.com/agorokh/ac-copilot-trainer/pull/89)
  - Failed jobs: score

- **PR #88:** [docs(vault): post-merge handoff for PR #87](https://github.com/agorokh/ac-copilot-trainer/pull/88)
  - Failed jobs: build, guard-and-automerge

- **PR #87:** [chore: sync template to template-repo@061d9ab (template-2026.04)](https://github.com/agorokh/ac-copilot-trainer/pull/87)
  - Failed jobs: score, classify

---

## Most Churned Files

Files with the most review comments:

- `src/ac_copilot_trainer/ac_copilot_trainer.lua`: 97 comments
- `src/ac_copilot_trainer/modules/hud.lua`: 70 comments
- `src/ac_copilot_trainer/modules/realtime_coaching.lua`: 57 comments
- `src/ac_copilot_trainer/modules/ws_bridge.lua`: 56 comments
- `firmware/screen/src/main.cpp`: 50 comments
- `tools/ai_sidecar/server.py`: 47 comments
- `scripts/post_merge_sync.sh`: 37 comments
- `scripts/check_vault_follow_up.sh`: 35 comments
- `tests/test_design_conformance.py`: 33 comments
- `src/ac_copilot_trainer/modules/lap_archive.lua`: 27 comments

---

## Suggested Next Actions

### Proposed Cursor Rule Updates

**Automation Gates (lint/format/pre-commit):**

- Add check for: code / code_block / agorokh
- Add check for: obsidian / docs / 01_vault
- Add check for: failed / format / error

**Type Checking:**

- Add type check for: ac_copilot_trainer / with / modules
- Add type check for: code / json / guard
- Add type check for: code / warning / with
- Add type check for: code / branch / line
- Add type check for: code / call / module

**Repository Guidelines:**

- Document pattern: code / commit / unstaged
- Document pattern: program / files / review
- Document pattern: code / only / code_block
- Document pattern: code / font / stack
- Document pattern: code / that / test

**Architecture/Agent Rules:**

- Add agent rule: code / corner / label
- Add agent rule: code / reset / stale
- Add agent rule: code / archive / severity
- Add agent rule: code / socket / close
- Add agent rule: corner / real / time

### Proposed CI/Pre-commit Changes

- Add automated checks for common formatting/import issues
- Add type checking to CI pipeline
- Add pre-commit hooks for common issues

### Proposed Documentation Updates

- Update AGENTS.md with common patterns
- Add examples to documentation
- Create troubleshooting guide

---

## Appendix: All Comment Clusters

### Cluster 0: code / code_block / ensure

- Count: 61
- Severity: bug
- Preventability: test
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/modules/hud.lua, scripts/post_merge_sync.sh, tools/ai_sidecar/server.py

**All Examples:**

```
_⚠️ Potential issue_ | _🟡 Minor_

**Handle fixture read/parse failures with a clean CLI error path.**

`_run_compare_laps` currently crashes with a traceback on missing files or invalid JSON. For an o...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove redundant local import.**

`extract_corner_table` is already imported at line 10. The local import at line 70 is redundant.


<details>
<summary>♻️ Proposed fix</su...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Run `ruff format` to unblock CI.**

`ruff format --check` is currently failing on this file; formatting must be applied before merge.



<details>
<summary>💡 Exampl...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Don't derive `apexSpeedKmh` from the entry/exit average.**

`src/ac_copilot_trainer/modules/corner_analysis.lua` only gives you `entrySpeed`, `minSpeed`, and `exitS...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Decouple wrapping fallback from style-color support.**

At Line 355, `ui.textWrapped` is only used when style color APIs are also present. If style APIs are missing, long ...
```

```
_⚠️ Potential issue_ | _🔴 Critical_

**Guard non-object JSON before calling `.get()`.**

Line 267 assumes every parseable payload is a mapping. A valid JSON array/string/null from a bad proxy or non-O...
```

---

### Cluster 1: code / code_block / agorokh

- Count: 30
- Severity: maintainability
- Preventability: automation
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, scripts/_build_pr80_ledger.py, firmware/screen/src/main.cpp, firmware/screen/include/lv_conf.h, tools/ai_sidecar/features.py

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify current coercion path and absence/presence of finite-value guards/tes...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify lapTimeMs coercion behavior is currently unguarded and check test cov...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify whether boolean corner IDs are explicitly guarded before int conversi...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
set -e
rg -n -C2 'writeSessionEnd|sidecarDebriefText|sidecar_debrief_last' src...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cd /tmp && find . -name "hud.lua" -type f 2>/dev/null | head -5
```

Repository: agorokh/a...
```

```
_⚠️ Potential issue_ | _🔴 Critical_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
set -euo pipefail

overlay="$(fd -p 'coaching_overlay.lua' | head -n1)"

pr...
```

---

### Cluster 2: code / commit / unstaged

- Count: 19
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: scripts/check_vault_follow_up.sh

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Detect commit -a changes in vault follow-up guard**

This guard exits when `git diff --cached` is empty, but the...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Detect pathspec/include commits in vault follow-up guard**

`FILES_TO_CHECK` only includes unstaged tracked file...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Parse long commit options before scanning short flags**

In `commit_may_include_unstaged_tracked`, any token sta...
```

```
### Long options parsed as short flags cause false blocks

**Medium Severity**

<!-- DESCRIPTION START -->
The embedded Python parser in `commit_may_include_unstaged_tracked` does not distinguish `--l...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Ignore unstaged vault docs when satisfying follow-up requirement**

`ACKED` is derived from `FILES_TO_CHECK`, wh...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Recognize interactive commit modes in vault guard**

`commit_may_include_unstaged_tracked` misses interactive co...
```

---

### Cluster 3: program / files / review

- Count: 12
- Severity: nit
- Preventability: guideline
- Dominant author type: human
- Affected Files: N/A

**All Examples:**

```
C:/Program Files/Git/gemini review
```

```
C:/Program Files/Git/review
```

```
C:/Program Files/Git/gemini review
```

```
C:/Program Files/Git/review
```

```
C:/Program Files/Git/gemini review
```

```
C:/Program Files/Git/review
```

---

### Cluster 4: code / only / code_block

- Count: 11
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: tools/ai_sidecar/features.py, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/persistence.lua, tools/ai_sidecar/server.py

**All Examples:**

```
> `_as_float` should reject booleans like `_positive_lap_time_ms` so JSON `true` is not treated as `1.0`.

Addressed: Added `isinstance(v, bool)` guard in `_as_float` and `test_as_float_rejects_bool`....
```

```
`findNextBrakeOrCorner()` forces `d` to 1 when `entry.s0 == sp` (`if d < 1e-9 then d = 1 end`). That makes the “next” segment appear a full lap away exactly at a brake/corner boundary, which can preve...
```

```
`script.update` calls `wsBridge.tick()` and then `wsBridge.startSidecarIfNeeded()` every frame. Since `wsBridge.tick()` already attempts `tryOpen()` on its 5s cadence when `sock` is nil, `startSidecar...
```

```
The EmmyLua annotations for `archiveCarIdFromCar` / `archiveTrackIdFromSim` declare `car`/`sim` as `table|nil`, but the functions are designed to accept CSP `ac.StateCar` / `ac.StateSim` userdata (the...
```

```
`make_token_check` imports `Headers` and `Response` from `websockets.datastructures` / `websockets.http11`, which are internal modules and may change across `websockets>=16`. Prefer the public return ...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Classifier misses several common dependency ecosystems.**

The `deps` branch only covers Python (`pyproject.toml`, `poetry.lock`, `pdm.lock`, `requirements.txt`, `environm...
```

---

### Cluster 5: code / font / stack

- Count: 7
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, tests/test_design_conformance.py

**All Examples:**

```
![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

This function contains a critical logic error and a redundant calculation:

1. **Font Stack Corruption**: `fontMod.pop()` is called ...
```

```
Same font-stack issue here: `pushNamed()` result isn’t captured, and `fontMod.pop()` is called without the required kind token, so the font won’t be popped.
```suggestion
  local titleFont = fontMod.p...
```

```
Same font-stack issue here: `pushNamed()` must be paired with `pop(kind)` using the returned token; `pop()` without args won’t undo the font push.
```suggestion
  local titleFont = fontMod.pushNamed("...
```

```
### Font stack leak: `pop()` called without required kind argument

**High Severity**

<!-- DESCRIPTION START -->
All four `fontMod.pushNamed()` calls in `drawActiveSuggestion` discard the return valu...
```

```
Same font-stack issue here: capture the return value of `pushNamed("brand", ...)` and pass it into `fontMod.pop(kind)`; otherwise the font stack will grow each draw.
```suggestion
    local brandFontK...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Font stack leak: `pushNamed` return value discarded.**

`fontMod.pushNamed` returns a kind token (`"dwrite"`, `"builtin"`, or `nil`) that must be passed to `fontMod...
```

---

### Cluster 6: code / that / test

- Count: 7
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/hud.lua, tests/test_design_conformance.py, tests/test_phase5_rebuild_ete.py

**All Examples:**

```
### Exported `M.activeHint()` function is never called

**Low Severity**

<!-- DESCRIPTION START -->
`M.activeHint()` is exported but never called anywhere in the codebase. The active hint is already ...
```

```
### Exported `M.phase()` function is never called

**Low Severity**

<!-- DESCRIPTION START -->
`M.phase()` is exported from `realtime_coaching.lua` but never called anywhere in the runtime codebase. ...
```

```
### Dead code kept solely for test string matching

**Low Severity**

<!-- DESCRIPTION START -->
The `_pe07Touch` variable reads `vm.debriefText` into a local that is never used, followed by a no-op `...
```

```
### Test PC-07 asserts dead `approachHudData` function presence

**Low Severity**

<!-- DESCRIPTION START -->
Test `PC-07` asserts that `"approachHudData"` appears in the entry script. The function de...
```

```
**suggestion (testing):** There’s no test for the HUD’s behavior when `realtimeView` is nil, even though always-visible tiles are part of the contract.

ETE-01 currently always passes a non-nil `realt...
```

```
**suggestion (testing):** autoPlaceOnce is only validated via grepping source; behavior (one-time move and persisted flag) is not exercised via Lua.

Given the existing `ac.storage`, `ac.getAppWindows...
```

---

### Cluster 7: audit / security / subprocess

- Count: 6
- Severity: security
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: scripts/_build_pr80_ledger.py, firmware/screen/tools/long_cmd_fix_post.py

**All Examples:**

```
**security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'run' without a static string. If this data can be controlled by a malicious actor, it may be an ...
```

```
**security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'run' without a static string. If this data can be controlled by a malicious actor, it may be an ...
```

```
**security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'check_output' without a static string. If this data can be controlled by a malicious actor, it m...
```

```
**security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'call' without a static string. If this data can be controlled by a malicious actor, it may be an...
```

```
**security (python.lang.security.audit.dangerous-subprocess-use-tainted-env-args):** Detected subprocess function 'call' with user controlled data. A malicious actor could leverage this to perform com...
```

```
**security (python.lang.security.audit.dangerous-subprocess-use-tainted-env-args):** Detected subprocess function 'run' with user controlled data. A malicious actor could leverage this to perform comm...
```

---

### Cluster 8: code / corner / label

- Count: 6
- Severity: bug
- Preventability: architecture
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

There is a logic error in the corner label priority. Currently, if `nextBrake` is found (which is almost always true as it wraps aro...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**In-corner coaching should override the next-corner label.**

`cornerLabel` is resolved from the next brake point first, and Line 235 only fills the current segment ...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Keep the queried corner label aligned with the telemetry.**

When `topLabel` comes from `inCornerLabel`, this block still sends `targetSpeed`/`distToBrakeM` from `n...
```

```
### Top tile mixes current corner label with next corner's brake data

**Medium Severity**

<!-- DESCRIPTION START -->
When the car is in a corner's apex region, `topCornerLabel` is set to the current...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Send corner query with matching corner telemetry**

When `view.cornerLabel` is overridden to the in-corner label...
```

```
`corner_query` is keyed off `topLabel` (which becomes the *in-corner* label when `inCornerLabel` is set), but the values sent (`distToBrakeM`/`targetSpeed` via `refAtBrake`) are always computed from `...
```

---

### Cluster 9: code / frontmatter / yaml

- Count: 6
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Affected Files: docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The YAML frontmatter is structurally invalid. It should not contain Markdown headers (`##`) or leading empty lines within the bl...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The closing `---` for the YAML frontmatter block is missing. This is required for the metadata section to be recognized and pars...
```

```
The YAML frontmatter in `Current Focus.md` is missing its closing `---` delimiter before the Markdown body (`# Current focus`). As written, the entire document is treated as frontmatter, which can bre...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Restore valid YAML frontmatter in Current Focus node**

`Current Focus.md` no longer has parseable frontmatter: ...
```

```
`Current Focus.md` frontmatter is no longer valid YAML: it starts with `---` but the `type` field was changed to a Markdown heading (`## type: current-focus`). This will break any tooling/Obsidian Pro...
```

```
### YAML frontmatter destroyed in Current Focus file

**High Severity**

<!-- DESCRIPTION START -->
The YAML frontmatter in `Current Focus.md` is corrupted. The `type: current-focus` property was repl...
```

---

### Cluster 10: code / reset / stale

- Count: 6
- Severity: maintainability
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### Rolling reset skips clearing `_cachedRealtimeView` state

**Medium Severity**

<!-- DESCRIPTION START -->
Both `resetDrivingState` and `resetRollingDrivingState` clear `state.realtimeActiveHint = ...
```

```
### Stale recv queue survives reset, repopulates cleared state

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` clears `cornerAdvisories` and resets `currentSimT` to 0, but does not clear ...
```

```
### Reset clears launch flag, preventing sidecar relaunch after crash

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` sets `sidecarChildEverLaunched = false` while leaving `spawnedAlive` ...
```

```
### Stale summary cache after rolling driving state reset

**Medium Severity**

<!-- DESCRIPTION START -->
`resetRollingDrivingState` clears `focusPracticeHudSummary` to `""` but does not reset `focus...
```

```
### Rolling reset skips realtime coaching module reset

**Low Severity**

<!-- DESCRIPTION START -->
`resetRollingDrivingState()` clears `state.realtimeActiveHint` but does not call `realtimeCoaching....
```

```
### Tick runs before lap data update causing stale hints

**Low Severity**

<!-- DESCRIPTION START -->
`realtimeCoaching.tick()` runs in `script.update()` before the lap-completion logic that updates ...
```

---

### Cluster 11: code / this / severity

- Count: 6
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, tools/ai_sidecar/features.py, src/ac_copilot_trainer/modules/realtime_coaching.lua, firmware/screen/src/ui/nav.cpp, firmware/screen/src/main.cpp

**All Examples:**

```
### Missing boolean guard in `_as_float` unlike `_positive_lap_time_ms`

**Low Severity**

<!-- DESCRIPTION START -->
`_as_float` accepts Python booleans (`True` → `1.0`, `False` → `0.0`) because `boo...
```

```
### Unused `COLOR_LABEL` variable is dead code

**Low Severity**

<!-- DESCRIPTION START -->
`COLOR_LABEL` is declared on line 15 but never referenced anywhere in `hud.lua`. All other `COLOR_*` consta...
```

```
### Several HUD fields computed every frame but never rendered

**Low Severity**

<!-- DESCRIPTION START -->
The diff removes all rendering of `vm.sectorMessage`, `vm.coastWarn`, `vm.tireLockupFlash`,...
```

```
### `lastEmittedAt` variable is now dead code

**Low Severity**

<!-- DESCRIPTION START -->
`lastEmittedAt` is declared and initialized to `-1e9` but never read or written to anywhere in the module af...
```

```
### Unused pending_delete tracking variable in navigator

**Low Severity**

<!-- DESCRIPTION START -->
The `pending_delete` static is only ever assigned (in `ui_nav_init`, `ui_nav_pop`, and `async_del...
```

```
### Duplicated `numOr` lambda in dispatch function

**Low Severity**

<!-- DESCRIPTION START -->
The `numOr` lambda is defined twice inside `dispatch_phase2_message` — once for the `coaching.snapshot`...
```

---

### Cluster 12: code / severity / which

- Count: 6
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: tools/ai_sidecar/session.py, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/lap_archive.lua, scripts/check_vault_follow_up.sh

**All Examples:**

```
### `_positive_lap_time_ms` misses `OverflowError` from infinity

**Medium Severity**

<!-- DESCRIPTION START -->
`_positive_lap_time_ms` catches `(TypeError, ValueError)` but `int(float('inf'))` rais...
```

```
### Fallback clock `monoClock` is never incremented

**Low Severity**

<!-- DESCRIPTION START -->
New debounce code uses `opts.simT or monoClock` as the time reference, but `monoClock` is initialized ...
```

```
### `drawSidecarDebrief` is now dead code with no callers

**Low Severity**

<!-- DESCRIPTION START -->
The `drawSidecarDebrief` function in `coaching_overlay.lua` is now completely unused. Its only c...
```

```
### Stats count includes entries that rotate would skip

**Low Severity**

<!-- DESCRIPTION START -->
`stats()` sets `count = #list` without filtering by `type(list[i]) == "string"`, but `rotate()` gu...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**`docs/00_Core/...` entries in `ACK_ALLOW` are dead code.**

`SENSITIVE` on Line 89 only matches paths beginning with `.claude/`, `docs/01_Vault/`, `scripts/`, or `....
```

```
### Dead `wifi_state == Failed` clause in `ws_tick`

**Low Severity**

<!-- DESCRIPTION START -->
The `wifi_state == WifiState::Failed` term in `link_failure` is unreachable from `ws_tick`. Both `wifi...
```

---

### Cluster 13: code / this / template

- Count: 6
- Severity: nit
- Preventability: guideline
- Dominant author type: human
- Affected Files: .github/workflows/vault-automerge.yml, .github/workflows/post-merge-notify.yml, scripts/hook_protect_main_impl.py, scripts/post_merge_sync.sh, tools/pr_pain/pain_score.py

**All Examples:**

```
Valid observation, but deferring to an upstream `template-repo` issue rather than changing it in this sync PR. The workflow comes in verbatim from `template-repo@061d9ab` and reverting the unlabeled-t...
```

```
The `ref: merge_commit_sha` removal is from the upstream template and the classifier script (`scripts/post_merge_classify.py`) reads PR diff via `gh api` rather than the local checkout, so it does not...
```

```
Valid robustness suggestion, but the file is unchanged from upstream `template-repo@061d9ab` in this sync PR. Deferring to an upstream issue so the fix lands once and propagates to every child repo ra...
```

```
Reasonable suggestion on stash label uniqueness, but this file is unchanged from upstream `template-repo@061d9ab`. Deferring to a follow-up issue against the template so the fix is authoritative and p...
```

```
Accepted — REST caps at 250 commits per PR and the GraphQL switch would genuinely improve `fix_commits` / `commits_after_first_review` accuracy for large PRs. Same rationale as the other review thread...
```

```
## Review round 2 disposition

All unresolved review threads on this sync PR fall into two buckets:

**Fixed in this PR**
- `tools/process_miner/github_client.py:327` (Copilot) — tightened `base64.b64...
```

---

### Cluster 14: sorry / unable / review

- Count: 6
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: N/A

**All Examples:**

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

---

### Cluster 15: ac_copilot_trainer / with / modules

- Count: 5
- Severity: reliability
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: N/A

**All Examples:**

```
<h3>Review Summary by Qodo</h3>

Add Ollama post-lap debrief with HUD display and rules fallback (issue #46)

<code>✨ Enhancement</code> <code>🧪 Tests</code>

<img src="https://www.qodo.ai/wp-content/...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (1)</code>  <code>📘 Rule violations (3)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (1)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (1)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

```
<h3>Review Summary by Qodo</h3>

Sidecar auto-launch and per-lap archive with car setup capture

<code>✨ Enhancement</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.sv...
```

---

### Cluster 16: code / brake / corner

- Count: 5
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
`cornerLabelForBrake()` uses `math.abs(s.brakeSpline - brakeSeg.s0)` to match a brake segment to its corner. This breaks for wrap-around near spline 0/1 (e.g., brakeSpline=0.001, s0=0.989 → abs≈0.988)...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The spline distance calculation does not account for the track wrap-around at the start/finish line. If a brake segment starts a...
```

```
### Wrap-around spline distance breaks label lookup near start/finish

**Medium Severity**

<!-- DESCRIPTION START -->
`cornerLabelForBrake` computes spline distance as `math.abs(s.brakeSpline - brake...
```

```
`cornerLabelForBrake()` uses `math.abs(s.brakeSpline - brakeSeg.s0)` which breaks for wrap-around positions near 0/1 (e.g., brakeSpline=0.005 vs brakeSeg.s0=0.993). This can mis-assign corner labels a...
```

```
`cornerLabelForBrake()` performs a full scan of `segments` on every call and matches corner labels using `splineDist(s.brakeSpline, brakeSeg.s0) < 0.03`. Two issues: (1) this is O(n) per frame in brak...
```

---

### Cluster 17: code / code_block / remove

- Count: 5
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove unused color constants.**

`COLOR_RED_LIGHT` and `PANEL_PAD_X` are defined but never used, as flagged by static analysis. Remove them to reduce noise.

<details>
<s...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove unused `carSp` parameter from `placeholderView`.**

The static analysis correctly flags that `carSp` is never used in this function. Since the placeholder view does...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove unused dedupe variables left over from the removed time-based suppression.**

Static analysis correctly flags `DEDUP_HOLD_SEC` (line 30) as unused and `lastEmittedA...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove dead `hasReference` variable.**

`hasReference` is computed at line 137 but never used for any actual logic—the only "use" is the dummy assignment `local _ = hasRef...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Remove unused `lastCornerQueryAt` variable.**

The variable is declared but never accessed (confirmed by static analysis). The comment states it's "kept for backwar...
```

---

### Cluster 18: ranking / code / computes

- Count: 5
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: tools/ai_sidecar/session.py

**All Examples:**

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Ranking on new pb <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
LapComparis...
```

```
LapComparisonState computes and returns an improvement ranking *before* updating the PB when the current lap is faster than the stored best. That means a new PB lap can still return regressions vs the...
```

```
> Ranking on new pb — computes improvement ranking against the previous PB before updating PB, so a new fastest lap could still get improvementRanking.

Addressed: `improvement_ranking_for` now treats...
```

```
> LapComparisonState computes ranking before updating PB when the current lap is faster than the stored best.

Addressed: Same as Qodo thread — new PB path updates reference first and returns an empty...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

---

### Cluster 19: code / json / guard

- Count: 5
- Severity: bug
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: tools/ai_sidecar/protocol.py, tools/ai_sidecar/external_protocol.py, tools/ai_sidecar/coaching/llm_coach.py

**All Examples:**

```
### Unguarded `.get()` on possibly non-dict JSON response crashes connection

**Medium Severity**

<!-- DESCRIPTION START -->
`json.loads(raw)` can return any JSON type (list, string, number, `None`, ...
```

```
### Missing bool guard on float() for JSON values

**Medium Severity**

<!-- DESCRIPTION START -->
The `corner_query` handler coerces untrusted JSON values `cur`, `ref`, and `dist` via `float(inbound....
```

```
### Protocol field lacks bool guard on int coercion

**Low Severity**

<!-- DESCRIPTION START -->
The `int(proto_raw)` coercion at the protocol validation entry point has no `isinstance(proto_raw, boo...
```

```
### `is_external_frame` missing bool guard on version check

**Medium Severity**

<!-- DESCRIPTION START -->
`is_external_frame` compares `frame.get("v") == 1` without a `bool` guard. Since Python `Tr...
```

```
### Version check missing bool guard in validate_inbound

**Low Severity**

<!-- DESCRIPTION START -->
`is_external_frame` correctly guards against `bool` with `not isinstance(v, bool)` before compari...
```

---

### Cluster 20: your / warning / have

- Count: 5
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: gemini
- Affected Files: N/A

**All Examples:**

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

---

### Cluster 21: sorry / agorokh / your

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: N/A

**All Examples:**

```
Sorry @agorokh, your pull request is larger than the review limit of 150000 diff characters
```

```
Sorry @agorokh, your pull request is larger than the review limit of 150000 diff characters
```

```
Sorry @agorokh, you have reached your weekly rate limit of 500000 diff characters.

Please try again later or [upgrade](https://app.sourcery.ai/login?connection=github&git_namespace_id=23216638&utm_so...
```

```
Sorry @agorokh, you have reached your weekly rate limit of 500000 diff characters.

Please try again later or [upgrade](https://app.sourcery.ai/login?connection=github&git_namespace_id=23216638&utm_so...
```

---

### Cluster 22: alpha / text / background

- Count: 4
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: tests/test_design_conformance.py, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
**issue (testing):** Docstring mentions title text alpha, but the test currently only verifies background alpha

The assertions only check the background rgbm alpha, so the title text alpha requiremen...
```

```
This docstring claims the test checks both (1) background alpha < 1 and (2) title text alpha == 1 “before fade”, but the assertions only validate the background alpha. Either add an assertion that the...
```

```
Design brief/PR description says text should be fully opaque (background only is transparent), but COLOR_BRAND is defined with alpha 0.70 and used for footer text. Consider making footer text alpha 1....
```

```
The module header/doc says “text 100% opaque”, but `textAlpha` is set to `fadeAlpha` and then applied to all text colors. If the design requirement is that only the background is transparent (text alw...
```

---

### Cluster 23: code / archive / severity

- Count: 4
- Severity: bug
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/lap_archive.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### Nonexistent `copilotStateDir` breaks all archive operations

**High Severity**

<!-- DESCRIPTION START -->
`lapArchiveDir()` calls `persistence.copilotStateDir()`, but that function does not exist...
```

```
### Archive uses pretty-print JSON, inflating file sizes

**Medium Severity**

<!-- DESCRIPTION START -->
`M.write()` calls `persistence.encodeJson(rec)` which uses `JSON.stringify(t, true)` — the `tr...
```

```
### Duplicated car/track ID extraction from persistence module

**Low Severity**

<!-- DESCRIPTION START -->
`carIdFromOptsCar` and `trackIdFromOptsSim` duplicate nearly identical functions `tryCarFro...
```

```
### Archive write not pcall-wrapped, risking script termination

**Medium Severity**

<!-- DESCRIPTION START -->
`lapArchive.write` is called without `pcall` in the lap boundary handler. Inside `write...
```

---

### Cluster 24: branch / code / local

- Count: 4
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Affected Files: scripts/post_merge_sync.sh

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Fail sync when local main is ahead of origin/main**

In `phase_sync`, `git pull --ff-only origin main` is treate...
```

```
This deletes any existing local branch named `vault/post-merge-pr<PR>` and then force-pushes it. That’s potentially destructive if someone has local work on that branch name (or if a prior run created...
```

```
`git push -u --force-with-lease origin "$VAULT_BRANCH"` can overwrite an existing remote branch if this script is rerun and the remote branch already exists (especially if the local repo hasn’t fetche...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Stop force-deleting unrelated local branches during sync**

This loop force-deletes every local branch whose ups...
```

---

### Cluster 25: code / socket / close

- Count: 4
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close prior sidecar socket before reconfiguring URL**

The settings path now calls `wsBridge.configure(url)` whe...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close active websocket inside ws_bridge.reset**

`M.reset()` drops the `sock` reference without closing it, but ...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Ignore stale socket close callbacks**

`_onClose` unconditionally assigns `sock = nil`, but this callback is sha...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close callable websocket handles during bridge teardown**

Fresh evidence this still applies after the socket-cl...
```

---

### Cluster 26: code / code_block / language

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: firmware/screen/README.md, docs/01_Vault/AcCopilotTrainer/01_Decisions/screen-firmware-in-trainer-monorepo.md, docs/01_Vault/AcCopilotTrainer/03_Investigations/jc3248w535-board-identification-2026-04-21.md, docs/01_Vault/AcCopilotTrainer/03_Investigations/screen-firmware-windows-build-gotchas-2026-04-21.md

**All Examples:**

```
_⚠️ Potential issue_ | _🟡 Minor_

**Fenced code block missing language hint (MD040).**

markdownlint flags the `Layout` tree block. Add a language (`text` is fine for a directory tree) to silence the ...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Specify a language on the fenced code block (MD040).**

markdownlint flags Line 23 for the missing language tag. Use `text` (or `tree`) for a plain directory listin...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Add a language to the boot-log fence.**

`markdownlint` will flag this as MD040; serial captures should use `text`.


<details>
<summary>Proposed fix</summary>

```...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Mark the SCons phase diagram as text.**

This clears MD040 for the non-code timeline block.


<details>
<summary>Proposed markdown fix</summary>

```diff
-```
+```t...
```

---

### Cluster 27: code / code_block / lines

- Count: 4
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**Consider reusing `focusLabelMap()` to reduce duplication.**

Lines 1000-1006 duplicate the logic in `focusLabelMap()` (lines 333-341). Since `focusPracticeActive` is alrea...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Reset the active-suggestion state on session resets.**

`fadeAlpha`, `fadeTarget`, and `lastHintText` live at module scope, so they survive track exits. Because `sc...
```

```
_⚠️ Potential issue_ | _🔴 Critical_

**Restore the `hud.reset()` compatibility hook.**

`src/ac_copilot_trainer/ac_copilot_trainer.lua`, Lines 586 and 604 still call `hud.reset()`. With no `M.reset` e...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Include `subState` in the dedupe key.**

The current key collapses `ON PACE`→`APPROACHING` and `PREPARE TO BRAKE`→`BRAKE NOW` for the same corner because they all s...
```

---

### Cluster 28: code / function / draw

- Count: 4
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Affected Files: tests/test_design_conformance.py

**All Examples:**

```
**suggestion (testing):** Font push/pop balancing is checked globally, not per draw function as the requirement states

The docstring requires each draw function to bracket fontMod.push()/fontMod.pop(...
```

```
The docstring says this verifies “Every draw function … brackets font push/pop”, but the implementation only checks total `fontMod.push()`/`fontMod.pop()` counts across the whole file. This can produc...
```

```
### Regex excludes main `M.draw` from font balance check

**Medium Severity**

<!-- DESCRIPTION START -->
The regex `r"^function\s+M\.(draw\w+)\s*\("` uses `\w+` (one or more word characters after "dr...
```

```
Hey - I've found 2 issues, and left some high level feedback:

- A number of the tests assert on exact code snippets (e.g. string literals like `'#out >= 3'`, `"function M.drawMainWindowStrip"`, `"bmw...
```

---

### Cluster 29: code / loopback / token

- Count: 4
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: tools/ai_sidecar/server.py, tests/test_ai_sidecar_external.py

**All Examples:**

```
`--token` help text says it's "Required whenever --external-bind is non-loopback", but the implementation exits whenever `--external-bind` is set and `--token` is missing (even if `--external-bind` is...
```

```
Current CLI still allows `--host 0.0.0.0` (or other non-loopback) without `--token`, which would expose an unauthenticated socket despite the new external-client auth model. Consider refusing non-loop...
```

```
Token-gate tests currently only cover “no header → 401” and “matching header → accept”. If loopback clients are supposed to connect without the token even when `--token` is set (per issue #81 / PR des...
```

```
`--external-bind` currently always requires `--token`, even if the bind address is loopback. This contradicts the `--token` help text (“Required whenever --external-bind is non-loopback”) and makes it...
```

---

### Cluster 30: code / severity / nbsp

- Count: 4
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, tools/ai_sidecar/coaching/llm_coach.py, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
### Uncaught `http.client` exception crashes WebSocket handler

**Medium Severity**

<!-- DESCRIPTION START -->
The `except` clause in `call_ollama_generate` catches `urllib.error.URLError`, `TimeoutE...
```

```
### Reset clears process-alive flag without terminating child

**Low Severity**

<!-- DESCRIPTION START -->
`M.reset()` sets `spawnedAlive = false` and `sidecarChildEverLaunched = false` even though t...
```

```
### Config set of wsSidecarUrl silently drops ack

**Low Severity**

<!-- DESCRIPTION START -->
When an external client sends `config.set` with key `wsSidecarUrl`, `applyExternalConfigSet` calls `wsBr...
```

```
### Generic config.set path silently skips persistence

**Medium Severity**

<!-- DESCRIPTION START -->
The fall-through path in `applyExternalConfigSet` updates `config[key]` in memory but never pers...
```

---

### Cluster 31: code / toast / timer

- Count: 4
- Severity: reliability
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: firmware/screen/src/ui/toast.cpp, firmware/screen/src/ui/screen_ac_copilot.cpp

**All Examples:**

```
### Toast timer fires after screen deletion

**High Severity**

<!-- DESCRIPTION START -->
`ui_toast_show` creates the toast as a child of `lv_scr_act()` and arms a one-shot `lv_timer` that holds the ...
```

```
_⚠️ Potential issue_ | _🔴 Critical_

**Use-after-free if the toast's parent screen is popped before the timer fires.**

If `ui_nav_pop()` (or any screen replacement) deletes the screen the toast was a...
```

```
### Toast timer use-after-free from double deletion

**High Severity**

<!-- DESCRIPTION START -->
`lv_timer_set_repeat_count(t, 1)` tells LVGL 8.3 to auto-delete the timer when the repeat count reach...
```

```
### Freed pointer compared in async refresh callback

**Low Severity**

<!-- DESCRIPTION START -->
The comment in `async_refresh_cb` claims "`LV_EVENT_DELETE` fires before `lv_async_call` drains," but...
```

---

### Cluster 32: code / warning / with

- Count: 4
- Severity: maintainability
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: tools/ai_sidecar/server.py

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**Fan-out trust model: any authenticated peer can spoof server→client frames to other peers.**

`_handle_external_frame` delegates validation to `validate_inbound`, w...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Peer must send `hello` before any frame is fanned out to it.**

`_external_peers.add(websocket)` only runs in the `TYPE_HELLO` branch, so a peer that connects and i...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Reject client commands when no Lua peer is connected**

After a client sends `hello`, request frames like `confi...
```

```


<details>
<summary>♻️ Duplicate comments (2)</summary><blockquote>

<details>
<summary>tools/ai_sidecar/server.py (1)</summary><blockquote>

`521-531`: _🧹 Nitpick_ | _🔵 Trivial_

**Redundant token-g...
```

---

### Cluster 33: corner / real / time

- Count: 4
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_hints.lua

**All Examples:**

```
`realtimeCoaching.tick()` is fed `lastLapCornerFeats` (features from the previous completed lap) rather than any current-lap telemetry, so “real-time” hints will reflect last lap’s entry/min speeds in...
```

```
`buildRealTime()` only compares **last-lap** corner features vs **best-lap** features; it doesn’t use any current-lap telemetry (e.g., current speed on approach). That means the “real-time” hint won’t...
```

```
The realtime coaching tick currently generates hints solely from `state.lastLapCornerFeats` vs `state.bestCornerFeatures`. That means hints during lap N are based on lap N-1 performance and won’t reac...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements a new real-time per-corner coaching engine as a Lua state machine with an O(1) bucket-based track segment in...
```

---

### Cluster 34: persistent / review / updated

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: N/A

**All Examples:**

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/439817a753b4ea5700a...
```

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/ce20cc4fb1afc32956e...
```

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/92eff75e47176357a67...
```

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/7b7c1f59016c5156ccb...
```

---

### Cluster 35: docs / 01_vault / accopilottrainer

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: N/A

**All Examples:**

```
<h3>Review Summary by Qodo</h3>

Vault enrichment: EPIC #59 hardware roadmap, PR #75–#84 coverage, debugging journey, and Figma design source

<code>📝 Documentation</code>

<img src="https://www.qodo....
```

```
<h3>Review Summary by Qodo</h3>

Post-merge handoff updates for template-sync PR #87

<code>📝 Documentation</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.svg" height...
```

```
<h3>Review Summary by Qodo</h3>

Document PR #91 Phase-2 Part A+B progress and ci-conventional pitfall

<code>📝 Documentation</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-gre...
```

---

### Cluster 36: obsidian / docs / 01_vault

- Count: 3
- Severity: maintainability
- Preventability: automation
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: N/A

**All Examples:**

```
<h3>Review Summary by Qodo</h3>

Post-merge vault handoff for PR #91 with Obsidian config and rig documentation

<code>📝 Documentation</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/...
```

```
<h3>Review Summary by Qodo</h3>

Remove Obsidian config and accidental draft from vault

<code>🐞 Bug fix</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.svg" height="1...
```

```
## Code Review

This pull request removes local Obsidian configuration files and a rig setup documentation file. The reviewer recommends updating the .gitignore file to exclude the .obsidian/ director...
```

---

### Cluster 37: code / severity / ac_copilot_trainer

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/lap_archive.lua

**All Examples:**

```
### Telemetry footer never renders in idle state

**Medium Severity**

<!-- DESCRIPTION START -->
`drawTelemetryFooter` guards on `fadeAlpha < 0.01` and returns early, but `fadeAlpha` is only ever inc...
```

```
### Fade-out clears hint content one frame before render stops

**Low Severity**

<!-- DESCRIPTION START -->
The `elseif fadeAlpha < 0.01` branch clears `lastHintText`, `lastHintKind`, and `lastCorner...
```

```
### Stats mishandles negative file sizes unlike rotate

**Low Severity**

<!-- DESCRIPTION START -->
`stats()` adds raw `io.fileSize` results to the total via `tonumber(s) or 0`, which means negative ...
```

---

### Cluster 38: code / already / addressed

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: human
- Affected Files: firmware/screen/include/board/JC3248W535_Touch.h

**All Examples:**

```
Already addressed in 8f88881: the guard now reads `if (rx >= JC_TOUCH_NATIVE_W || ry >= JC_TOUCH_NATIVE_H) return false;`, clamping against the actual native panel bounds (320×480) instead of the loos...
```

```
Already addressed in 8f88881: the guard now clamps against the actual native panel bounds — `if (rx >= JC_TOUCH_NATIVE_W || ry >= JC_TOUCH_NATIVE_H) return false;` (JC3248W535_Touch.h line 60). The 50...
```

```
Already addressed in 8f88881: `if (rx >= JC_TOUCH_NATIVE_W || ry >= JC_TOUCH_NATIVE_H) return false;` (JC3248W535_Touch.h line 60). The 500 sentinel is replaced by the panel-native bounds (320×480), s...
```

---

### Cluster 39: code / addressed / lines

- Count: 3
- Severity: reliability
- Preventability: guideline
- Dominant author type: human
- Affected Files: src/ac_copilot_trainer/modules/setup_library.lua, firmware/screen/src/main.cpp

**All Examples:**

```
Addressed: `dispatch_phase2_message` already accepts both integer and fractional JSON numerics for snapshot fields via the `numOr` lambda in `firmware/screen/src/main.cpp` (lines 528-542 at `fe5ee83`)...
```

```
Addressed: `loadByName` accepts either a basename (legacy) or a `{name=..., path=...}` table where the path wins when both are present. The screen now sends `path` alongside `name` in `setup.load`, an...
```

```
Addressed: `M.list()` now walks `<car>/<track>` AND `<car>/<track>/<layout>` (two-level recursion), see lines 110-141 at `fe5ee83`. Layout-specific setups under `<car>/monza/junior/race.ini` are now e...
```

---

### Cluster 40: code / before / boolean

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: human
- Affected Files: tools/ai_sidecar/session.py, tools/ai_sidecar/features.py

**All Examples:**

```
> Add boolean guard before integer coercion for `lapTimeMs` (`True` → 1 ms PB).

Addressed: `_positive_lap_time_ms` returns `None` when `isinstance(raw, bool)`; added `test_lap_time_true_is_ignored_fo...
```

```
`@agorokh`, thanks for the confirmation! The boolean guard and the accompanying test `test_lap_time_true_is_ignored_for_pb` are exactly the right approach here — rejecting `True`/`False` before the `i...
```

```
> Add explicit boolean check before integer coercion on corner `id` (`int(True)` → corner 1).

Addressed: Skip `item` when `isinstance(cid_raw, bool)` before `int(cid_raw)`; added `test_extract_corner...
```

---

### Cluster 41: setup / best / code

- Count: 3
- Severity: bug
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/setup_library.lua, firmware/screen/include/ui/screen_pocket_technician.h

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Match BEST laps by unique setup identifier**

This compares archived setup records by basename only, so two setu...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Avoid truncating setup path used for disambiguation**

The request struct caps `path` at 160 bytes even though t...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Compute BEST from full archive, not last 200 laps**

`bestForSetup()` is documented and used as the setup’s BEST...
```

---

### Cluster 42: code / thanks / both

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: firmware/screen/src/ui/screen_launcher.cpp, src/ac_copilot_trainer/modules/coaching_publisher.lua

**All Examples:**

```
`@agorokh`, thanks for the detailed summary! Both fixes look correct — the `first_render` flag cleanly avoids the `-fshort-enums` UB, and the null-guard on the `new launcher_ctx_t{}` allocation handle...
```

```
`@agorokh`, thanks for the update! The null-check guard and the `first_render` flag replacement both look like solid, clean fixes — much cleaner than the sentinel cast. Appreciated!

<!-- <review_comm...
```

```
`@agorokh`, thanks for the detailed summary! Both fixes look correct — carrying residual time with `_accumDt = _accumDt - PUBLISH_INTERVAL_SEC` keeps the effective publish rate tight at 10 Hz, and cap...
```

---

### Cluster 43: code / brake / dedupe

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

The dedupe key is too coarse and can suppress critical coaching updates. Since the key only consists of `kind` and `cornerLabel`, a ...
```

```
### Dedupe suppresses urgent BRAKE NOW after PREPARE hint

**Medium Severity**

<!-- DESCRIPTION START -->
The dedupe key is `kind:cornerLabel`, but both "PREPARE TO BRAKE" and "BRAKE NOW" share `kind...
```

```
The 600 ms dedupe key is only `(kind, cornerLabel)`, so different messages of the same kind for the same corner (e.g., `PREPARE TO BRAKE` → `BRAKE NOW`) will be suppressed for up to `DEDUP_HOLD_SEC`, ...
```

---

### Cluster 44: code / branch / line

- Count: 3
- Severity: bug
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/lap_archive.lua, scripts/post_merge_sync.sh, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
### Track length never captured due to userdata type guard

**Medium Severity**

<!-- DESCRIPTION START -->
`buildRecord` guards `sim.trackLengthM` access with `type(sim) == "table"`, but `ac.StateSim...
```

```
### Vault branch not cleaned up on mid-flow failure

**Medium Severity**

<!-- DESCRIPTION START -->
In `phase_vault`, if `git commit` (line 146), `git push` (line 147), or the `gh pr create`/`gh pr e...
```

```
### Unhandled WS types now log warning spam

**Medium Severity**

<!-- DESCRIPTION START -->
The newly added generic-dispatch `else` branch logs an `[COPILOT][WS-DIAG] unhandled request type` line for...
```

---

### Cluster 45: code / float / call

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: tools/ai_sidecar/improvement_ranking.py, src/ac_copilot_trainer/modules/hud_settings.lua, firmware/screen/src/main.cpp

**All Examples:**

```
> The `float(regret)` call is redundant as `regret` is already a float.

Addressed: Use `round(regret, 4)` for `priority`.
```

```
### Disk cap slider persists unclamped raw float value

**Low Severity**

<!-- DESCRIPTION START -->
The `ui.slider` returns a raw floating-point value (e.g., `247.3`), and `pcall(vm.setLapArchiveMaxM...
```

```
### `is<int>` rejects floats for `best_ms` parsing

**Low Severity**

<!-- DESCRIPTION START -->
`best_ms` is parsed with `entry["best_ms"].is<int>() ? ... : -1`, but the trainer derives lap times fro...
```

---

### Cluster 46: code / call / module

- Count: 3
- Severity: nit
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: gemini
- Affected Files: tools/ai_sidecar/features.py, tools/ai_sidecar/improvement_ranking.py

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `aliases` dictionary is static and should be defined as a module-level constant to avoid re-allocation on every call to `_no...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `float(regret)` call is redundant as `regret` is already a float.

```suggestion
                "priority": round(regret, 4...
```

```
> The `aliases` dictionary is static and should be defined as a module-level constant to avoid re-allocation on every call to `_normalize_metric_key`. Additionally, since `name` is type-hinted as a st...
```

---

### Cluster 47: code / staged / changes

- Count: 3
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Affected Files: scripts/post_merge_sync.sh, scripts/check_vault_follow_up.sh

**All Examples:**

```
`phase_vault` only checks for out-of-scope changes using `git diff --name-only HEAD` (working tree), but it does not check the index (`--cached`). If there are staged non-vault changes, `git checkout ...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

This check only inspects staged changes (`--cached`). It can be bypassed if a user runs `git commit -a` (or `-am`), which commit...
```

```
### Vault out-of-scope check misses staged-only index changes

**Low Severity**

<!-- DESCRIPTION START -->
The vault-change detection on line 71 properly inspects both `git diff` (index→worktree) and...
```

---

### Cluster 48: code / wifi / close

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: firmware/screen/src/main.cpp

**All Examples:**

```
**suggestion (bug_risk):** Consider surfacing a clear disconnected state when WiFi drops, not only on clean WS close.

Right now the 3 s grace window and `APP_DISCONNECTED` transition depend only on W...
```

```
### Disconnect grace fires while WS still Open

**Medium Severity**

<!-- DESCRIPTION START -->
A transient `WiFi.status() != WL_CONNECTED` reading inside `ws_tick` arms the grace timer even when `ws_...
```

```
### Stale Open state blocks reconnect after WiFi blip

**Medium Severity**

<!-- DESCRIPTION START -->
When WiFi briefly drops and reconnects faster than the underlying TCP/WS keepalive can detect the...
```

---

### Cluster 49: code / commit / staged

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: scripts/check_vault_follow_up.sh, scripts/claude_pretool_vault_guard.sh

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Account for `git add && git commit` in vault guard**

The guard snapshots staged files before the commit command...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Exempt `--dry-run` from vault follow-up commit enforcement**

Per `git commit -h`, `--[no-]dry-run` only “show[s...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Restrict commit-intent regex to actual git commit commands**

The `_git_commit_intent` pattern is still broad en...
```

---

### Cluster 50: code / corner / hint

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
### Dedup causes hint to vanish at phase transitions

**High Severity**

<!-- DESCRIPTION START -->
When the car transitions from "approaching" to "braking" (or "braking" to "corner"), `selectHint` is...
```

```
### Stale hint persists when transitioning between different corners

**Medium Severity**

<!-- DESCRIPTION START -->
The `or activeHint` fallback in `selectHint(label, ...) or activeHint` preserves a...
```

```
### Approach detection preempts the "exiting" phase transition

**Low Severity**

<!-- DESCRIPTION START -->
The approach-detection block (lines 256–272) runs *before* the segment-kind state-machine t...
```

---

### Cluster 51: code / corner / query

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, tools/ai_sidecar/server.py, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Re-send corner queries when revisiting the same turn**

This query gate only sends when `prev == nil` (first tim...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Coalesce queued corner_query tasks per connection**

Each `corner_query` unconditionally spawns a new background...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Reject advisories from prior laps**

`takeCornerAdvisory()` only checks advisory age, not advisory lap, so cache...
```

---

### Cluster 52: dash / code / string

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
`formatLapMs()` is returning a mojibake string ("â") instead of an actual em dash. This will render incorrectly in-game; use a proper UTF-8 em dash ("—") or construct it via `utf8.char`/byte sequenc...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Corrupted UTF-8 character in fallback string.**

Line 81 shows `"â"` but the comment indicates it should be an em dash (`—`, U+2014). This appears to be a UTF-8 enc...
```

```
`formatLapMs()` returns a mojibake string ("â") instead of an actual em dash. This will render incorrectly in-game and in screenshots; use a real em dash ("—") or construct it via UTF-8 bytes (e.g.,...
```

---

### Cluster 53: code / dead / severity

- Count: 3
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: tools/ai_sidecar/external_protocol.py, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### `make_action_ack` and `make_config_ack` are unused

**Low Severity**

<!-- DESCRIPTION START -->
`make_action_ack` and `make_config_ack` are defined as envelope helpers but never imported or calle...
```

```
### Removed call leaves `wsUrlMatchesAutolaunchTarget` as dead code

**Low Severity**

<!-- DESCRIPTION START -->
The `wsUrlMatchesAutolaunchTarget` function (defined at line 95) is now unused — its o...
```

```
### Exported `is_external_frame` function is never used

**Low Severity**

<!-- DESCRIPTION START -->
`is_external_frame` is defined and exported but never called anywhere in the codebase. The server ...
```

---

### Cluster 54: code / tokens / design

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
`COLOR_BG` and `COLOR_BG_BORDER` are defined as design tokens but the panel background/border uses hard-coded `rgbm(...)` values instead, leaving these constants unused. Either use the constants or re...
```

```
### Unused design token constants `COLOR_BG` and `COLOR_BG_BORDER`

**Low Severity**

<!-- DESCRIPTION START -->
`COLOR_BG` and `COLOR_BG_BORDER` are declared as design tokens but never referenced. Th...
```

```
Design tokens `COLOR_BG` / `COLOR_BG_BORDER` are defined but the panel background/border uses duplicated inline `rgbm(...)` literals instead. This makes it easier for tokens to drift from actual rende...
```

---

### Cluster 55: code / with / empty

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Affected Files: firmware/screen/src/main.cpp

**All Examples:**

```
### Empty-string error from sidecar produces blank toast

**Low Severity**

<!-- DESCRIPTION START -->
When parsing `setup.load.ack`, the dispatch passes `ok ? nullptr : error` where `error` is initia...
```

```
Addressed in this push (`fe5ee83`): `dispatch_phase2_message` now collapses an empty `error` C-string to `nullptr` before passing to `screen_pocket_technician_apply_load_ack`, so the screen-side `erro...
```

```
<!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- This is an auto-generated comment: skip review by coderabbit.ai -->

> [!IMPORTANT]
> ## Review skipped
> 
> Too many files!...
```

---

### Cluster 56: code / exit / error

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, .claude/agents/post-merge-steward.md, scripts/post_merge_sync.sh

**All Examples:**

```
### Unknown exit code resets nonzero streak counter

**Low Severity**

<!-- DESCRIPTION START -->
In the `runConsoleProcess` exit callback, when `result` is not a table or lacks `exitCode`, `codeNum` ...
```

```
The documented exit code meanings don’t match the script: `scripts/post_merge_sync.sh` uses exit code `10` for failures like `git checkout main` / `git pull --ff-only` and a failed `git commit`, not o...
```

```
### Missing error handler on `git add` in vault phase

**Low Severity**

<!-- DESCRIPTION START -->
`git add docs/01_Vault/` on line 145 lacks `|| { fail "..."; exit N; }` error handling, unlike every...
```

---

### Cluster 57: code / external / malformed

- Count: 3
- Severity: bug
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: tools/ai_sidecar/server.py

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Reject non-v1 external envelopes explicitly**

`_handler` only calls `_handle_external_frame` when `is_external_...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Route malformed v-envelopes into external validation**

The handler only enters external-envelope error handling...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Route malformed external payloads to v1 error handling**

External frame routing is only attempted after JSON-ro...
```

---

### Cluster 58: code / factory / null

- Count: 3
- Severity: maintainability
- Preventability: guideline
- Dominant author type: human
- Affected Files: firmware/screen/src/main.cpp

**All Examples:**

```
Partially addressed in 9bbad35: I added a dimensional sanity check in `setup()` (main.cpp ~line 576-585) that warns to Serial if `gfx_canvas->width()/height()` ever diverge from the `JC_TFT_NATIVE_*` ...
```

```
**issue (bug_risk):** The `gfx_canvas` comment claims safety on non-canvas displays, but the later `static_cast` guarantees a non-null, potentially invalid pointer.

In `setup()` you always do `gfx_ca...
```

```
Addressed in ef85f0a: rewrote the comment to match the code. The narrow-once + null-guard at the call site is now framed as a defensive null check, not a "stays null on non-canvas" claim. The actual s...
```

---

### Cluster 59: code / hint / fade

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
### Hint color snaps abruptly during panel fade-out

**Low Severity**

<!-- DESCRIPTION START -->
The fade-out transition preserves `lastHintText` so the text persists during the fade, but doesn't pre...
```

```
### Corner label vanishes instantly instead of fading out

**Medium Severity**

<!-- DESCRIPTION START -->
The corner label rendering is guarded by `hasHint` (line 163), which becomes `false` immediat...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Preserve `cornerLabel` through fade-out too.**

The panel caches `lastHintText` and `lastHintKind`, but the corner label is still read from the live hint. When the ...
```

---

### Cluster 60: code / feedback / severity

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: firmware/screen/src/ui/screen_pocket_technician.cpp

**All Examples:**

```
### Per-add full list rebuild causes quadratic widget churn

**Medium Severity**

<!-- DESCRIPTION START -->
`screen_pocket_technician_add_setup` calls `rebuild_list_widgets` on every entry as the WS ...
```

```
### Pocket Technician error path disables row press feedback

**Low Severity**

<!-- DESCRIPTION START -->
On a `setup.load.ack` failure the code rewrites the row's `LV_PART_MAIN | LV_STATE_PRESSED` b...
```

```
### Load ack applied to wrong row after rapid retap

**Low Severity**

<!-- DESCRIPTION START -->
`screen_pocket_technician_apply_load_ack` discards the `name` parameter and applies the visual feedbac...
```

---

### Cluster 61: code / layout / field

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/modules/setup_library.lua, src/ac_copilot_trainer/modules/lap_archive.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**`rotate()` silently no-ops when file sizes are unavailable — cap is not enforced.**

If `io.fileSize` is missing (or any pcall on it fails), every entry has `size =...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**`bestForSetup` ignores layout despite doc claim — wrong BEST for multi-layout tracks.**

The docstring at lines 138–140 says the BEST is computed for "the current c...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Record layout before enforcing layout-only BEST matching**

When an active track layout is present, this branch ...
```

---

### Cluster 62: code / idle / footer

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Render telemetry footer during idle state**

`M.draw()` calls `drawTelemetryFooter(vm)` in both active and idle ...
```

```
`drawTelemetryFooter()` returns early when `fadeAlpha < 0.01`, but `M.draw()` calls it in the idle path as well. In a fresh/idle session `fadeAlpha` will be 0, so the telemetry footer (speed/REC/delta...
```

```
`drawTelemetryFooter()` returns early when `fadeAlpha < 0.01`, but in the idle path `drawActiveSuggestion()` is never called so `fadeAlpha` stays 0. As a result, the telemetry footer won’t render in t...
```

---

### Cluster 63: code / from / throwing

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**issue:** Handle the case where os.runConsoleProcess fails without throwing, not just when pcall errors

`spawnedAlive` is set to `true` before the `pcall` and only reset if the `pcall` itself errors...
```

```
### Nil return from spawn API leaves sidecar permanently broken

**Medium Severity**

<!-- DESCRIPTION START -->
The return value check `if a == false then` only catches an explicit `false` from `os.r...
```

```
Hey - I've found 3 issues, and left some high level feedback:

- In `lap_archive.buildRecord` you ignore the `opts.car` value and always use `ch.safeCarIdRaw()/safeTrackIdRaw()`, so either wire the pa...
```

---

### Cluster 64: code / pill / grace

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: firmware/screen/src/main.cpp

**All Examples:**

```
### Launcher pill stuck in CONNECTING when sidecar unreachable

**Medium Severity**

<!-- DESCRIPTION START -->
`ws_disconnected_pending_at` is only ever armed inside the `ConnectionClosed` event hand...
```

```
### Boot pill flips to disconnected after grace timer

**Medium Severity**

<!-- DESCRIPTION START -->
During a normal boot, `ws_tick()` arms the disconnect grace timer on every iteration where `ws_st...
```

```
### Grace timer deadline can wrap to sentinel zero

**Low Severity**

<!-- DESCRIPTION START -->
`disconnect_grace_arm()` computes the deadline as `millis() + WS_DISCONNECT_GRACE_MS`. If `millis()` is...
```

---

### Cluster 65: code / ignore / patterns

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: human
- Affected Files: firmware/screen/src/ui/fonts/.gitignore

**All Examples:**

```
This `.gitignore` doesn't currently ignore anything: `!*.c`/`!*.bin` only un-ignore patterns that were previously ignored. If the intent is to keep `lv_font_conv` outputs local (per README), add ignor...
```

```
Already addressed in 8f88881: `firmware/screen/src/ui/fonts/.gitignore` now uses concrete patterns `*.c` / `*.bin` (with a comment explaining bundled sources under `src/`, the README, and `OFL.txt` ar...
```

```
Already addressed in 8f88881: `firmware/screen/src/ui/fonts/.gitignore` now ships concrete `*.c` / `*.bin` ignore patterns with a comment explaining the bundled sources under `src/`, the README, and `...
```

---

### Cluster 66: code / layout / setup

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: firmware/screen/src/ui/screen_pocket_technician.cpp, src/ac_copilot_trainer/modules/setup_library.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Invalidate cached setup list on car/track/layout changes**

`M.list()` returns `_cachedList` as soon as it is se...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Clear stale active setup on identity refresh**

`screen_pocket_technician_set_identity()` updates car/track meta...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Reset active setup when track layout changes**

The stale-active guard only clears `g_active_name` when `car_id`...
```

---

### Cluster 67: code / lock / ollama

- Count: 3
- Severity: reliability
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: tools/ai_sidecar/server.py

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Do not hold prepare lock during corner_query Ollama calls**

`_corner_job` takes `_prepare_lock` before running ...
```

```
### Lock held during blocking Ollama call delays lap processing

**Medium Severity**

<!-- DESCRIPTION START -->
The `_corner_job` acquires `_prepare_lock` for the entire duration of the `asyncio.to_t...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Scope prepare lock per websocket connection**

`_prepare_lock` is declared at module scope and then awaited insi...
```

---

### Cluster 68: code / merge / that

- Count: 3
- Severity: maintainability
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: scripts/post_merge_sync.sh

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Re-check PR merge state after invoking `gh pr merge`**

`phase_sync` assumes `gh pr merge` means the PR is alrea...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Re-check PR merge state before treating sync as successful**

`gh pr merge` is not a guarantee that the PR is me...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Validate PR base branch before running auto-merge**

Phase `sync` now merges any OPEN PR number via `gh pr merge...
```

---

### Cluster 69: code / with / newer

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Prefer newer Ollama debriefs over stale queued follow-ups**

This guard only accepts a debrief-only `ollama` fol...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Avoid archiving debrief text for the wrong lap**

The archive record is written immediately after `lap_complete`...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Use pre-increment lap counter for advice snapshot**

`corner_advice_used` is captured after `state.lapsCompleted...
```

---

### Cluster 70: code / real / ssid

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: firmware/screen/secrets/wifi.h.example

**All Examples:**

```
### Real SSID leaked in committed example file

**Low Severity**

<!-- DESCRIPTION START -->
`wifi.h.example` contains `WIFI_SSID "AHOME5G"` which appears to be a real home network SSID, unlike the co...
```

```
`wifi.h.example` contains what looks like a real SSID (`AHOME5G`). Even though it’s an example, it’s safer to keep templates strictly placeholder-only (e.g., `"your-ssid-here"`) to avoid accidentally ...
```

```
This example secrets template includes what looks like a real SSID (`AHOME5G`). Even in an `.example` file, avoid committing real network identifiers—use a generic placeholder (e.g., `"your-ssid-here"...
```

---

### Cluster 71: code / socket / with

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close failed WebSocket before clearing bridge handle**

When `reconnect = true`, this module intentionally keeps...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Re-send hello after loopback socket reconnects**

This `hello` is only sent during the initial `tryOpen` call, b...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Emit Lua hello without relying on params.onOpen**

Fresh evidence: the repo’s CSP socket API notes (`docs/01_Vau...
```

---

### Cluster 72: code / with / this

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: gemini
- Affected Files: docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md, docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, tests/test_ai_sidecar_protocol.py

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The dimensions `132×456` do not align with the window sizes defined in `src/ac_copilot_trainer/manifest.ini` (`520,200` for `WIN...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The dimensions `132×456` do not align with the window sizes defined in `src/ac_copilot_trainer/manifest.ini` (`520,200` for `WIN...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Monkeypatch signature mismatch with actual function.**

The monkeypatch uses `lambda **_: None` but `compose_corner_hint` has a specific signature with positional-or-keywo...
```

---

### Cluster 73: code / warning / variable

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: firmware/screen/src/ui/screen_launcher.cpp, firmware/screen/src/main.cpp

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**Gate the demo action before merging.**

This sends a real `toggleFocusPractice` action every 10 seconds after WS open. With the Lua handlers now wired, the screen c...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Unchecked `new` — guard against allocation failure.**

ESP32 Arduino is built with `-fno-exceptions`, so `new` returns `nullptr` on OOM rather than throwing. A fail...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Avoid casting `0xFF` to `app_state_t`; use a dedicated "dirty" flag.**

`app_state_t` enumerates `BOOTING/CONNECTED/LAUNCHER_IDLE/DISCONNECTED` (values 0..3). Casti...
```

---

### Cluster 74: merge / conflicts / this

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: human
- Affected Files: N/A

**All Examples:**

```
@copilot resolve the merge conflicts in this pull request
```

```

> @copilot resolve the merge conflicts in this pull request

Conflicts are resolved by merging `origin/main` into this branch and committing the handoff-file resolution in `9271c24`. The PR now has a...
```

```
@copilot resolve the merge conflicts in this pull request
```

---

### Cluster 75: test / docstring / phase

- Count: 3
- Severity: maintainability
- Preventability: architecture
- Dominant author type: bot
- Affected Files: tests/test_design_conformance.py

**All Examples:**

```
**issue (testing):** PD-02 description says "6 phases" but tests and implementation only cover 5, which makes the test misleading.

The docstring mentions “all 6 phase string literals,” but the test a...
```

```
PD-02 comment says the state machine has "6 phase string literals", but the test only checks for 5 phases (straight/approaching/braking/corner/exiting). Update the docstring to match the actual assert...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Test method name contradicts docstring: "six_phases" vs "5 phase".**

The method is named `test_realtime_coaching_six_phases` but the docstring says "5 phase string...
```

---

### Cluster 76: failed / format / error

- Count: 3
- Severity: bug
- Preventability: automation
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: tests/test_design_conformance.py

**All Examples:**

```
_⚠️ Potential issue_ | _🟡 Minor_

**Fix formatting to pass CI.**

The pipeline reports that `ruff format --check` failed for this file. Run the formatter to fix:

```shell
python3 -m ruff format tests...
```

```
## CI Feedback 🧐

A test triggered by this PR failed. Here is an AI-generated analysis of the failure:

<table><tr><td>

**Action:** build</td></tr>
<tr><td>

**Failed stage:** [make ci-fast](https://...
```

```
## CI Feedback 🧐

A test triggered by this PR failed. Here is an AI-generated analysis of the failure:

<table><tr><td>

**Action:** build</td></tr>
<tr><td>

**Failed stage:** [make ci-fast](https://...
```

---

### Cluster 77: security / issues / found

- Count: 3
- Severity: security
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: N/A

**All Examples:**

```
New security issues found
```

```
New security issues found
```

```
New security issues found
```

---

### Cluster 78: vault / merge / post

- Count: 3
- Severity: bug
- Preventability: typecheck
- Dominant author type: bot
- Affected Files: N/A

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Introduce a deterministic post-merge steward flow that standardizes post-merge sync, classification, and vault-only han...
```

```
<h3>Review Summary by Qodo</h3>

Add deterministic post-merge steward contract with sync, classify, and vault automation

<code>✨ Enhancement</code>

<img src="https://www.qodo.ai/wp-content/uploads/2...
```

```
## Code Review

This pull request introduces the post-merge-steward agent and supporting scripts (post_merge_sync.sh, post_merge_classify.py, and check_vault_follow_up.sh) to automate post-merge synch...
```

---

## Hook suggestions (not applied)

The following are **suggested** `settings.json` hook snippets for `preventability: automation` patterns.
Review and merge manually if appropriate.

### code / code_block / agorokh
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / code_block / agorokh (30 occurrences)
```

### obsidian / docs / 01_vault
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: obsidian / docs / 01_vault (3 occurrences)
```

### failed / format / error
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: failed / format / error (3 occurrences)
```
