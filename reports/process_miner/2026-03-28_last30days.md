# Process Improvement Miner Report

**Repository:** agorokh/ac-copilot-trainer
**Analysis period:** last 30 day(s) (since 2026-03-28 UTC, through 2026-04-27 UTC)
**Generated:** 2026-04-27T09:57:05.476012+00:00

---

## Executive Summary

- **PRs Analyzed:** 40
- **Total Comments:** 1630
- **Average Comments per PR:** 40.8
- **Total Files Changed:** 493
- **Total Additions:** 39,928
- **Total Deletions:** 5,238
- **CI Failures:** 5
- **Comment Clusters Found:** 69

---

## Bot vs human review comments

Counts are over review and issue thread comments for this run (excludes the PR body).

- **bot:** 1539
- **human:** 91

---

## Per-bot comment severity (heuristic)

- **bugbot:** bug=54, maintainability=32, nit=166, reliability=8
- **chatgpt_codex_connector_bot:** bug=30, maintainability=14, nit=170, perf=1, reliability=4
- **coderabbit:** bug=149, maintainability=210, nit=16, perf=4, reliability=67, security=5
- **copilot:** bug=39, maintainability=55, nit=236, perf=4, reliability=6, security=3
- **gemini:** bug=13, maintainability=15, nit=58, perf=10, reliability=2, security=4
- **github_actions:** nit=2
- **qodo_code_review_bot:** bug=21, maintainability=11, nit=10, perf=1, reliability=2, security=3
- **sourcery:** bug=26, maintainability=40, nit=35, perf=3, reliability=2, security=8

---

## Multi-bot agreement (PRs with ≥2 distinct bots)

- **PRs in window with ≥2 bots:** 39

Per-PR summaries (pair co-occurrence counts use agreement locations: file+line when present, else path+comment id, else comment id):

- **PR #92** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #90** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #89** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #88** — bots: chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: copilot|gemini=1

- **PR #87** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 2
  top pairs: bugbot|copilot=2, bugbot|chatgpt_codex_connector_bot=1, chatgpt_codex_connector_bot|copilot=1

- **PR #85** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: chatgpt_codex_connector_bot|copilot=1

- **PR #84** — bots: coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 0

- **PR #83** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, github_actions, qodo_code_review_bot, sourcery; locations with multiple bots: 4
  top pairs: coderabbit|copilot=2, bugbot|coderabbit=1, chatgpt_codex_connector_bot|coderabbit=1

- **PR #80** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, github_actions, qodo_code_review_bot, sourcery; locations with multiple bots: 4
  top pairs: bugbot|coderabbit=2, chatgpt_codex_connector_bot|coderabbit=1, chatgpt_codex_connector_bot|gemini=1

- **PR #78** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 3
  top pairs: bugbot|chatgpt_codex_connector_bot=1, bugbot|copilot=1, chatgpt_codex_connector_bot|copilot=1, coderabbit|copilot=1, coderabbit|gemini=1

- **PR #75** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: coderabbit|copilot=1

- **PR #74** — bots: coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: copilot|gemini=2

- **PR #73** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: bugbot|coderabbit=1, chatgpt_codex_connector_bot|coderabbit=1

- **PR #70** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: coderabbit|copilot=2, bugbot|coderabbit=1, bugbot|copilot=1

- **PR #68** — bots: coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

- **PR #67** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

- **PR #65** — bots: coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

- **PR #64** — bots: bugbot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: bugbot|coderabbit=1, bugbot|copilot=1, coderabbit|copilot=1, coderabbit|gemini=1

- **PR #63** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 2
  top pairs: bugbot|copilot=1, coderabbit|copilot=1

- **PR #51** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: coderabbit|gemini=1

- **PR #42** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, qodo_code_review_bot, sourcery; locations with multiple bots: 1
  top pairs: coderabbit|gemini=1

- **PR #40** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot; locations with multiple bots: 1
  top pairs: bugbot|copilot=1

- **PR #34** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot; locations with multiple bots: 0

- **PR #30** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot; locations with multiple bots: 1
  top pairs: bugbot|copilot=1

- **PR #62** — bots: bugbot, chatgpt_codex_connector_bot, coderabbit, copilot, gemini, sourcery; locations with multiple bots: 0

---

## Analyzed PRs

- **PR #92:** [docs(vault): session SAVE — PR #91 Phase-2 Part A+B in flight](https://github.com/agorokh/ac-copilot-trainer/pull/92)
  - Author: agorokh
  - Merged: 2026-04-25
  - Comments: 9
  - Files: 5
  - CI: failure

- **PR #90:** [docs(vault): post-merge handoff for PR #89](https://github.com/agorokh/ac-copilot-trainer/pull/90)
  - Author: agorokh
  - Merged: 2026-04-25
  - Comments: 6
  - Files: 1
  - CI: failure

- **PR #89:** [fix(gitattributes): pin *.sh and *.bash to eol=lf](https://github.com/agorokh/ac-copilot-trainer/pull/89)
  - Author: agorokh
  - Merged: 2026-04-25
  - Comments: 10
  - Files: 1
  - CI: failure

- **PR #88:** [docs(vault): post-merge handoff for PR #87](https://github.com/agorokh/ac-copilot-trainer/pull/88)
  - Author: agorokh
  - Merged: 2026-04-24
  - Comments: 11
  - Files: 3
  - CI: failure

- **PR #87:** [chore: sync template to template-repo@061d9ab (template-2026.04)](https://github.com/agorokh/ac-copilot-trainer/pull/87)
  - Author: agorokh
  - Merged: 2026-04-24
  - Comments: 68
  - Files: 200
  - CI: failure

- **PR #85:** [docs(vault): enrich with PR #70-#84 context + EPIC #59 + debugging journey + Figma design](https://github.com/agorokh/ac-copilot-trainer/pull/85)
  - Author: agorokh
  - Merged: 2026-04-23
  - Comments: 28
  - Files: 15
  - CI: success

- **PR #84:** [docs(vault): post-merge handoff for PR #83](https://github.com/agorokh/ac-copilot-trainer/pull/84)
  - Author: agorokh
  - Merged: 2026-04-22
  - Comments: 9
  - Files: 2
  - CI: success

- **PR #83:** [feat(sidecar,bridge): external WS client support (issue #81)](https://github.com/agorokh/ac-copilot-trainer/pull/83)
  - Author: agorokh
  - Merged: 2026-04-22
  - Comments: 178
  - Files: 47
  - CI: success

- **PR #80:** [feat(post-merge): roll out deterministic steward contract](https://github.com/agorokh/ac-copilot-trainer/pull/80)
  - Author: agorokh
  - Merged: 2026-04-22
  - Comments: 178
  - Files: 12
  - CI: success

- **PR #78:** [feat(#77): sidecar auto-launch + per-lap archive (with car setup capture)](https://github.com/agorokh/ac-copilot-trainer/pull/78)
  - Author: agorokh
  - Merged: 2026-04-21
  - Comments: 127
  - Files: 11
  - CI: success

- **PR #76:** [chore(deps-dev): bump the python group with 14 updates](https://github.com/agorokh/ac-copilot-trainer/pull/76)
  - Author: dependabot[bot]
  - Merged: 2026-04-19
  - Comments: 0
  - Files: 1
  - CI: success

- **PR #75:** [fix(#75): in-game smoke test — CSP runtime fixes + Ollama coaching pipeline](https://github.com/agorokh/ac-copilot-trainer/pull/75)
  - Author: agorokh
  - Merged: 2026-04-14
  - Comments: 120
  - Files: 19
  - CI: success

- **PR #74:** [docs(vault): SAVE — PR #73 merged, refresh handoff + current focus](https://github.com/agorokh/ac-copilot-trainer/pull/74)
  - Author: agorokh
  - Merged: 2026-04-12
  - Comments: 9
  - Files: 2
  - CI: success

- **PR #73:** [fix(#72): Phase 5 HUD rebuild — live-frame engine + bundled fonts + FIXED_SIZE windows + always-visible tiles](https://github.com/agorokh/ac-copilot-trainer/pull/73)
  - Author: agorokh
  - Merged: 2026-04-07
  - Comments: 67
  - Files: 16
  - CI: success

- **PR #70:** [fix(#69): visual design rewrite matching Figma spec](https://github.com/agorokh/ac-copilot-trainer/pull/70)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 48
  - Files: 6
  - CI: success

- **PR #68:** [docs: SAVE vault after PR #67 merge](https://github.com/agorokh/ac-copilot-trainer/pull/68)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 6
  - Files: 2
  - CI: success

- **PR #67:** [fix(#66): CSP textColored signature reversal + checkbox semantics + em dash (P0 hotfix)](https://github.com/agorokh/ac-copilot-trainer/pull/67)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 39
  - Files: 11
  - CI: success

- **PR #65:** [docs: SAVE vault for issue #57 closure](https://github.com/agorokh/ac-copilot-trainer/pull/65)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 7
  - Files: 2
  - CI: success

- **PR #64:** [Phase 5 Part E: Active suggestion panel (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/64)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 51
  - Files: 5
  - CI: success

- **PR #63:** [Phase 5 Part D: Real-time per-corner coaching engine (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/63)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 59
  - Files: 7
  - CI: success

- **PR #51:** [Add session journal export under ScriptConfig](https://github.com/agorokh/ac-copilot-trainer/pull/51)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 41
  - Files: 7
  - CI: success

- **PR #42:** [feat(#41): coaching HUD, diag gate, BMW font, brake walls](https://github.com/agorokh/ac-copilot-trainer/pull/42)
  - Author: agorokh
  - Merged: 2026-04-03
  - Comments: 21
  - Files: 8
  - CI: success

- **PR #40:** [Rendering pipeline: shaderedQuad, sim clock, diagnostics (#39)](https://github.com/agorokh/ac-copilot-trainer/pull/40)
  - Author: agorokh
  - Merged: 2026-04-03
  - Comments: 51
  - Files: 13
  - CI: success

- **PR #34:** [Visual overhaul: market-quality 3D overlays and HUD (#33)](https://github.com/agorokh/ac-copilot-trainer/pull/34)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 19
  - Files: 5
  - CI: success

- **PR #30:** [debug(#24): enable Draw3D coordinate diagnostics](https://github.com/agorokh/ac-copilot-trainer/pull/30)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 6
  - Files: 1
  - CI: success

- **PR #62:** [Phase 5 Part C: Polished approach telemetry panel (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/62)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 37
  - Files: 6
  - CI: success

- **PR #61:** [Add design conformance test suite (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/61)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 29
  - Files: 2
  - CI: success

- **PR #60:** [Phase 5 Part B: Settings window (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/60)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 26
  - Files: 6
  - CI: success

- **PR #58:** [Phase 5 Part A: ac.storage, corner names, structured approach data (#57)](https://github.com/agorokh/ac-copilot-trainer/pull/58)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 23
  - Files: 4
  - CI: success

- **PR #56:** [Add focus practice mode for corner targets and marker emphasis (#44)](https://github.com/agorokh/ac-copilot-trainer/pull/56)
  - Author: agorokh
  - Merged: 2026-04-05
  - Comments: 32
  - Files: 7
  - CI: success

- **PR #55:** [Add Ollama post-lap debrief and HUD fallback (#46)](https://github.com/agorokh/ac-copilot-trainer/pull/55)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 32
  - Files: 14
  - CI: success

- **PR #54:** [Phase 3c: sidecar lap features + improvement ranking (#49)](https://github.com/agorokh/ac-copilot-trainer/pull/54)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 64
  - Files: 12
  - CI: success

- **PR #53:** [WebSocket sidecar protocol v1 + Lua inbound (fixes #45)](https://github.com/agorokh/ac-copilot-trainer/pull/53)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 26
  - Files: 9
  - CI: success

- **PR #52:** [Coaching UX: configurable hint display cap and WARP docs (#43)](https://github.com/agorokh/ac-copilot-trainer/pull/52)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 16
  - Files: 7
  - CI: success

- **PR #50:** [fix(#9): coaching hold timer, Coaching window idle, strip layout (v0.4.2)](https://github.com/agorokh/ac-copilot-trainer/pull/50)
  - Author: agorokh
  - Merged: 2026-04-04
  - Comments: 28
  - Files: 5
  - CI: success

- **PR #38:** [feat(#37): 3D brake walls, speed diagnostics, coaching overlay fix](https://github.com/agorokh/ac-copilot-trainer/pull/38)
  - Author: agorokh
  - Merged: 2026-04-03
  - Comments: 39
  - Files: 6
  - CI: success

- **PR #36:** [feat(#35): Mavil-quality 3D line, coaching overlay, brake marker upgrade](https://github.com/agorokh/ac-copilot-trainer/pull/36)
  - Author: agorokh
  - Merged: 2026-04-02
  - Comments: 43
  - Files: 6
  - CI: success

- **PR #31:** [fix(#24): filled geometry for brake markers + racing line](https://github.com/agorokh/ac-copilot-trainer/pull/31)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 27
  - Files: 4
  - CI: success

- **PR #29:** [debug(#24): log world coordinates for brake markers + racing line](https://github.com/agorokh/ac-copilot-trainer/pull/29)
  - Author: agorokh
  - Merged: 2026-04-01
  - Comments: 12
  - Files: 1
  - CI: success

- **PR #28:** [fix(#24): line-based brake markers — pcall isolation](https://github.com/agorokh/ac-copilot-trainer/pull/28)
  - Author: agorokh
  - Merged: 2026-03-31
  - Comments: 28
  - Files: 2
  - CI: success

---

## Top Recurring Comment Clusters

These clusters represent patterns of repeated feedback across PRs.

### 1. code / code_block / ensure

- **Count:** 69 comments
- **Severity:** maintainability
- **Preventability:** test
- **Dominant author type:** bot
- **Dominant bot:** coderabbit
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/modules/hud.lua

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟠 Major_

**Keep Draw3D diagnostics opt-in by default.**

With this set to `true`, all sessions emit periodic debug logs, which can quickly flood AC logs outside targeted debug...

2. _🧹 Nitpick_ | _🔵 Trivial_

**Redundant nested pcall structure.**

The outer `pcall` on line 111 wraps an anonymous function that contains additional `pcall` calls for each render primitive. If any inn...

3. _⚠️ Potential issue_ | _🟡 Minor_

**Handle non-finite delta values before rendering numeric/bar output.**

Line 117 handles `nil`, but not `NaN`/non-finite values. This can produce unstable HUD output...

---

### 2. code / with / this

- **Count:** 36 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Affected Files:** docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, tools/ai_sidecar/features.py, src/ac_copilot_trainer/modules/track_markers.lua

**Representative Examples:**

1. ### Marker budget shrinks visible brake points

**Medium Severity**

<!-- DESCRIPTION START -->
With `hasDebugLine`, `primitivesPerMarker` is set to `5` while `MAX_DEBUG_PRIMITIVES` stays `150`, so `m...

2. ### Budget undercounts legacy sphere draws when debugSphere unusable

**Low Severity**

<!-- DESCRIPTION START -->
The budget condition on line 167 uses `(hasLegacyDrawSphere and not hasDebugSphere)` ...

3. > The `float(regret)` call is redundant as `regret` is already a float.

Addressed: Use `round(regret, 4)` for `priority`.

---

### 3. code_block / code / agorokh

- **Count:** 35 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Dominant author type:** bot
- **Dominant bot:** coderabbit
- **Affected Files:** src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, check the file exists and read the relevant lines
wc -l src/ac_copilot_trainer/mo...

2. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify that the module has sticky guards for debug* helpers but not for lega...

3. _⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🌐 Web query:

`In Lua/LuaJIT, are sequence-like tables conventionally 1-based, and what does math.floor(1/2) return?`

...

---

### 4. code / commit / unstaged

- **Count:** 19 comments
- **Severity:** maintainability
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** chatgpt_codex_connector_bot
- **Affected Files:** scripts/check_vault_follow_up.sh

**Representative Examples:**

1. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Detect commit -a changes in vault follow-up guard**

This guard exits when `git diff --cached` is empty, but the...

2. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Detect pathspec/include commits in vault follow-up guard**

`FILES_TO_CHECK` only includes unstaged tracked file...

3. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Parse long commit options before scanning short flags**

In `commit_may_include_unstaged_tracked`, any token sta...

---

### 5. ac_copilot_trainer / this / prompt

- **Count:** 11 comments
- **Severity:** bug
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** qodo_code_review_bot
- **Affected Files:** src/ac_copilot_trainer/modules/session_journal.lua

**Representative Examples:**

1. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Debrief not journaled <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
<b><i>s...

2. <h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...

3. <h3>Code Review by Qodo</h3>

<code>🐞 Bugs (2)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (1)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...

---

### 6. code / reset / stale

- **Count:** 8 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. ### Rolling reset skips clearing `_cachedRealtimeView` state

**Medium Severity**

<!-- DESCRIPTION START -->
Both `resetDrivingState` and `resetRollingDrivingState` clear `state.realtimeActiveHint = ...

2. ### Stale recv queue survives reset, repopulates cleared state

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` clears `cornerAdvisories` and resets `currentSimT` to 0, but does not clear ...

3. ### Reset clears launch flag, preventing sidecar relaunch after crash

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` sets `sidecarChildEverLaunched = false` while leaving `spawnedAlive` ...

---

### 7. code / template / upstream

- **Count:** 7 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** human
- **Affected Files:** .github/workflows/vault-automerge.yml, .github/workflows/post-merge-notify.yml, scripts/hook_protect_main_impl.py

**Representative Examples:**

1. Valid observation, but deferring to an upstream `template-repo` issue rather than changing it in this sync PR. The workflow comes in verbatim from `template-repo@061d9ab` and reverting the unlabeled-t...

2. Same comment as the line-12 thread — the `unlabeled` trigger removal is intentional per the upstream template. Not changing here; will file a follow-up against `agorokh/template-repo` so all child rep...

3. The `ref: merge_commit_sha` removal is from the upstream template and the classifier script (`scripts/post_merge_classify.py`) reads PR diff via `gh api` rather than the local checkout, so it does not...

---

### 8. coaching / code / window

- **Count:** 6 comments
- **Severity:** maintainability
- **Preventability:** guideline
- **Dominant author type:** bot
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**Representative Examples:**

1. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Adds a gated render-diagnostics config, improves coaching HUD visibility (including a new BMW-based font and a main-win...

2. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Refactors coaching timers to use a wall-clock-style countdown, fixes alpha/fade behavior so coaching is visible for the...

3. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Adds a configurable cap for how many post-lap coaching hints are visibly rendered, threads this setting through the HUD...

---

### 9. code / corner / label

- **Count:** 6 comments
- **Severity:** bug
- **Preventability:** architecture
- **Dominant author type:** bot
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. ![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

There is a logic error in the corner label priority. Currently, if `nextBrake` is found (which is almost always true as it wraps aro...

2. _⚠️ Potential issue_ | _🟠 Major_

**In-corner coaching should override the next-corner label.**

`cornerLabel` is resolved from the next brake point first, and Line 235 only fills the current segment ...

3. _⚠️ Potential issue_ | _🟠 Major_

**Keep the queried corner label aligned with the telemetry.**

When `topLabel` comes from `inCornerLabel`, this block still sends `targetSpeed`/`distToBrakeM` from `n...

---

### 10. prompt / this / copy

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** qodo_code_review_bot
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/corner_names.lua

**Representative Examples:**

1. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>coachingholdseconds</i></b> set to 30s <code>📎 Requirement gap</code> <code>...

2. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

2\. Timer freezes when car nil <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
<b...

3. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>coachingmaxvisiblehints</i></b> change untested <code>📘 Rule violation</code...

---

### 11. program / files / review

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** human

**Representative Examples:**

1. C:/Program Files/Git/gemini review

2. C:/Program Files/Git/review

3. C:/Program Files/Git/gemini review

---

### 12. sorry / unable / review

- **Count:** 6 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** sourcery

**Representative Examples:**

1. Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines

2. Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines

3. Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines

---

### 13. ranking / code / computes

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** qodo_code_review_bot
- **Affected Files:** tools/ai_sidecar/session.py

**Representative Examples:**

1. <img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Ranking on new pb <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
LapComparis...

2. LapComparisonState computes and returns an improvement ranking *before* updating the PB when the current lap is faster than the stored best. That means a new PB lap can still return regressions vs the...

3. > Ranking on new pb — computes improvement ranking against the previous PB before updating PB, so a new fastest lap could still get improvementRanking.

Addressed: `improvement_ranking_for` now treats...

---

### 14. code / exported / never

- **Count:** 5 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/render_diag.lua, src/ac_copilot_trainer/modules/persistence.lua

**Representative Examples:**

1. ### Exported diagnostic functions are never called anywhere

**Low Severity**

<!-- DESCRIPTION START -->
`setEnabled`, `isActive`, `getProbeResults`, and `getDrawStats` are exported from `render_diag...

2. ### Exported `decodeJson` function is never used

**Low Severity**

<!-- DESCRIPTION START -->
`persistence.decodeJson` is newly exported but has zero callers anywhere in the codebase. The internal `j...

3. ### Exported function duplicates existing local helper

**Low Severity**

<!-- DESCRIPTION START -->
`M.labelFromWorstRow` in `focus_practice.lua` is logic-identical to the existing `labelFromConsiste...

---

### 15. code / push / function

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** guideline
- **Dominant author type:** bot
- **Affected Files:** src/ac_copilot_trainer/modules/coaching_overlay.lua, tests/test_design_conformance.py

**Representative Examples:**

1. _🧹 Nitpick_ | _🔵 Trivial_

**Style color push/pop mismatch on early returns or exceptions.**

The `ui.pushStyleColor` at line 25 may succeed, but if any code between lines 28-46 throws an exception or...

2. ### Style pop without confirming push succeeded

**Low Severity**

<!-- DESCRIPTION START -->
`ui.popStyleColor` is called whenever the function exists, regardless of whether `pcall(ui.pushStyleColor,...

3. **suggestion (testing):** Font push/pop balancing is checked globally, not per draw function as the requirement states

The docstring requires each draw function to bracket fontMod.push()/fontMod.pop(...

---

### 16. code / json / guard

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** tools/ai_sidecar/protocol.py, tools/ai_sidecar/external_protocol.py, tools/ai_sidecar/coaching/llm_coach.py

**Representative Examples:**

1. ### Unguarded `.get()` on possibly non-dict JSON response crashes connection

**Medium Severity**

<!-- DESCRIPTION START -->
`json.loads(raw)` can return any JSON type (list, string, number, `None`, ...

2. ### Missing bool guard on float() for JSON values

**Medium Severity**

<!-- DESCRIPTION START -->
The `corner_query` handler coerces untrusted JSON values `cur`, `ref`, and `dist` via `float(inbound....

3. ### Protocol field lacks bool guard on int coercion

**Low Severity**

<!-- DESCRIPTION START -->
The `int(proto_raw)` coercion at the protocol validation entry point has no `isinstance(proto_raw, boo...

---

### 17. code / separator / severity

- **Count:** 5 comments
- **Severity:** nit
- **Preventability:** automation
- **Dominant author type:** bot
- **Dominant bot:** bugbot
- **Affected Files:** src/ac_copilot_trainer/modules/coaching_font.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/ws_bridge.lua

**Representative Examples:**

1. ### Path separator lost when root has trailing slash

**Low Severity**

<!-- DESCRIPTION START -->
When `root` ends with a path separator, `sep` is set to `""` to avoid a double separator between `roo...

2. ### `lastEmittedAt` variable is now dead code

**Low Severity**

<!-- DESCRIPTION START -->
`lastEmittedAt` is declared and initialized to `-1e9` but never read or written to anywhere in the module af...

3. ### Reset clears process-alive flag without terminating child

**Low Severity**

<!-- DESCRIPTION START -->
`M.reset()` sets `spawnedAlive = false` and `sidecarChildEverLaunched = false` even though t...

---

### 18. your / warning / have

- **Count:** 5 comments
- **Severity:** nit
- **Preventability:** guideline
- **Dominant author type:** bot
- **Dominant bot:** gemini

**Representative Examples:**

1. > [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!

2. > [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!

3. > [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!

---

### 19. ac_copilot_trainer / coaching / with

- **Count:** 4 comments
- **Severity:** bug
- **Preventability:** automation
- **Dominant author type:** bot
- **Dominant bot:** qodo_code_review_bot

**Representative Examples:**

1. <h3>Review Summary by Qodo</h3>

Gate render diagnostics, add coaching HUD strip, BMW font support, taller brake walls

<code>✨ Enhancement</code> <code>🐞 Bug fix</code>

<img src="https://www.qodo.ai...

2. <h3>Review Summary by Qodo</h3>

Add configurable coaching hints display cap with HUD parity

<code>✨ Enhancement</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.svg" ...

3. <h3>Review Summary by Qodo</h3>

Add Ollama post-lap debrief with HUD display and rules fallback (issue #46)

<code>✨ Enhancement</code> <code>🧪 Tests</code>

<img src="https://www.qodo.ai/wp-content/...

---

### 20. code / ac_copilot_trainer / code_block

- **Count:** 4 comments
- **Severity:** bug
- **Preventability:** automation
- **Dominant author type:** bot
- **Dominant bot:** coderabbit

**Representative Examples:**

1. 

> [!CAUTION]
> Some comments are outside the diff and can’t be posted inline due to platform limitations.
> 
> 
> 
> <details>
> <summary>⚠️ Outside diff range comments (1)</summary><blockquote>
> 
...

2. 

<details>
<summary>♻️ Duplicate comments (3)</summary><blockquote>

<details>
<summary>src/ac_copilot_trainer/modules/session_journal.lua (3)</summary><blockquote>

`24-60`: _⚠️ Potential issue_ | _...

3. 

> [!CAUTION]
> Some comments are outside the diff and can’t be posted inline due to platform limitations.
> 
> 
> 
> <details>
> <summary>⚠️ Outside diff range comments (1)</summary><blockquote>
> 
...

---

## Recurring CI Failures

- **PR #92:** [docs(vault): session SAVE — PR #91 Phase-2 Part A+B in flight](https://github.com/agorokh/ac-copilot-trainer/pull/92)
  - Failed jobs: build, guard-and-automerge, guard-and-automerge

- **PR #90:** [docs(vault): post-merge handoff for PR #89](https://github.com/agorokh/ac-copilot-trainer/pull/90)
  - Failed jobs: guard-and-automerge, build

- **PR #89:** [fix(gitattributes): pin *.sh and *.bash to eol=lf](https://github.com/agorokh/ac-copilot-trainer/pull/89)
  - Failed jobs: score

- **PR #88:** [docs(vault): post-merge handoff for PR #87](https://github.com/agorokh/ac-copilot-trainer/pull/88)
  - Failed jobs: build, guard-and-automerge

- **PR #87:** [chore: sync template to template-repo@061d9ab (template-2026.04)](https://github.com/agorokh/ac-copilot-trainer/pull/87)
  - Failed jobs: score, classify

---

## Most Churned Files

Files with the most review comments:

- `src/ac_copilot_trainer/ac_copilot_trainer.lua`: 132 comments
- `src/ac_copilot_trainer/modules/hud.lua`: 76 comments
- `src/ac_copilot_trainer/modules/ws_bridge.lua`: 59 comments
- `src/ac_copilot_trainer/modules/realtime_coaching.lua`: 57 comments
- `tools/ai_sidecar/server.py`: 49 comments
- `src/ac_copilot_trainer/modules/coaching_overlay.lua`: 40 comments
- `src/ac_copilot_trainer/modules/track_markers.lua`: 40 comments
- `src/ac_copilot_trainer/modules/racing_line.lua`: 38 comments
- `scripts/post_merge_sync.sh`: 37 comments
- `scripts/check_vault_follow_up.sh`: 35 comments

---

## Suggested Next Actions

### Proposed Cursor Rule Updates

**Automation Gates (lint/format/pre-commit):**

- Add check for: code / separator / severity
- Add check for: ac_copilot_trainer / coaching / with
- Add check for: code / ac_copilot_trainer / code_block
- Add check for: code / edge / comment
- Add check for: code / test / that

**Type Checking:**

- Add type check for: code_block / code / agorokh
- Add type check for: code / json / guard
- Add type check for: code / type / annotation
- Add type check for: code / warning / with
- Add type check for: code / call / module

**Repository Guidelines:**

- Document pattern: code / with / this
- Document pattern: code / commit / unstaged
- Document pattern: ac_copilot_trainer / this / prompt
- Document pattern: code / reset / stale
- Document pattern: code / template / upstream

**Architecture/Agent Rules:**

- Add agent rule: code / corner / label
- Add agent rule: corner / real / time
- Add agent rule: test / phase / phases
- Add agent rule: docs / 01_vault / accopilottrainer
- Add agent rule: code / archive / module

### Proposed CI/Pre-commit Changes

- Add automated checks for common formatting/import issues
- Add type checking to CI pipeline
- Add pre-commit hooks for common issues

### Proposed Documentation Updates

- Update AGENTS.md with common patterns
- Add examples to documentation
- Create troubleshooting guide

---

## Appendix: All Comment Clusters

### Cluster 0: code / code_block / ensure

- Count: 69
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/modules/hud.lua, scripts/post_merge_sync.sh, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**Keep Draw3D diagnostics opt-in by default.**

With this set to `true`, all sessions emit periodic debug logs, which can quickly flood AC logs outside targeted debug...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Redundant nested pcall structure.**

The outer `pcall` on line 111 wraps an anonymous function that contains additional `pcall` calls for each render primitive. If any inn...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Handle non-finite delta values before rendering numeric/bar output.**

Line 117 handles `nil`, but not `NaN`/non-finite values. This can produce unstable HUD output...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Treat 80 km/h as mid-speed, not red.**

Line 32 uses `> 80`, so a segment at exactly 80 km/h falls into the red branch even though the issue `#35` thresholds define...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Treat unreadable files as a failed scan.**

Line 105 only adds a warning, so CI can pass without checking part of the tree. For a blocking API-policy script, an `OS...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove unnecessary initial assignments to `body` and `accent`.**

The static analysis correctly identifies that the initial values assigned to `body` (line 159) and `accen...
```

---

### Cluster 1: code / with / this

- Count: 36
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Affected Files: docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, tools/ai_sidecar/features.py, src/ac_copilot_trainer/modules/track_markers.lua, tools/ai_sidecar/server.py, tools/ai_sidecar/session.py

**All Examples:**

```
### Marker budget shrinks visible brake points

**Medium Severity**

<!-- DESCRIPTION START -->
With `hasDebugLine`, `primitivesPerMarker` is set to `5` while `MAX_DEBUG_PRIMITIVES` stays `150`, so `m...
```

```
### Budget undercounts legacy sphere draws when debugSphere unusable

**Low Severity**

<!-- DESCRIPTION START -->
The budget condition on line 167 uses `(hasLegacyDrawSphere and not hasDebugSphere)` ...
```

```
> The `float(regret)` call is redundant as `regret` is already a float.

Addressed: Use `round(regret, 4)` for `priority`.
```

```
> `_run_compare_laps` should catch file/JSON errors and exit with a clear message.

Addressed: Wrapped reads and `json.loads` in `try/except` for `FileNotFoundError`, `PermissionError`, `JSONDecodeErr...
```

```
> Add boolean guard before integer coercion for `lapTimeMs` (`True` → 1 ms PB).

Addressed: `_positive_lap_time_ms` returns `None` when `isinstance(raw, bool)`; added `test_lap_time_true_is_ignored_fo...
```

```
`@agorokh`, confirmed — the `Parent `#9`` line in the current file content now enumerates all remaining milestones explicitly (`#44`, `#46`, `#49`, `#51`, `#19`) with short descriptive labels. That fu...
```

---

### Cluster 2: code_block / code / agorokh

- Count: 35
- Severity: bug
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/modules/track_markers.lua, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, firmware/screen/src/main.cpp, tools/ai_sidecar/features.py

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, check the file exists and read the relevant lines
wc -l src/ac_copilot_trainer/mo...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Verify that the module has sticky guards for debug* helpers but not for lega...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🌐 Web query:

`In Lua/LuaJIT, are sequence-like tables conventionally 1-based, and what does math.floor(1/2) return?`

...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
wc -l src/ac_copilot_trainer/modules/track_markers.lua
```

Repository: agorokh/ac-copilot...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Check if brake point records include a heading field in the codebase
echo "=...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
set -euo pipefail

echo "=== buildAfterLap call sites ==="
rg -n -C4 --glob '*...
```

---

### Cluster 3: code / commit / unstaged

- Count: 19
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: scripts/check_vault_follow_up.sh

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Detect commit -a changes in vault follow-up guard**

This guard exits when `git diff --cached` is empty, but the...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Detect pathspec/include commits in vault follow-up guard**

`FILES_TO_CHECK` only includes unstaged tracked file...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Parse long commit options before scanning short flags**

In `commit_may_include_unstaged_tracked`, any token sta...
```

```
### Long options parsed as short flags cause false blocks

**Medium Severity**

<!-- DESCRIPTION START -->
The embedded Python parser in `commit_may_include_unstaged_tracked` does not distinguish `--l...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Ignore unstaged vault docs when satisfying follow-up requirement**

`ACKED` is derived from `FILES_TO_CHECK`, wh...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Recognize interactive commit modes in vault guard**

`commit_may_include_unstaged_tracked` misses interactive co...
```

---

### Cluster 4: ac_copilot_trainer / this / prompt

- Count: 11
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: src/ac_copilot_trainer/modules/session_journal.lua

**All Examples:**

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Debrief not journaled <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
<b><i>s...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (2)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (1)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (1)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

---

### Cluster 5: code / reset / stale

- Count: 8
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### Rolling reset skips clearing `_cachedRealtimeView` state

**Medium Severity**

<!-- DESCRIPTION START -->
Both `resetDrivingState` and `resetRollingDrivingState` clear `state.realtimeActiveHint = ...
```

```
### Stale recv queue survives reset, repopulates cleared state

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` clears `cornerAdvisories` and resets `currentSimT` to 0, but does not clear ...
```

```
### Reset clears launch flag, preventing sidecar relaunch after crash

**Medium Severity**

<!-- DESCRIPTION START -->
`M.reset()` sets `sidecarChildEverLaunched = false` while leaving `spawnedAlive` ...
```

```
### Nil return from spawn API leaves sidecar permanently broken

**Medium Severity**

<!-- DESCRIPTION START -->
The return value check `if a == false then` only catches an explicit `false` from `os.r...
```

```
### Stale sidecar coaching survives rolling lap-counter reset

**Medium Severity**

<!-- DESCRIPTION START -->
When `state.lapsCompleted` resets (e.g., car reset or teleport), `resetRollingDrivingStat...
```

```
### Stale summary cache after rolling driving state reset

**Medium Severity**

<!-- DESCRIPTION START -->
`resetRollingDrivingState` clears `focusPracticeHudSummary` to `""` but does not reset `focus...
```

---

### Cluster 6: code / template / upstream

- Count: 7
- Severity: nit
- Preventability: guideline
- Dominant author type: human
- Affected Files: .github/workflows/vault-automerge.yml, .github/workflows/post-merge-notify.yml, scripts/hook_protect_main_impl.py, scripts/post_merge_sync.sh, tools/pr_pain/pain_score.py

**All Examples:**

```
Valid observation, but deferring to an upstream `template-repo` issue rather than changing it in this sync PR. The workflow comes in verbatim from `template-repo@061d9ab` and reverting the unlabeled-t...
```

```
Same comment as the line-12 thread — the `unlabeled` trigger removal is intentional per the upstream template. Not changing here; will file a follow-up against `agorokh/template-repo` so all child rep...
```

```
The `ref: merge_commit_sha` removal is from the upstream template and the classifier script (`scripts/post_merge_classify.py`) reads PR diff via `gh api` rather than the local checkout, so it does not...
```

```
Valid robustness suggestion, but the file is unchanged from upstream `template-repo@061d9ab` in this sync PR. Deferring to an upstream issue so the fix lands once and propagates to every child repo ra...
```

```
Reasonable suggestion on stash label uniqueness, but this file is unchanged from upstream `template-repo@061d9ab`. Deferring to a follow-up issue against the template so the fix is authoritative and p...
```

```
Accepted — REST caps at 250 commits per PR and the GraphQL switch would genuinely improve `fix_commits` / `commits_after_first_review` accuracy for large PRs. Same rationale as the other review thread...
```

---

### Cluster 7: coaching / code / window

- Count: 6
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Adds a gated render-diagnostics config, improves coaching HUD visibility (including a new BMW-based font and a main-win...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Refactors coaching timers to use a wall-clock-style countdown, fixes alpha/fade behavior so coaching is visible for the...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Adds a configurable cap for how many post-lap coaching hints are visibly rendered, threads this setting through the HUD...
```

```
The coaching window is documented as “transparent background, top-right”, but `script.windowCoaching()`/`coachingOverlay.draw()` don’t set any window background alpha/flags or position. If the intent ...
```

```
The `coaching_overlay.lua` header/doc says this is a “transparent” overlay, but the module itself only draws text/separators and doesn’t apply any window background/style changes. If transparency is r...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🌐 Web query:

`In Assetto Corsa Custom Shaders Patch Lua apps, how do I make a secondary app window transparent/no-back...
```

---

### Cluster 8: code / corner / label

- Count: 6
- Severity: bug
- Preventability: architecture
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

There is a logic error in the corner label priority. Currently, if `nextBrake` is found (which is almost always true as it wraps aro...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**In-corner coaching should override the next-corner label.**

`cornerLabel` is resolved from the next brake point first, and Line 235 only fills the current segment ...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Keep the queried corner label aligned with the telemetry.**

When `topLabel` comes from `inCornerLabel`, this block still sends `targetSpeed`/`distToBrakeM` from `n...
```

```
### Top tile mixes current corner label with next corner's brake data

**Medium Severity**

<!-- DESCRIPTION START -->
When the car is in a corner's apex region, `topCornerLabel` is set to the current...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Send corner query with matching corner telemetry**

When `view.cornerLabel` is overridden to the in-corner label...
```

```
`corner_query` is keyed off `topLabel` (which becomes the *in-corner* label when `inCornerLabel` is set), but the values sent (`distToBrakeM`/`targetSpeed` via `refAtBrake`) are always computed from `...
```

---

### Cluster 9: prompt / this / copy

- Count: 6
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/corner_names.lua

**All Examples:**

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>coachingholdseconds</i></b> set to 30s <code>📎 Requirement gap</code> <code>...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

2\. Timer freezes when car nil <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
<b...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>coachingmaxvisiblehints</i></b> change untested <code>📘 Rule violation</code...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>fmforfilter</i></b> duplicates focus logic <code>📘 Rule violation</code> <co...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. <b><i>resolveapproachlabel</i></b> linear scan <code>📎 Requirement gap</code> <cod...
```

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

2\. Defaults table can mutate <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
loa...
```

---

### Cluster 10: program / files / review

- Count: 6
- Severity: nit
- Preventability: guideline
- Dominant author type: human
- Affected Files: N/A

**All Examples:**

```
C:/Program Files/Git/gemini review
```

```
C:/Program Files/Git/review
```

```
C:/Program Files/Git/gemini review
```

```
C:/Program Files/Git/review
```

```
C:/Program Files/Git/gemini review
```

```
C:/Program Files/Git/review
```

---

### Cluster 11: sorry / unable / review

- Count: 6
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: N/A

**All Examples:**

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

```
Sorry, we are unable to review this pull request

The GitHub API does not allow us to fetch diffs exceeding 20000 lines
```

---

### Cluster 12: ranking / code / computes

- Count: 5
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: tools/ai_sidecar/session.py

**All Examples:**

```
<img src="https://www.qodo.ai/wp-content/uploads/2026/01/action-required.png" height="20" alt="Action required">

1\. Ranking on new pb <code>🐞 Bug</code> <code>≡ Correctness</code>

<pre>
LapComparis...
```

```
LapComparisonState computes and returns an improvement ranking *before* updating the PB when the current lap is faster than the stored best. That means a new PB lap can still return regressions vs the...
```

```
> Ranking on new pb — computes improvement ranking against the previous PB before updating PB, so a new fastest lap could still get improvementRanking.

Addressed: `improvement_ranking_for` now treats...
```

```
> LapComparisonState computes ranking before updating PB when the current lap is faster than the stored best.

Addressed: Same as Qodo thread — new PB path updates reference first and returns an empty...
```

```
<h3>Code Review by Qodo</h3>

<code>🐞 Bugs (0)</code>  <code>📘 Rule violations (0)</code>  <code>📎 Requirement gaps (0)</code>  <code>🎨 UX Issues (0)</code>

<img src="https://www.qodo.ai/wp-content/u...
```

---

### Cluster 13: code / exported / never

- Count: 5
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/render_diag.lua, src/ac_copilot_trainer/modules/persistence.lua, src/ac_copilot_trainer/modules/focus_practice.lua

**All Examples:**

```
### Exported diagnostic functions are never called anywhere

**Low Severity**

<!-- DESCRIPTION START -->
`setEnabled`, `isActive`, `getProbeResults`, and `getDrawStats` are exported from `render_diag...
```

```
### Exported `decodeJson` function is never used

**Low Severity**

<!-- DESCRIPTION START -->
`persistence.decodeJson` is newly exported but has zero callers anywhere in the codebase. The internal `j...
```

```
### Exported function duplicates existing local helper

**Low Severity**

<!-- DESCRIPTION START -->
`M.labelFromWorstRow` in `focus_practice.lua` is logic-identical to the existing `labelFromConsiste...
```

```
### Exported `M.activeHint()` function is never called

**Low Severity**

<!-- DESCRIPTION START -->
`M.activeHint()` is exported but never called anywhere in the codebase. The active hint is already ...
```

```
### Exported `M.phase()` function is never called

**Low Severity**

<!-- DESCRIPTION START -->
`M.phase()` is exported from `realtime_coaching.lua` but never called anywhere in the runtime codebase. ...
```

---

### Cluster 14: code / push / function

- Count: 5
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/coaching_overlay.lua, tests/test_design_conformance.py

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**Style color push/pop mismatch on early returns or exceptions.**

The `ui.pushStyleColor` at line 25 may succeed, but if any code between lines 28-46 throws an exception or...
```

```
### Style pop without confirming push succeeded

**Low Severity**

<!-- DESCRIPTION START -->
`ui.popStyleColor` is called whenever the function exists, regardless of whether `pcall(ui.pushStyleColor,...
```

```
**suggestion (testing):** Font push/pop balancing is checked globally, not per draw function as the requirement states

The docstring requires each draw function to bracket fontMod.push()/fontMod.pop(...
```

```
The docstring says this verifies “Every draw function … brackets font push/pop”, but the implementation only checks total `fontMod.push()`/`fontMod.pop()` counts across the whole file. This can produc...
```

```
Hey - I've found 2 issues, and left some high level feedback:

- A number of the tests assert on exact code snippets (e.g. string literals like `'#out >= 3'`, `"function M.drawMainWindowStrip"`, `"bmw...
```

---

### Cluster 15: code / json / guard

- Count: 5
- Severity: bug
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: tools/ai_sidecar/protocol.py, tools/ai_sidecar/external_protocol.py, tools/ai_sidecar/coaching/llm_coach.py

**All Examples:**

```
### Unguarded `.get()` on possibly non-dict JSON response crashes connection

**Medium Severity**

<!-- DESCRIPTION START -->
`json.loads(raw)` can return any JSON type (list, string, number, `None`, ...
```

```
### Missing bool guard on float() for JSON values

**Medium Severity**

<!-- DESCRIPTION START -->
The `corner_query` handler coerces untrusted JSON values `cur`, `ref`, and `dist` via `float(inbound....
```

```
### Protocol field lacks bool guard on int coercion

**Low Severity**

<!-- DESCRIPTION START -->
The `int(proto_raw)` coercion at the protocol validation entry point has no `isinstance(proto_raw, boo...
```

```
### `is_external_frame` missing bool guard on version check

**Medium Severity**

<!-- DESCRIPTION START -->
`is_external_frame` compares `frame.get("v") == 1` without a `bool` guard. Since Python `Tr...
```

```
### Version check missing bool guard in validate_inbound

**Low Severity**

<!-- DESCRIPTION START -->
`is_external_frame` correctly guards against `bool` with `not isinstance(v, bool)` before compari...
```

---

### Cluster 16: code / separator / severity

- Count: 5
- Severity: nit
- Preventability: automation
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/coaching_font.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/modules/lap_archive.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### Path separator lost when root has trailing slash

**Low Severity**

<!-- DESCRIPTION START -->
When `root` ends with a path separator, `sep` is set to `""` to avoid a double separator between `roo...
```

```
### `lastEmittedAt` variable is now dead code

**Low Severity**

<!-- DESCRIPTION START -->
`lastEmittedAt` is declared and initialized to `-1e9` but never read or written to anywhere in the module af...
```

```
### Reset clears process-alive flag without terminating child

**Low Severity**

<!-- DESCRIPTION START -->
`M.reset()` sets `spawnedAlive = false` and `sidecarChildEverLaunched = false` even though t...
```

```
### Stats count includes entries that rotate would skip

**Low Severity**

<!-- DESCRIPTION START -->
`stats()` sets `count = #list` without filtering by `type(list[i]) == "string"`, but `rotate()` gu...
```

```
### Config set of wsSidecarUrl silently drops ack

**Low Severity**

<!-- DESCRIPTION START -->
When an external client sends `config.set` with key `wsSidecarUrl`, `applyExternalConfigSet` calls `wsBr...
```

---

### Cluster 17: your / warning / have

- Count: 5
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: gemini
- Affected Files: N/A

**All Examples:**

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

```
> [!WARNING]
> You have reached your daily quota limit. Please wait up to 24 hours and I will start processing your requests again!
```

---

### Cluster 18: ac_copilot_trainer / coaching / with

- Count: 4
- Severity: bug
- Preventability: automation
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: N/A

**All Examples:**

```
<h3>Review Summary by Qodo</h3>

Gate render diagnostics, add coaching HUD strip, BMW font support, taller brake walls

<code>✨ Enhancement</code> <code>🐞 Bug fix</code>

<img src="https://www.qodo.ai...
```

```
<h3>Review Summary by Qodo</h3>

Add configurable coaching hints display cap with HUD parity

<code>✨ Enhancement</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.svg" ...
```

```
<h3>Review Summary by Qodo</h3>

Add Ollama post-lap debrief with HUD display and rules fallback (issue #46)

<code>✨ Enhancement</code> <code>🧪 Tests</code>

<img src="https://www.qodo.ai/wp-content/...
```

```
<h3>Review Summary by Qodo</h3>

Sidecar auto-launch and per-lap archive with car setup capture

<code>✨ Enhancement</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.sv...
```

---

### Cluster 19: code / ac_copilot_trainer / code_block

- Count: 4
- Severity: bug
- Preventability: automation
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: N/A

**All Examples:**

```


> [!CAUTION]
> Some comments are outside the diff and can’t be posted inline due to platform limitations.
> 
> 
> 
> <details>
> <summary>⚠️ Outside diff range comments (1)</summary><blockquote>
> 
...
```

```


<details>
<summary>♻️ Duplicate comments (3)</summary><blockquote>

<details>
<summary>src/ac_copilot_trainer/modules/session_journal.lua (3)</summary><blockquote>

`24-60`: _⚠️ Potential issue_ | _...
```

```


> [!CAUTION]
> Some comments are outside the diff and can’t be posted inline due to platform limitations.
> 
> 
> 
> <details>
> <summary>⚠️ Outside diff range comments (1)</summary><blockquote>
> 
...
```

```


<details>
<summary>♻️ Duplicate comments (1)</summary><blockquote>

<details>
<summary>src/ac_copilot_trainer/modules/lap_archive.lua (1)</summary><blockquote>

`360-371`: _⚠️ Potential issue_ | _🟡 ...
```

---

### Cluster 20: code / type / annotation

- Count: 4
- Severity: nit
- Preventability: typecheck
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/coaching_font.lua, src/ac_copilot_trainer/modules/ws_bridge.lua, tests/test_phase5_rebuild_ete.py

**All Examples:**

```
LuaDoc type annotation for `speedColor()` says `---@return table`, but it returns an `rgbm(...)` color object. Updating the return type improves editor/tooling accuracy and matches how the value is us...
```

```
**nitpick:** The return type annotation does not match the documented kind values.

The `---@return string|nil kind: "dwrite" | "builtin"` annotation mixes a general type with a specific union. Since ...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Update `pendingCoaching` type annotation to include `debrief`.**

Line 155 now stores `debrief`, but the declared shape at Line 16 still documents only `{ lap, hints }`. T...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Consider adding return type annotations to private fixtures.**

Static analysis flags missing return type annotations on `_build_trace`, `_build_brake_points`, and `_build...
```

---

### Cluster 21: code / around / corner

- Count: 4
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
`cornerLabelForBrake()` uses `math.abs(s.brakeSpline - brakeSeg.s0)` to match a brake segment to its corner. This breaks for wrap-around near spline 0/1 (e.g., brakeSpline=0.001, s0=0.989 → abs≈0.988)...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The spline distance calculation does not account for the track wrap-around at the start/finish line. If a brake segment starts a...
```

```
### Wrap-around spline distance breaks label lookup near start/finish

**Medium Severity**

<!-- DESCRIPTION START -->
`cornerLabelForBrake` computes spline distance as `math.abs(s.brakeSpline - brake...
```

```
`cornerLabelForBrake()` uses `math.abs(s.brakeSpline - brakeSeg.s0)` which breaks for wrap-around positions near 0/1 (e.g., brakeSpline=0.005 vs brakeSeg.s0=0.993). This can mis-assign corner labels a...
```

---

### Cluster 22: branch / code / local

- Count: 4
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Affected Files: scripts/post_merge_sync.sh

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Fail sync when local main is ahead of origin/main**

In `phase_sync`, `git pull --ff-only origin main` is treate...
```

```
This deletes any existing local branch named `vault/post-merge-pr<PR>` and then force-pushes it. That’s potentially destructive if someone has local work on that branch name (or if a prior run created...
```

```
`git push -u --force-with-lease origin "$VAULT_BRANCH"` can overwrite an existing remote branch if this script is rerun and the remote branch already exists (especially if the local repo hasn’t fetche...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Stop force-deleting unrelated local branches during sync**

This loop force-deletes every local branch whose ups...
```

---

### Cluster 23: code / code_block / remove

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove unused color constants.**

`COLOR_RED_LIGHT` and `PANEL_PAD_X` are defined but never used, as flagged by static analysis. Remove them to reduce noise.

<details>
<s...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove unused dedupe variables left over from the removed time-based suppression.**

Static analysis correctly flags `DEDUP_HOLD_SEC` (line 30) as unused and `lastEmittedA...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Remove dead `hasReference` variable.**

`hasReference` is computed at line 137 but never used for any actual logic—the only "use" is the dummy assignment `local _ = hasRef...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Remove unused `lastCornerQueryAt` variable.**

The variable is declared but never accessed (confirmed by static analysis). The comment states it's "kept for backwar...
```

---

### Cluster 24: code / code_block / session

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟡 Minor_

**Reset the coaching diag budget on session teardown.**

`_coachDiagT` and `_coachDiagCount` live on `state`, but `resetRuntimeAfterLeavingTrack()` never clears them....
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Consider reusing `focusLabelMap()` to reduce duplication.**

Lines 1000-1006 duplicate the logic in `focusLabelMap()` (lines 333-341). Since `focusPracticeActive` is alrea...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Reset the active-suggestion state on session resets.**

`fadeAlpha`, `fadeTarget`, and `lastHintText` live at module scope, so they survive track exits. Because `sc...
```

```
_⚠️ Potential issue_ | _🔴 Critical_

**Restore the `hud.reset()` compatibility hook.**

`src/ac_copilot_trainer/ac_copilot_trainer.lua`, Lines 586 and 604 still call `hud.reset()`. With no `M.reset` e...
```

---

### Cluster 25: code / frame / dead

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
### Unused variable `coachingDiagLogged` is dead code

**Low Severity**

<!-- DESCRIPTION START -->
`coachingDiagLogged` is declared as `false` but never read or written to anywhere in the codebase. T...
```

```
### Dead store: `state.realtimeCoachingPhase` written but never read

**Low Severity**

<!-- DESCRIPTION START -->
`state.realtimeCoachingPhase` is assigned every frame (both in the `if` and `else` br...
```

```
### Several HUD fields computed every frame but never rendered

**Low Severity**

<!-- DESCRIPTION START -->
The diff removes all rendering of `vm.sectorMessage`, `vm.coastWarn`, `vm.tireLockupFlash`,...
```

```
### Orphaned `_cachedApproachData` computed every frame but never read

**Low Severity**

<!-- DESCRIPTION START -->
`state._cachedApproachData` is still computed via `approachHudData()` every frame o...
```

---

### Cluster 26: code / loopback / token

- Count: 4
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: tools/ai_sidecar/server.py, tests/test_ai_sidecar_external.py

**All Examples:**

```
`--token` help text says it's "Required whenever --external-bind is non-loopback", but the implementation exits whenever `--external-bind` is set and `--token` is missing (even if `--external-bind` is...
```

```
Current CLI still allows `--host 0.0.0.0` (or other non-loopback) without `--token`, which would expose an unauthenticated socket despite the new external-client auth model. Consider refusing non-loop...
```

```
Token-gate tests currently only cover “no header → 401” and “matching header → accept”. If loopback clients are supposed to connect without the token even when `--token` is set (per issue #81 / PR des...
```

```
`--external-bind` currently always requires `--token`, even if the bind address is loopback. This contradicts the `--token` help text (“Required whenever --external-bind is non-loopback”) and makes it...
```

---

### Cluster 27: code / with / newer

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Prefer newer Ollama debriefs over stale queued follow-ups**

This guard only accepts a debrief-only `ollama` fol...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Reject advisories from prior laps**

`takeCornerAdvisory()` only checks advisory age, not advisory lap, so cache...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Avoid archiving debrief text for the wrong lap**

The archive record is written immediately after `lap_complete`...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Use pre-increment lap counter for advice snapshot**

`corner_advice_used` is captured after `state.lapsCompleted...
```

---

### Cluster 28: code / segment / segments

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
The tilt continuity state (`prevTiltH`) is carried across skipped segments (e.g., segments outside the cull radius). If rendering resumes at a non-adjacent segment (common on tracks with close/overlap...
```

```
`findNextBrakeOrCorner()` linearly scans all segments every frame while on straights (`for i = 1, #segments do ...`). That makes the “approaching” detection O(n) per tick and undermines the stated O(1...
```

```
`cornerLabelForBrake()` performs a full scan of `segments` on every call and matches corner labels using `splineDist(s.brakeSpline, brakeSeg.s0) < 0.03`. Two issues: (1) this is O(n) per frame in brak...
```

```
### Approach hints never fire due to missing segment index

**High Severity**

<!-- DESCRIPTION START -->
During the approach phase, `cornerLabelForBrake(nextSeg, nil)` is called with `nil` for the se...
```

---

### Cluster 29: code / warning / with

- Count: 4
- Severity: maintainability
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: tools/ai_sidecar/server.py

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**Fan-out trust model: any authenticated peer can spoof server→client frames to other peers.**

`_handle_external_frame` delegates validation to `validate_inbound`, w...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Peer must send `hello` before any frame is fanned out to it.**

`_external_peers.add(websocket)` only runs in the `TYPE_HELLO` branch, so a peer that connects and i...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Reject client commands when no Lua peer is connected**

After a client sends `hello`, request frames like `confi...
```

```


<details>
<summary>♻️ Duplicate comments (2)</summary><blockquote>

<details>
<summary>tools/ai_sidecar/server.py (1)</summary><blockquote>

`521-531`: _🧹 Nitpick_ | _🔵 Trivial_

**Redundant token-g...
```

---

### Cluster 30: corner / real / time

- Count: 4
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/coaching_hints.lua

**All Examples:**

```
`realtimeCoaching.tick()` is fed `lastLapCornerFeats` (features from the previous completed lap) rather than any current-lap telemetry, so “real-time” hints will reflect last lap’s entry/min speeds in...
```

```
`buildRealTime()` only compares **last-lap** corner features vs **best-lap** features; it doesn’t use any current-lap telemetry (e.g., current speed on approach). That means the “real-time” hint won’t...
```

```
The realtime coaching tick currently generates hints solely from `state.lastLapCornerFeats` vs `state.bestCornerFeatures`. That means hints during lap N are based on lap N-1 performance and won’t reac...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements a new real-time per-corner coaching engine as a Lua state machine with an O(1) bucket-based track segment in...
```

---

### Cluster 31: persistent / review / updated

- Count: 4
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: N/A

**All Examples:**

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/439817a753b4ea5700a...
```

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/ce20cc4fb1afc32956e...
```

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/92eff75e47176357a67...
```

```
**[Persistent review](https://github.com/agorokh/ac-copilot-trainer/pull/80#issuecomment-4292037109)** updated to latest commit https://github.com/agorokh/ac-copilot-trainer/commit/7b7c1f59016c5156ccb...
```

---

### Cluster 32: test / phase / phases

- Count: 4
- Severity: maintainability
- Preventability: architecture
- Dominant author type: bot
- Affected Files: tests/test_design_conformance.py

**All Examples:**

```
**issue (testing):** PD-02 description says "6 phases" but tests and implementation only cover 5, which makes the test misleading.

The docstring mentions “all 6 phase string literals,” but the test a...
```

```
Test name/docstring says “six phases” (PD-02), but the assertion loop checks only 5 phase literals (and the state machine comment lists 5 states including straight). Please align the wording with what...
```

```
PD-02 comment says the state machine has "6 phase string literals", but the test only checks for 5 phases (straight/approaching/braking/corner/exiting). Update the docstring to match the actual assert...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Test method name contradicts docstring: "six_phases" vs "5 phase".**

The method is named `test_realtime_coaching_six_phases` but the docstring says "5 phase string...
```

---

### Cluster 33: docs / 01_vault / accopilottrainer

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: qodo_code_review_bot
- Affected Files: N/A

**All Examples:**

```
<h3>Review Summary by Qodo</h3>

Vault enrichment: EPIC #59 hardware roadmap, PR #75–#84 coverage, debugging journey, and Figma design source

<code>📝 Documentation</code>

<img src="https://www.qodo....
```

```
<h3>Review Summary by Qodo</h3>

Post-merge handoff updates for template-sync PR #87

<code>📝 Documentation</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-grey-line.svg" height...
```

```
<h3>Review Summary by Qodo</h3>

Document PR #91 Phase-2 Part A+B progress and ci-conventional pitfall

<code>📝 Documentation</code>

<img src="https://www.qodo.ai/wp-content/uploads/2025/11/light-gre...
```

---

### Cluster 34: code / ac_copilot_trainer / severity

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/lap_archive.lua

**All Examples:**

```
### Fade-out clears hint content one frame before render stops

**Low Severity**

<!-- DESCRIPTION START -->
The `elseif fadeAlpha < 0.01` branch clears `lastHintText`, `lastHintKind`, and `lastCorner...
```

```
### Archive `is_pb` always true for current best

**Low Severity**

<!-- DESCRIPTION START -->
The `is_pb` field is evaluated after `state.bestLapMs` is already updated at line 1548–1549. For PB laps,...
```

```
### Stats mishandles negative file sizes unlike rotate

**Low Severity**

<!-- DESCRIPTION START -->
`stats()` adds raw `io.fileSize` results to the total via `tonumber(s) or 0`, which means negative ...
```

---

### Cluster 35: text / alpha / only

- Count: 3
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: tests/test_design_conformance.py, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
**issue (testing):** Docstring mentions title text alpha, but the test currently only verifies background alpha

The assertions only check the background rgbm alpha, so the title text alpha requiremen...
```

```
Design brief/PR description says text should be fully opaque (background only is transparent), but COLOR_BRAND is defined with alpha 0.70 and used for footer text. Consider making footer text alpha 1....
```

```
The module header/doc says “text 100% opaque”, but `textAlpha` is set to `fadeAlpha` and then applied to all text colors. If the design requirement is that only the background is transparent (text alw...
```

---

### Cluster 36: code / archive / module

- Count: 3
- Severity: bug
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/lap_archive.lua

**All Examples:**

```
### Nonexistent `copilotStateDir` breaks all archive operations

**High Severity**

<!-- DESCRIPTION START -->
`lapArchiveDir()` calls `persistence.copilotStateDir()`, but that function does not exist...
```

```
### Archive uses pretty-print JSON, inflating file sizes

**Medium Severity**

<!-- DESCRIPTION START -->
`M.write()` calls `persistence.encodeJson(rec)` which uses `JSON.stringify(t, true)` — the `tr...
```

```
### Duplicated car/track ID extraction from persistence module

**Low Severity**

<!-- DESCRIPTION START -->
`carIdFromOptsCar` and `trackIdFromOptsSim` duplicate nearly identical functions `tryCarFro...
```

---

### Cluster 37: audit / security / subprocess

- Count: 3
- Severity: security
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: scripts/_build_pr80_ledger.py

**All Examples:**

```
**security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'run' without a static string. If this data can be controlled by a malicious actor, it may be an ...
```

```
**security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'run' without a static string. If this data can be controlled by a malicious actor, it may be an ...
```

```
**security (python.lang.security.audit.dangerous-subprocess-use-audit):** Detected subprocess function 'check_output' without a static string. If this data can be controlled by a malicious actor, it m...
```

---

### Cluster 38: code / brake / dedupe

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

The dedupe key is too coarse and can suppress critical coaching updates. Since the key only consists of `kind` and `cornerLabel`, a ...
```

```
### Dedupe suppresses urgent BRAKE NOW after PREPARE hint

**Medium Severity**

<!-- DESCRIPTION START -->
The dedupe key is `kind:cornerLabel`, but both "PREPARE TO BRAKE" and "BRAKE NOW" share `kind...
```

```
The 600 ms dedupe key is only `(kind, cornerLabel)`, so different messages of the same kind for the same corner (e.g., `PREPARE TO BRAKE` → `BRAKE NOW`) will be suppressed for up to `DEDUP_HOLD_SEC`, ...
```

---

### Cluster 39: code / call / module

- Count: 3
- Severity: nit
- Preventability: typecheck
- Dominant author type: bot
- Dominant bot: gemini
- Affected Files: tools/ai_sidecar/features.py, tools/ai_sidecar/improvement_ranking.py

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `aliases` dictionary is static and should be defined as a module-level constant to avoid re-allocation on every call to `_no...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `float(regret)` call is redundant as `regret` is already a float.

```suggestion
                "priority": round(regret, 4...
```

```
> The `aliases` dictionary is static and should be defined as a module-level constant to avoid re-allocation on every call to `_normalize_metric_key`. Additionally, since `name` is type-hinted as a st...
```

---

### Cluster 40: code / socket / callback

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
### Cdata socket never closed on reconfigure

**Medium Severity**

<!-- DESCRIPTION START -->
In `M.configure`, the guard `type(prev) == "table"` prevents calling `prev:close()` on CSP web.socket obje...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Ignore stale socket close callbacks**

`_onClose` unconditionally assigns `sock = nil`, but this callback is sha...
```

```
### Callback-queued messages lost when socket is nil

**Medium Severity**

<!-- DESCRIPTION START -->
`pollInbound` early-returns when `sock` is nil, but the rewrite changed message receipt to a callb...
```

---

### Cluster 41: code / staged / changes

- Count: 3
- Severity: maintainability
- Preventability: guideline
- Dominant author type: bot
- Affected Files: scripts/post_merge_sync.sh, scripts/check_vault_follow_up.sh

**All Examples:**

```
`phase_vault` only checks for out-of-scope changes using `git diff --name-only HEAD` (working tree), but it does not check the index (`--cached`). If there are staged non-vault changes, `git checkout ...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

This check only inspects staged changes (`--cached`). It can be bypassed if a user runs `git commit -a` (or `-am`), which commit...
```

```
### Vault out-of-scope check misses staged-only index changes

**Low Severity**

<!-- DESCRIPTION START -->
The vault-change detection on line 71 properly inspects both `git diff` (index→worktree) and...
```

---

### Cluster 42: code / socket / close

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close active websocket inside ws_bridge.reset**

`M.reset()` drops the `sock` reference without closing it, but ...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close failed WebSocket before clearing bridge handle**

When `reconnect = true`, this module intentionally keeps...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close callable websocket handles during bridge teardown**

Fresh evidence this still applies after the socket-cl...
```

---

### Cluster 43: code / says / code_block

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md

**All Examples:**

```
_⚠️ Potential issue_ | _🟡 Minor_

**Fix completion-state contradiction in the resume bullet.**

Line 18 says “All 5 Parts delivered,” but the same section says Part E is still in PR `#64` and still ne...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Update the handoff metadata date.**

This status block was edited for PR `#64`, but Line 5 still says `last_updated: 2026-04-05`. Please bump it to `2026-04-06` so ...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Use pre-merge wording for PR `#83`.**

This says PR `#83` “landed”, but the same handoff later says “Once PR `#83` merges” and the PR is still open. Use branch/PR w...
```

---

### Cluster 44: code / code_block / still

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/modules/session_journal.lua, tools/ai_sidecar/server.py

**All Examples:**

```
_⚠️ Potential issue_ | _🟡 Minor_

**Short coaching holds still start faded.**

When `coachingHoldSeconds < 5`, `timeRemaining` never reaches `fadeStart`, so the panel starts below full opacity again (...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**`car.id` and `track.id` can degrade to `"unknown"` at export time.**

`session_key` already uses `persistence.sessionKey(car, sim)` with fallbacks, but `car`/`track...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Only spawn the Ollama follow-up after a real coaching response.**

This branch keys off the inbound event, so a `lap_complete` that `prepare_outbound_message()` con...
```

---

### Cluster 45: code / edge / comment

- Count: 3
- Severity: bug
- Preventability: automation
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟠 Major_

**Tilt is applied to the wrong edge.**

`traceToLine()` preserves driving order, so `a -> b` points forward along the lap. Adding `tiltH` to `v3/v4` therefore raises ...
```

```
The comment says the “trailing edge (a, behind car) rises”, but the code applies the newly computed tilt to the `b` edge (`v3/v4` use `backTiltH` from `calcTiltHeight(sA, sB, len)`). Please either adj...
```

```
Tilt naming/commentary doesn’t match the geometry being built: the comment says the trailing edge at point `a` rises under braking, but `calcTiltHeight(sA, sB, len)` is assigned to `backTiltH` and app...
```

---

### Cluster 46: code / commit / staged

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: scripts/check_vault_follow_up.sh, scripts/claude_pretool_vault_guard.sh

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Account for `git add && git commit` in vault guard**

The guard snapshots staged files before the commit command...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Exempt `--dry-run` from vault follow-up commit enforcement**

Per `git commit -h`, `--[no-]dry-run` only “show[s...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Restrict commit-intent regex to actual git commit commands**

The `_git_commit_intent` pattern is still broad en...
```

---

### Cluster 47: code / corner / hint

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
### Dedup causes hint to vanish at phase transitions

**High Severity**

<!-- DESCRIPTION START -->
When the car transitions from "approaching" to "braking" (or "braking" to "corner"), `selectHint` is...
```

```
### Stale hint persists when transitioning between different corners

**Medium Severity**

<!-- DESCRIPTION START -->
The `or activeHint` fallback in `selectHint(label, ...) or activeHint` preserves a...
```

```
### Approach detection preempts the "exiting" phase transition

**Low Severity**

<!-- DESCRIPTION START -->
The approach-detection block (lines 256–272) runs *before* the segment-kind state-machine t...
```

---

### Cluster 48: code / dead / severity

- Count: 3
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: tools/ai_sidecar/external_protocol.py, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### `make_action_ack` and `make_config_ack` are unused

**Low Severity**

<!-- DESCRIPTION START -->
`make_action_ack` and `make_config_ack` are defined as envelope helpers but never imported or calle...
```

```
### Removed call leaves `wsUrlMatchesAutolaunchTarget` as dead code

**Low Severity**

<!-- DESCRIPTION START -->
The `wsUrlMatchesAutolaunchTarget` function (defined at line 95) is now unused — its o...
```

```
### Exported `is_external_frame` function is never used

**Low Severity**

<!-- DESCRIPTION START -->
`is_external_frame` is defined and exported but never called anywhere in the codebase. The server ...
```

---

### Cluster 49: code / tokens / design

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
`COLOR_BG` and `COLOR_BG_BORDER` are defined as design tokens but the panel background/border uses hard-coded `rgbm(...)` values instead, leaving these constants unused. Either use the constants or re...
```

```
### Unused design token constants `COLOR_BG` and `COLOR_BG_BORDER`

**Low Severity**

<!-- DESCRIPTION START -->
`COLOR_BG` and `COLOR_BG_BORDER` are declared as design tokens but never referenced. Th...
```

```
Design tokens `COLOR_BG` / `COLOR_BG_BORDER` are defined but the panel background/border uses duplicated inline `rgbm(...)` literals instead. This makes it easier for tokens to drift from actual rende...
```

---

### Cluster 50: code / this / duplicated

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
### Duplicated render state restore logic across two modules

**Low Severity**

<!-- DESCRIPTION START -->
`restoreRenderState` in `racing_line.lua` and `restoreMarkerRenderState` in `track_markers.lu...
```

```
### Duplicated `setV3` helper across two modules

**Low Severity**

<!-- DESCRIPTION START -->
The `setV3` function is introduced with identical implementation in both `racing_line.lua` and `track_mar...
```

```
### Duplicated rgbm setter not deduped like setV3

**Low Severity**

<!-- DESCRIPTION START -->
`setRgbm` in `racing_line.lua` and `setWallColor` in `track_markers.lua` are near-identical functions bo...
```

---

### Cluster 51: code / exit / error

- Count: 3
- Severity: bug
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, .claude/agents/post-merge-steward.md, scripts/post_merge_sync.sh

**All Examples:**

```
### Unknown exit code resets nonzero streak counter

**Low Severity**

<!-- DESCRIPTION START -->
In the `runConsoleProcess` exit callback, when `result` is not a table or lacks `exitCode`, `codeNum` ...
```

```
The documented exit code meanings don’t match the script: `scripts/post_merge_sync.sh` uses exit code `10` for failures like `git checkout main` / `git pull --ff-only` and a failed `git commit`, not o...
```

```
### Missing error handler on `git add` in vault phase

**Low Severity**

<!-- DESCRIPTION START -->
`git add docs/01_Vault/` on line 145 lacks `|| { fail "..."; exit N; }` error handling, unlike every...
```

---

### Cluster 52: code / frame / every

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua, src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
### Speed diagnostic logs every frame in "both" mode

**Medium Severity**

<!-- DESCRIPTION START -->
The module-level `speedDiagLineId` stores a single line table identity, but `drawLineStrip` is cal...
```

```
### Font stack leak: `pop()` called without required kind argument

**High Severity**

<!-- DESCRIPTION START -->
All four `fontMod.pushNamed()` calls in `drawActiveSuggestion` discard the return valu...
```

```
### tryOpen called every frame during spawn backoff period

**Medium Severity**

<!-- DESCRIPTION START -->
During the 120-second spawn backoff, `tryOpen()` is called every frame because `lastLaunchAt...
```

---

### Cluster 53: code / external / malformed

- Count: 3
- Severity: bug
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: tools/ai_sidecar/server.py

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Reject non-v1 external envelopes explicitly**

`_handler` only calls `_handle_external_frame` when `is_external_...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Route malformed v-envelopes into external validation**

The handler only enters external-envelope error handling...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Route malformed external payloads to v1 error handling**

External frame routing is only attempted after JSON-ro...
```

---

### Cluster 54: code / passed / field

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### approachData passed to HUD but never consumed

**Low Severity**

<!-- DESCRIPTION START -->
The `approachData` field is still declared in `HudViewModel` and passed from the entry script on line 92...
```

```
### `drawSidecarDebrief` is now dead code with no callers

**Low Severity**

<!-- DESCRIPTION START -->
The `drawSidecarDebrief` function in `coaching_overlay.lua` is now completely unused. Its only c...
```

```
### Unused `setWsSidecarUrl` passed in Settings viewmodel

**Low Severity**

<!-- DESCRIPTION START -->
`setWsSidecarUrl = setWsSidecarUrlAndReconfigure` is still passed in the Settings viewmodel, but...
```

---

### Cluster 55: font / code / same

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_font.lua

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The current regex `[%w_%-]+` used to extract the font filename does not support spaces, which are common in Assetto Corsa font f...
```

```
Same font-stack issue here: `pushNamed()` must be paired with `pop(kind)` using the returned token; `pop()` without args won’t undo the font push.
```suggestion
  local titleFont = fontMod.pushNamed("...
```

```
Same font-stack issue here: capture the return value of `pushNamed("brand", ...)` and pass it into `fontMod.pop(kind)`; otherwise the font stack will grow each draw.
```suggestion
    local brandFontK...
```

---

### Cluster 56: code / idle / footer

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Render telemetry footer during idle state**

`M.draw()` calls `drawTelemetryFooter(vm)` in both active and idle ...
```

```
`drawTelemetryFooter()` returns early when `fadeAlpha < 0.01`, but `M.draw()` calls it in the idle path as well. In a fresh/idle session `fadeAlpha` will be 0, so the telemetry footer (speed/REC/delta...
```

```
`drawTelemetryFooter()` returns early when `fadeAlpha < 0.01`, but in the idle path `drawActiveSuggestion()` is never called so `fadeAlpha` stays 0. As a result, the telemetry footer won’t render in t...
```

---

### Cluster 57: code / from / values

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
`tokens` is imported from `coachingOverlay.tokens` but never used; instead this file duplicates the token values in local constants. That undermines the “single source of truth” intent and risks visua...
```

```
`coachingOverlay.tokens` is assigned to `local tokens` but never used, while the file redefines “shadow” token constants that can drift from the canonical values (they already do: `PANEL_PAD_Y` is 18 ...
```

```
Despite importing `coachingOverlay.tokens` as a “single source of truth”, `hud.lua` still defines its own panel constants (e.g., `PANEL_PAD_Y = 18`) that already diverge from `coaching_overlay.lua`’s ...
```

---

### Cluster 58: phase / lvgl / code

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: firmware/screen/include/lv_conf.h, firmware/screen/README.md

**All Examples:**

```
Phase-1 acceptance says "LVGL status screen renders" and references `secrets/wifi.h`, but the Phase-1 firmware (`src/main.cpp`) uses Arduino_GFX (no LVGL) and includes `wifi_secrets.h`. Please update ...
```

```
`lv_conf.h` is described as an LVGL v9 config for the Phase-1 status screen, but the Phase-1 firmware in this PR uses Arduino_GFX directly and the PlatformIO config explicitly avoids LVGL. If LVGL is ...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**`lv_conf.h` is committed but LVGL is not wired in Phase-1.**

`main.cpp` explicitly comments "LVGL intentionally absent in Phase 1", `platformio.ini` does not list LVGL in...
```

---

### Cluster 59: code / merge / that

- Count: 3
- Severity: maintainability
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: scripts/post_merge_sync.sh

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Re-check PR merge state after invoking `gh pr merge`**

`phase_sync` assumes `gh pr merge` means the PR is alrea...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Re-check PR merge state before treating sync as successful**

`gh pr merge` is not a guarantee that the PR is me...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Validate PR base branch before running auto-merge**

Phase `sync` now merges any OPEN PR number via `gh pr merge...
```

---

### Cluster 60: code / vault / merge

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: .github/workflows/vault-automerge.yml, .github/workflows/post-merge-notify.yml

**All Examples:**

```
### Vault auto-merge workflow lost branch filter for main

**Medium Severity**

<!-- DESCRIPTION START -->
The `branches: [main]` filter was removed from the `pull_request` trigger in `vault-automerge...
```

```
### Post-merge notify fails for merged fork PRs

**Low Severity**

<!-- DESCRIPTION START -->
The fork guard (`github.event.pull_request.head.repo.fork == false`) was removed from the `classify` job c...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Introduce a deterministic post-merge steward flow that standardizes post-merge sync, classification, and vault-only han...
```

---

### Cluster 61: code / speed / missing

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: src/ac_copilot_trainer/modules/racing_line.lua

**All Examples:**

```
Per-segment coloring treats `segSpeed == 0` as “missing speed data” and falls back to `fallbackColor`. But 0 km/h is a valid speed and should map to the red/yellow range per `speedColor()`. Consider c...
```

```
`traceToLine()` now preserves `speed=nil` (line 71) to distinguish “no data” from 0 km/h, but the downsampling branch in the same function still coerces missing speed to `0` (via `or 0`). This makes b...
```

```
Per-segment speed coloring treats a missing speed value as `0` when only one of `a.speed`/`b.speed` is available (`(a.speed or 0) + (b.speed or 0)`), which will skew the average toward red even if the...
```

---

### Cluster 62: code / table / only

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: bugbot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
### Entire application state table exposed to HUD module

**Low Severity**

<!-- DESCRIPTION START -->
`focusPracticeUi = state` passes the entire mutable application `state` table (containing `bestLa...
```

```
### Archive write not pcall-wrapped, risking script termination

**Medium Severity**

<!-- DESCRIPTION START -->
`lapArchive.write` is called without `pcall` in the lap boundary handler. Inside `write...
```

```
### Handler closures reference `state` before table assignment

**Medium Severity**

<!-- DESCRIPTION START -->
The `toggleFocusPractice` handler at line 389 reads and writes `state.focusPracticeActiv...
```

---

### Cluster 63: code / real / ssid

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: copilot
- Affected Files: firmware/screen/secrets/wifi.h.example

**All Examples:**

```
### Real SSID leaked in committed example file

**Low Severity**

<!-- DESCRIPTION START -->
`wifi.h.example` contains `WIFI_SSID "AHOME5G"` which appears to be a real home network SSID, unlike the co...
```

```
`wifi.h.example` contains what looks like a real SSID (`AHOME5G`). Even though it’s an example, it’s safer to keep templates strictly placeholder-only (e.g., `"your-ssid-here"`) to avoid accidentally ...
```

```
This example secrets template includes what looks like a real SSID (`AHOME5G`). Even in an `.example` file, avoid committing real network identifiers—use a generic placeholder (e.g., `"your-ssid-here"...
```

---

### Cluster 64: code / sidecar / socket

- Count: 3
- Severity: maintainability
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Preserve externally managed sidecar sockets**

This block closes any existing WebSocket whenever `spawnedAlive` ...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Keep child-launch provenance across bridge resets**

`startSidecarIfNeeded()` only clears stale `sock` handles w...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Keep manual sidecar sockets after a previous auto-launch**

Fresh evidence for the earlier socket-ownership conc...
```

---

### Cluster 65: code / sidecar / with

- Count: 3
- Severity: nit
- Preventability: guideline
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/start_sidecar.bat

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Preserve non-empty sidecar URL when enabling auto-launch**

Setting `wsSidecarUrl` to localhost here does not ac...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Resolve sidecar repo root from deployed app layout**

The launcher computes `REPO_ROOT` as two directories above...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Migrate legacy sidecar URLs to localhost for auto-launch**

With this change, the sidecar URL is no longer edita...
```

---

### Cluster 66: code / socket / with

- Count: 3
- Severity: nit
- Preventability: architecture
- Dominant author type: bot
- Dominant bot: chatgpt_codex_connector_bot
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close prior sidecar socket before reconfiguring URL**

The settings path now calls `wsBridge.configure(url)` whe...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Re-send hello after loopback socket reconnects**

This `hello` is only sent during the initial `tryOpen` call, b...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Emit Lua hello without relying on params.onOpen**

Fresh evidence: the repo’s CSP socket API notes (`docs/01_Vau...
```

---

### Cluster 67: code / thanks / test

- Count: 3
- Severity: maintainability
- Preventability: test
- Dominant author type: bot
- Dominant bot: coderabbit
- Affected Files: tools/ai_sidecar/features.py, tools/ai_sidecar/session.py

**All Examples:**

```
`@agorokh`, thanks for the fix and the dedicated test — adding `math.isfinite(f)` guard and covering it with `test_as_float_rejects_non_finite` is exactly the right approach. 

<!-- <review_comment_ad...
```

```
`@agorokh`, thanks for the confirmation! The boolean guard and the accompanying test `test_lap_time_true_is_ignored_for_pb` are exactly the right approach here — rejecting `True`/`False` before the `i...
```

```
`@agorokh`, thanks for confirming! The boolean guard and the accompanying test `test_extract_corner_table_skips_bool_corner_id` look like exactly the right fix. 

<!-- <review_comment_addressed> -->

...
```

---

### Cluster 68: code / test / that

- Count: 3
- Severity: maintainability
- Preventability: automation
- Dominant author type: bot
- Dominant bot: sourcery
- Affected Files: tests/test_phase5_rebuild_ete.py

**All Examples:**

```
**suggestion (testing):** There’s no test for the HUD’s behavior when `realtimeView` is nil, even though always-visible tiles are part of the contract.

ETE-01 currently always passes a non-nil `realt...
```

```
**suggestion (testing):** autoPlaceOnce is only validated via grepping source; behavior (one-time move and persisted flag) is not exercised via Lua.

Given the existing `ac.storage`, `ac.getAppWindows...
```

```
Hey - I've found 3 issues, and left some high level feedback:

- The new dedupe logic in realtime_coaching.tick keys only on kind + cornerLabel, so informational transitions within the same corner (e....
```

---

## Hook suggestions (not applied)

The following are **suggested** `settings.json` hook snippets for `preventability: automation` patterns.
Review and merge manually if appropriate.

### code / separator / severity
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / separator / severity (5 occurrences)
```

### ac_copilot_trainer / coaching / with
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: ac_copilot_trainer / coaching / with (4 occurrences)
```

### code / ac_copilot_trainer / code_block
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / ac_copilot_trainer / code_block (4 occurrences)
```
