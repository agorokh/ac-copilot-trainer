# Process Improvement Miner Report

**Repository:** agorokh/ac-copilot-trainer
**Analysis period:** last 7 day(s) (since 2026-04-13 UTC, through 2026-04-20 UTC)
**Generated:** 2026-04-20T09:45:25.978083+00:00

---

## Executive Summary

- **PRs Analyzed:** 2
- **Total Comments:** 120
- **Average Comments per PR:** 60.0
- **Total Files Changed:** 20
- **Total Additions:** 1,644
- **Total Deletions:** 257
- **CI Failures:** 0
- **Comment Clusters Found:** 12

---

## Analyzed PRs

- **PR #76:** [chore(deps-dev): bump the python group with 14 updates](https://github.com/agorokh/ac-copilot-trainer/pull/76)
  - Author: dependabot[bot]
  - Merged: 2026-04-19
  - Comments: 0
  - Files: 1
  - CI: success

- **PR #75:** [fix(#75): in-game smoke test — CSP runtime fixes + Ollama coaching pipeline](https://github.com/agorokh/ac-copilot-trainer/pull/75)
  - Author: agorokh
  - Merged: 2026-04-14
  - Comments: 120
  - Files: 19
  - CI: success

---

## Top Recurring Comment Clusters

These clusters represent patterns of repeated feedback across PRs.

### 1. code / media / prefers

- **Count:** 24 comments
- **Severity:** maintainability
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/ws_bridge.lua, tools/ai_sidecar/server.py, src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. ### Missing bool guard on float() for JSON values

**Medium Severity**

<!-- DESCRIPTION START -->
The `corner_query` handler coerces untrusted JSON values `cur`, `ref`, and `dist` via `float(inbound....

2. ### New function uses old broken cdata guard pattern

**Low Severity**

<!-- DESCRIPTION START -->
The newly added `drawLapDebrief` function uses `type(ui.drawRect) == "function"` to guard the border ...

3. ### Corner advice sent even without hint text

**Low Severity**

<!-- DESCRIPTION START -->
When `compose_corner_hint` returns `None` (Ollama disabled, failed, or produced no valid output), a `corner_...

---

### 2. codex / review / pull

- **Count:** 21 comments
- **Severity:** nit
- **Preventability:** guideline

**Representative Examples:**

1. 
### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `ef45989314`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...

2. 
### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `e100ba8919`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...

3. 
### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `eb0eb48032`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...

---

### 3. details / summary / code

- **Count:** 16 comments
- **Severity:** maintainability
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. _🧹 Nitpick_ | _🔵 Trivial_

**Consider extracting manifest sizes to a shared constant.**

The hardcoded window sizes must stay synchronized with `manifest.ini` per the comment. Consider defining these ...

2. _🧹 Nitpick_ | _🔵 Trivial_

**Diagnostic logging may be verbose for normal use.**

The 3-second `[RT-DIAG]` logging is helpful for debugging issue `#75` but will generate continuous log output during n...

3. _⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cat -n src/ac_copilot_trainer/modules/coaching_font.lua
```

Repository: agorokh/ac-copilo...

---

### 4. code / with / corner

- **Count:** 11 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/ws_bridge.lua, tools/ai_sidecar/server.py

**Representative Examples:**

1. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Clear expired corner advisories from shared state**

`wsBridge.takeCornerAdvisory()` can return `nil` when an ad...

2. `state.cornerAdvisories` is only updated when `wsBridge.takeCornerAdvisory()` returns a non-nil string, but entries are never removed when the bridge expires them. That means an expired hint can remai...

3. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Gate background follow-up on successful coaching response**

The handler schedules `_send_ollama_followup()` for...

---

### 5. code / corner / with

- **Count:** 7 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua, tools/ai_sidecar/server.py, src/ac_copilot_trainer/modules/ws_bridge.lua

**Representative Examples:**

1. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Re-send corner queries when revisiting the same turn**

This query gate only sends when `prev == nil` (first tim...

2. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Debounce corner queries only after successful send**

This block records `lastQueryState[topLabel]` before attem...

3. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Gate corner queries to approach distance**

The `corner_query` sender is currently enabled whenever `distToBrake...

---

### 6. code / summary / details

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** typecheck

**Representative Examples:**

1. <h3>Review Summary by Qodo</h3>

Fix issue #75: in-game smoke test — CSP runtime fixes + real-time per-corner LLM coaching

<code>✨ Enhancement</code> <code>🐞 Bug fix</code>

<img src="https://www.qod...

2. ## Pull request overview

This PR hardens the in-game CSP Lua runtime for the Phase 5 HUD rebuild and extends the Python AI sidecar + Lua WebSocket bridge to support non-blocking Ollama debrief follow...

3. ## Pull request overview

Copilot reviewed 18 out of 18 changed files in this pull request and generated 4 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...

---

### 7. code / details / summary

- **Count:** 5 comments
- **Severity:** bug
- **Preventability:** typecheck

**Representative Examples:**

1. **Actionable comments posted: 9**

> [!CAUTION]
> Some comments are outside the diff and can’t be posted inline due to platform limitations.
> 
> 
> 
> <details>
> <summary>⚠️ Outside diff range comme...

2. **Actionable comments posted: 4**

<details>
<summary>♻️ Duplicate comments (2)</summary><blockquote>

<details>
<summary>src/ac_copilot_trainer/modules/ws_bridge.lua (1)</summary><blockquote>

`25-27...

3. **Actionable comments posted: 2**

<details>
<summary>🤖 Prompt for all review comments with AI agents</summary>

```
Verify each finding against the current code and only fix it if needed.

Inline com...

---

### 8. code / follow / with

- **Count:** 4 comments
- **Severity:** nit
- **Preventability:** architecture
- **Affected Files:** src/ac_copilot_trainer/modules/ws_bridge.lua, tools/ai_sidecar/server.py

**Representative Examples:**

1. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Ignore stale Ollama follow-up hints after first consume**

When an `ollama` follow-up arrives after the initial ...

2. In the Ollama follow-up path, if a `coaching_response` arrives with `debriefSource == "ollama"` after the initial response has already been consumed (so `pendingCoaching` is nil), this branch creates ...

3. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Avoid overwriting pending coaching with older Ollama follow-ups**

In `pollInbound`, the `elseif debrief` branch...

---

### 9. code / when / corner

- **Count:** 4 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua, tools/ai_sidecar/protocol.py, src/ac_copilot_trainer/modules/hud.lua

**Representative Examples:**

1. When an LLM corner advisory is present, this code forces `view.kind = "info"`. Since `hud.draw()` uses `view.kind` to color the *primary* line, this can unintentionally turn urgent states like "BRAKE ...

2. `corner_query` is keyed off `topLabel` (which becomes the *in-corner* label when `inCornerLabel` is set), but the values sent (`distToBrakeM`/`targetSpeed` via `refAtBrake`) are always computed from `...

3. Protocol validation currently only enforces `protocol` when the field is present. That means `corner_query` frames without a `protocol` field will be accepted and processed, even though the module doc...

---

### 10. code / socket / close

- **Count:** 4 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/ws_bridge.lua

**Representative Examples:**

1. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close active websocket inside ws_bridge.reset**

`M.reset()` drops the `sock` reference without closing it, but ...

2. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close failed WebSocket before clearing bridge handle**

When `reconnect = true`, this module intentionally keeps...

3. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Ignore stale socket close callbacks**

`_onClose` unconditionally assigns `sock = nil`, but this callback is sha...

---

### 11. code / when / missing

- **Count:** 3 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Affected Files:** tools/ai_sidecar/protocol.py, src/ac_copilot_trainer/modules/ws_bridge.lua

**Representative Examples:**

1. `_corner_scalar()` returns 0.0 when the field is missing (`raw is None`), so a `corner_query` without `cur/ref/dist` can silently pass validation and call Ollama with zeros. That contradicts the inten...

2. `sendCornerQuery()` always returns `true` after calling `M.sendJson(...)`, even if the underlying send fails (e.g., `M.sendJson` catches an error and clears `sock`). Since `realtime_coaching.tick()` u...

3. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Require protocol field for corner_query frames**

`prepare_outbound_message()` currently accepts `corner_query` ...

---

### 12. code / lock / ollama

- **Count:** 3 comments
- **Severity:** reliability
- **Preventability:** architecture
- **Affected Files:** tools/ai_sidecar/server.py

**Representative Examples:**

1. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Do not hold prepare lock during corner_query Ollama calls**

`_corner_job` takes `_prepare_lock` before running ...

2. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Scope prepare lock per websocket connection**

`_prepare_lock` is declared at module scope and then awaited insi...

3. `corner_query` handling holds `prepare_lock` across the entire `asyncio.to_thread(prepare_outbound_message, ...)` call. Since `prepare_outbound_message` blocks on Ollama for `corner_query`, this can d...

---

## Most Churned Files

Files with the most review comments:

- `src/ac_copilot_trainer/modules/ws_bridge.lua`: 23 comments
- `tools/ai_sidecar/server.py`: 17 comments
- `src/ac_copilot_trainer/modules/realtime_coaching.lua`: 14 comments
- `tools/ai_sidecar/protocol.py`: 9 comments
- `src/ac_copilot_trainer/ac_copilot_trainer.lua`: 8 comments
- `src/ac_copilot_trainer/modules/coaching_overlay.lua`: 4 comments
- `src/ac_copilot_trainer/modules/hud.lua`: 4 comments
- `tests/test_ai_sidecar_protocol.py`: 2 comments
- `src/ac_copilot_trainer/modules/coaching_font.lua`: 1 comments
- `tools/ai_sidecar/coaching/llm_coach.py`: 1 comments

---

## Suggested Next Actions

### Proposed Cursor Rule Updates

**Type Checking:**

- Add type check for: code / summary / details
- Add type check for: code / details / summary
- Add type check for: code / when / missing

**Repository Guidelines:**

- Document pattern: code / media / prefers
- Document pattern: codex / review / pull
- Document pattern: details / summary / code
- Document pattern: code / with / corner
- Document pattern: code / corner / with

**Architecture/Agent Rules:**

- Add agent rule: code / follow / with
- Add agent rule: code / lock / ollama

### Proposed CI/Pre-commit Changes

- Add automated checks for common formatting/import issues
- Add type checking to CI pipeline
- Add pre-commit hooks for common issues

### Proposed Documentation Updates

- Update AGENTS.md with common patterns
- Add examples to documentation
- Create troubleshooting guide

---

## Appendix: All Comment Clusters

### Cluster 0: code / media / prefers

- Count: 24
- Severity: maintainability
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, tools/ai_sidecar/server.py, src/ac_copilot_trainer/modules/realtime_coaching.lua, tools/ai_sidecar/protocol.py, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
### Missing bool guard on float() for JSON values

**Medium Severity**

<!-- DESCRIPTION START -->
The `corner_query` handler coerces untrusted JSON values `cur`, `ref`, and `dist` via `float(inbound....
```

```
### New function uses old broken cdata guard pattern

**Low Severity**

<!-- DESCRIPTION START -->
The newly added `drawLapDebrief` function uses `type(ui.drawRect) == "function"` to guard the border ...
```

```
### Corner advice sent even without hint text

**Low Severity**

<!-- DESCRIPTION START -->
When `compose_corner_hint` returns `None` (Ollama disabled, failed, or produced no valid output), a `corner_...
```

```
### Background task may be garbage collected silently

**Medium Severity**

<!-- DESCRIPTION START -->
`asyncio.create_task()` is called without saving a reference to the returned task. The event loop...
```

```
### Stale corner advisories never cleared from state

**High Severity**

<!-- DESCRIPTION START -->
The 6-second staleness expiry for corner advisories is defeated by a one-way copy. `wsBridge.takeCor...
```

```
### Fallback clock `monoClock` is never incremented

**Low Severity**

<!-- DESCRIPTION START -->
New debounce code uses `opts.simT or monoClock` as the time reference, but `monoClock` is initialized ...
```

---

### Cluster 9: codex / review / pull

- Count: 21
- Severity: nit
- Preventability: guideline
- Affected Files: N/A

**All Examples:**

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `ef45989314`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `e100ba8919`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `eb0eb48032`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `6a7827de99`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `8305c35fc4`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `c3db3c538c`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

---

### Cluster 5: details / summary / code

- Count: 16
- Severity: maintainability
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, tests/test_ai_sidecar_protocol.py, tools/ai_sidecar/server.py

**All Examples:**

```
_🧹 Nitpick_ | _🔵 Trivial_

**Consider extracting manifest sizes to a shared constant.**

The hardcoded window sizes must stay synchronized with `manifest.ini` per the comment. Consider defining these ...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Diagnostic logging may be verbose for normal use.**

The 3-second `[RT-DIAG]` logging is helpful for debugging issue `#75` but will generate continuous log output during n...
```

```
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cat -n src/ac_copilot_trainer/modules/coaching_font.lua
```

Repository: agorokh/ac-copilo...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Wrap `ui.windowSize()` calls with `pcall` on the real layout path.**

The diagnostic block at Line 131 correctly uses `pcall(function() return ui.windowSize() end)`...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Remove unused `lastCornerQueryAt` variable.**

The variable is declared but never accessed (confirmed by static analysis). The comment states it's "kept for backwar...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**The callable fallback chain has subtle FFI cdata handling.**

The dual attempt to call `sock(js)` (first via `type(sock) == "function"` check, then via pcall for non-funct...
```

---

### Cluster 1: code / with / corner

- Count: 11
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/modules/ws_bridge.lua, tools/ai_sidecar/server.py, src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Clear expired corner advisories from shared state**

`wsBridge.takeCornerAdvisory()` can return `nil` when an ad...
```

```
`state.cornerAdvisories` is only updated when `wsBridge.takeCornerAdvisory()` returns a non-nil string, but entries are never removed when the bridge expires them. That means an expired hint can remai...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Gate background follow-up on successful coaching response**

The handler schedules `_send_ollama_followup()` for...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Send corner query with matching corner telemetry**

When `view.cornerLabel` is overridden to the in-corner label...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Handle sim time rewind when expiring corner advisories**

`takeCornerAdvisory()` only expires entries when `age ...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close prior sidecar socket before reconfiguring URL**

The settings path now calls `wsBridge.configure(url)` whe...
```

---

### Cluster 6: code / corner / with

- Count: 7
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, tools/ai_sidecar/server.py, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Re-send corner queries when revisiting the same turn**

This query gate only sends when `prev == nil` (first tim...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Debounce corner queries only after successful send**

This block records `lastQueryState[topLabel]` before attem...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Gate corner queries to approach distance**

The `corner_query` sender is currently enabled whenever `distToBrake...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Coalesce queued corner_query tasks per connection**

Each `corner_query` unconditionally spawns a new background...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Deduplicate corner jobs before starting thread work**

`pending_corner_task.cancel()` does not stop an already r...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Skip corner queries when target entry speed is unknown**

This sends `refAtBrake` as `0` whenever `targetSpeed` ...
```

---

### Cluster 10: code / summary / details

- Count: 5
- Severity: bug
- Preventability: typecheck
- Affected Files: N/A

**All Examples:**

```
<h3>Review Summary by Qodo</h3>

Fix issue #75: in-game smoke test — CSP runtime fixes + real-time per-corner LLM coaching

<code>✨ Enhancement</code> <code>🐞 Bug fix</code>

<img src="https://www.qod...
```

```
## Pull request overview

This PR hardens the in-game CSP Lua runtime for the Phase 5 HUD rebuild and extends the Python AI sidecar + Lua WebSocket bridge to support non-blocking Ollama debrief follow...
```

```
## Pull request overview

Copilot reviewed 18 out of 18 changed files in this pull request and generated 4 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...
```

```
## Pull request overview

Copilot reviewed 18 out of 18 changed files in this pull request and generated 3 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...
```

```
## Pull request overview

Copilot reviewed 18 out of 18 changed files in this pull request and generated 3 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...
```

---

### Cluster 11: code / details / summary

- Count: 5
- Severity: bug
- Preventability: typecheck
- Affected Files: N/A

**All Examples:**

```
**Actionable comments posted: 9**

> [!CAUTION]
> Some comments are outside the diff and can’t be posted inline due to platform limitations.
> 
> 
> 
> <details>
> <summary>⚠️ Outside diff range comme...
```

```
**Actionable comments posted: 4**

<details>
<summary>♻️ Duplicate comments (2)</summary><blockquote>

<details>
<summary>src/ac_copilot_trainer/modules/ws_bridge.lua (1)</summary><blockquote>

`25-27...
```

```
**Actionable comments posted: 2**

<details>
<summary>🤖 Prompt for all review comments with AI agents</summary>

```
Verify each finding against the current code and only fix it if needed.

Inline com...
```

```
**Actionable comments posted: 2**

> [!CAUTION]
> Some comments are outside the diff and can’t be posted inline due to platform limitations.
> 
> 
> 
> <details>
> <summary>⚠️ Outside diff range comme...
```

```
**Actionable comments posted: 1**

<details>
<summary>🤖 Prompt for all review comments with AI agents</summary>

```
Verify each finding against the current code and only fix it if needed.

Inline com...
```

---

### Cluster 2: code / follow / with

- Count: 4
- Severity: nit
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua, tools/ai_sidecar/server.py

**All Examples:**

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Ignore stale Ollama follow-up hints after first consume**

When an `ollama` follow-up arrives after the initial ...
```

```
In the Ollama follow-up path, if a `coaching_response` arrives with `debriefSource == "ollama"` after the initial response has already been consumed (so `pendingCoaching` is nil), this branch creates ...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Avoid overwriting pending coaching with older Ollama follow-ups**

In `pollInbound`, the `elseif debrief` branch...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Cancel orphaned Ollama follow-up tasks**

This schedules `_send_ollama_followup()` without tying its lifetime to...
```

---

### Cluster 3: code / when / corner

- Count: 4
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, tools/ai_sidecar/protocol.py, src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
When an LLM corner advisory is present, this code forces `view.kind = "info"`. Since `hud.draw()` uses `view.kind` to color the *primary* line, this can unintentionally turn urgent states like "BRAKE ...
```

```
`corner_query` is keyed off `topLabel` (which becomes the *in-corner* label when `inCornerLabel` is set), but the values sent (`distToBrakeM`/`targetSpeed` via `refAtBrake`) are always computed from `...
```

```
Protocol validation currently only enforces `protocol` when the field is present. That means `corner_query` frames without a `protocol` field will be accepted and processed, even though the module doc...
```

```
Debrief truncation can append an ellipsis twice: first when `raw` is trimmed to 140 chars (`.. "..."`), and again when rendering the 3rd wrapped line (`ln = ln .. "..."`). This can result in duplicate...
```

---

### Cluster 8: code / socket / close

- Count: 4
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close active websocket inside ws_bridge.reset**

`M.reset()` drops the `sock` reference without closing it, but ...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Close failed WebSocket before clearing bridge handle**

When `reconnect = true`, this module intentionally keeps...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Ignore stale socket close callbacks**

`_onClose` unconditionally assigns `sock = nil`, but this callback is sha...
```

```
close_socket_if_any() only calls :close() when type(s)=="table". CSP web.socket returns a polymorphic callable (often a function/cdata) that still supports :close(); with the current guard, configure(...
```

---

### Cluster 4: code / when / missing

- Count: 3
- Severity: bug
- Preventability: typecheck
- Affected Files: tools/ai_sidecar/protocol.py, src/ac_copilot_trainer/modules/ws_bridge.lua

**All Examples:**

```
`_corner_scalar()` returns 0.0 when the field is missing (`raw is None`), so a `corner_query` without `cur/ref/dist` can silently pass validation and call Ollama with zeros. That contradicts the inten...
```

```
`sendCornerQuery()` always returns `true` after calling `M.sendJson(...)`, even if the underlying send fails (e.g., `M.sendJson` catches an error and clears `sock`). Since `realtime_coaching.tick()` u...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Require protocol field for corner_query frames**

`prepare_outbound_message()` currently accepts `corner_query` ...
```

---

### Cluster 7: code / lock / ollama

- Count: 3
- Severity: reliability
- Preventability: architecture
- Affected Files: tools/ai_sidecar/server.py

**All Examples:**

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Do not hold prepare lock during corner_query Ollama calls**

`_corner_job` takes `_prepare_lock` before running ...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Scope prepare lock per websocket connection**

`_prepare_lock` is declared at module scope and then awaited insi...
```

```
`corner_query` handling holds `prepare_lock` across the entire `asyncio.to_thread(prepare_outbound_message, ...)` call. Since `prepare_outbound_message` blocks on Ollama for `corner_query`, this can d...
```

---
