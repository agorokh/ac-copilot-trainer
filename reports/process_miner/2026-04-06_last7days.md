# Process Improvement Miner Report

**Repository:** agorokh/ac-copilot-trainer
**Analysis period:** last 7 day(s) (since 2026-04-06 UTC, through 2026-04-13 UTC)
**Generated:** 2026-04-13T09:47:01.681434+00:00

---

## Executive Summary

- **PRs Analyzed:** 4
- **Total Comments:** 130
- **Average Comments per PR:** 32.5
- **Total Files Changed:** 26
- **Total Additions:** 2,684
- **Total Deletions:** 2,294
- **CI Failures:** 0
- **Comment Clusters Found:** 18

---

## Analyzed PRs

- **PR #74:** [docs(vault): SAVE — PR #73 merged, refresh handoff + current focus](https://github.com/agorokh/ac-copilot-trainer/pull/74)
  - Author: agorokh
  - Merged: 2026-04-12
  - Comments: 9
  - Files: 2
  - CI: success

- **PR #73:** [fix(#72): Phase 5 HUD rebuild — live-frame engine + bundled fonts + FIXED_SIZE windows + always-visible tiles](https://github.com/agorokh/ac-copilot-trainer/pull/73)
  - Author: agorokh
  - Merged: 2026-04-07
  - Comments: 67
  - Files: 16
  - CI: success

- **PR #70:** [fix(#69): visual design rewrite matching Figma spec](https://github.com/agorokh/ac-copilot-trainer/pull/70)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 48
  - Files: 6
  - CI: success

- **PR #68:** [docs: SAVE vault after PR #67 merge](https://github.com/agorokh/ac-copilot-trainer/pull/68)
  - Author: agorokh
  - Merged: 2026-04-06
  - Comments: 6
  - Files: 2
  - CI: success

---

## Top Recurring Comment Clusters

These clusters represent patterns of repeated feedback across PRs.

### 1. code / details / summary

- **Count:** 33 comments
- **Severity:** maintainability
- **Preventability:** architecture
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. _🛠️ Refactor suggestion_ | _🟠 Major_

**Actually consume the shared token table at runtime.**

`tokens` is imported on Line 12 but never read after that, so WINDOW_0 still renders from its own duplica...

2. _⚠️ Potential issue_ | _🟠 Major_

**Keep the idle title red.**

Line 256 switches `"ACTIVE SUGGESTION"` to `COLOR_LABEL_GREY`, but the active state uses `COLOR_RED` and the acceptance criteria call fo...

3. _🧹 Nitpick_ | _🔵 Trivial_

**Unused legacy color tokens are acceptable but consider documenting the intent.**

Static analysis flags `COLOR_LABEL`, `COLOR_TITLE`, and `COLOR_BRAND` as unused. These ap...

---

### 2. code / color / media

- **Count:** 20 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. ### Telemetry footer never renders in idle state

**Medium Severity**

<!-- DESCRIPTION START -->
`drawTelemetryFooter` guards on `fadeAlpha < 0.01` and returns early, but `fadeAlpha` is only ever inc...

2. ### Imported tokens table unused; values already diverge

**Low Severity**

<!-- DESCRIPTION START -->
`local tokens = coachingOverlay.tokens` is assigned but never read — no code references `tokens.X...

3. ### Missing background panel during fade-out final frame

**Low Severity**

<!-- DESCRIPTION START -->
`M.draw` ignores the return value of `drawActiveSuggestion`. When `fadeAlpha` is just above 0.01 ...

---

### 3. code / details / summary

- **Count:** 11 comments
- **Severity:** maintainability
- **Preventability:** typecheck
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. _⚠️ Potential issue_ | _🟡 Minor_

**Use post-integration alpha on fade-in.**

When a hint becomes active from idle, `hasHint` is already true while `fadeAlpha` is still `0`. Line 93 snapshots that `0`...

2. _⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
set -euo pipefail

fd -i 'coaching_font\.' | while read -r f; do
  echo "== $f...

3. _🧹 Nitpick_ | _🔵 Trivial_

**Inconsistent color token usage in delta display.**

The delta color logic mixes token usage with hardcoded values:
- Line 240: Uses `tokens.COLOR_RED` ✓
- Line 238: Uses h...

---

### 4. code / test / that

- **Count:** 8 comments
- **Severity:** bug
- **Preventability:** typecheck
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/ac_copilot_trainer.lua

**Representative Examples:**

1. `hud.lua` calls UI functions like `ui.drawRectFilled`, `ui.drawRect`, `ui.setCursor`, and `ui.windowSize()` without any availability/type checks. Other modules in this repo guard UI APIs (and sometime...

2. **suggestion (testing):** There’s no test for the HUD’s behavior when `realtimeView` is nil, even though always-visible tiles are part of the contract.

ETE-01 currently always passes a non-nil `realt...

3. **suggestion (testing):** autoPlaceOnce is only validated via grepping source; behavior (one-time move and persisted flag) is not exercised via Lua.

Given the existing `ac.storage`, `ac.getAppWindows...

---

### 5. code / from / tokens

- **Count:** 5 comments
- **Severity:** nit
- **Preventability:** automation
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua

**Representative Examples:**

1. ![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `tokens` table is imported from `coachingOverlay` but never used in this file. Furthermore, the `coachingOverlay` module its...

2. `tokens` is imported from `coachingOverlay.tokens` but never used; instead this file duplicates the token values in local constants. That undermines the “single source of truth” intent and risks visua...

3. `coachingOverlay.tokens` is assigned to `local tokens` but never used, while the file redefines “shadow” token constants that can drift from the canonical values (they already do: `PANEL_PAD_Y` is 18 ...

---

### 6. code / review / pull

- **Count:** 4 comments
- **Severity:** maintainability
- **Preventability:** test

**Representative Examples:**

1. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Docs-only vault SAVE updating AcCopilotTrainer session handoff and current focus to reflect PR #73 (Phase 5 HUD rebuild...

2. ## Code Review

This pull request updates the project documentation in the 'Current Focus' and 'Next Session Handoff' files to reflect the completion and merging of Issue #57 (Phase 5 HUD redesign) an...

3. ## Code Review

This pull request updates the project's internal documentation to reflect the completion of the Phase 5 HUD rebuild and the merge of PR #73. The updates include revised status reports,...

---

### 7. code / brake / dedupe

- **Count:** 4 comments
- **Severity:** maintainability
- **Preventability:** typecheck
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua, tests/test_phase5_rebuild_ete.py

**Representative Examples:**

1. **suggestion (testing):** Live-frame engine tests don’t exercise all documented priority rules (PREPARE TO BRAKE, EASE OFF, approach labeling, and dedupe).

ETE-02..04 cover BRAKE NOW and the slower-t...

2. ![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

The dedupe key is too coarse and can suppress critical coaching updates. Since the key only consists of `kind` and `cornerLabel`, a ...

3. **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Include hint text/state in realtime dedupe key**

The dedupe key only uses `kind` and `cornerLabel`, so distinct...

---

### 8. copilot / instructions / target

- **Count:** 4 comments
- **Severity:** maintainability
- **Preventability:** architecture

**Representative Examples:**

1. ## Pull request overview

This PR rewrites the in-game HUD panels (WINDOW_0 active suggestion + WINDOW_1 approach telemetry) to match the Figma-spec layout for issue #69, addressing prior stacking/lay...

2. ## Pull request overview

Copilot reviewed 6 out of 6 changed files in this pull request and generated 3 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instructio...

3. ## Pull request overview

Copilot reviewed 6 out of 6 changed files in this pull request and generated 2 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instructio...

---

### 9. code / idle / test

- **Count:** 4 comments
- **Severity:** maintainability
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, tests/test_lua_runtime_smoke.py

**Representative Examples:**

1. **suggestion (testing):** Idle state test should assert on the actual placeholder copy, not just non-blank chrome

Right now the test only checks for the panel chrome and `ACTIVE SUGGESTION`, so it wo...

2. **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Render telemetry footer during idle state**

`M.draw()` calls `drawTelemetryFooter(vm)` in both active and idle ...

3. `drawTelemetryFooter()` returns early when `fadeAlpha < 0.01`, but in the idle path `drawActiveSuggestion()` is never called so `fadeAlpha` stays 0. As a result, the telemetry footer won’t render in t...

---

### 10. corner / code / label

- **Count:** 3 comments
- **Severity:** bug
- **Preventability:** architecture
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. ![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

There is a logic error in the corner label priority. Currently, if `nextBrake` is found (which is almost always true as it wraps aro...

2. _⚠️ Potential issue_ | _🟠 Major_

**In-corner coaching should override the next-corner label.**

`cornerLabel` is resolved from the next brake point first, and Line 235 only fills the current segment ...

3. ### In-corner label override misattributes BRAKE NOW hints

**Medium Severity**

<!-- DESCRIPTION START -->
When the car is inside a corner segment, `cornerLabel` is overridden to the current corner's...

---

### 11. code / hold / dedupe

- **Count:** 3 comments
- **Severity:** nit
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. `DEDUP_HOLD_SEC` is defined but there is no logic that uses it to hold/merge viewmodels, so the advertised 600ms anti-flicker dedupe isn’t actually implemented. Either remove this constant (and relate...

2. `lastView`/`lastEmittedAt` are written but not read anywhere, which makes the dedupe state dead code and harder to reason about. If dedupe was intentionally removed, consider deleting these fields; ot...

3. This block updates `lastEmittedAt`, but nothing consults it to suppress/freeze output for `DEDUP_HOLD_SEC`. If the intention is a 600ms dedupe, implement the time check here (e.g., keep returning `las...

---

### 12. code / with / sourcery

- **Count:** 3 comments
- **Severity:** maintainability
- **Preventability:** architecture

**Representative Examples:**

1. <!-- Generated by sourcery-ai[bot]: start review_guide -->

<details>
<summary>Reviewer's guide (collapsed on small PRs)</summary>

## Reviewer's Guide

Updates AcCopilotTrainer vault docs to reflect ...

2. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Reworks the Assetto Corsa copilot HUD to match the new Figma spec by turning WINDOW_0 into a single full-window Active ...

3. <!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Phase 5 HUD rebuild replaces the old lap-aggregate coaching engine and widget-based HUD with a live-frame viewmodel eng...

---

### 13. fixed_size / geometry / size

- **Count:** 2 comments
- **Severity:** maintainability
- **Preventability:** test
- **Affected Files:** docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md

**Representative Examples:**

1. The text says FIXED_SIZE "must recover the previous 132×456 saved geometry", but FIXED_SIZE + manifest SIZE should override/ignore that persisted (bad) geometry and force the declared size. Consider r...

2. This sentence says FIXED_SIZE "recovers the 132×456 saved geometry", which reads as if the window will keep the persisted 132×456 size. If the intent is that FIXED_SIZE forces the manifest size and th...

---

### 14. consider / details / summary

- **Count:** 2 comments
- **Severity:** maintainability
- **Preventability:** test

**Representative Examples:**

1. Hey - I've left some high level feedback:

- Referencing the specific commit hash (`a1b2126`) in the docs may become stale after rebases or history rewrites; consider describing the state more generic...

2. Hey - I've reviewed your changes and they look great!

***

<details>
<summary>Sourcery is free for open source - if you like our reviews please consider sharing them ✨</summary>

- [X](https://twitte...

---

### 15. code / scans / frame

- **Count:** 2 comments
- **Severity:** perf
- **Preventability:** guideline
- **Affected Files:** src/ac_copilot_trainer/modules/realtime_coaching.lua

**Representative Examples:**

1. ![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The new implementation introduces O(N) scans of the `segments` table every frame (here and in the `findNextBrake` fallback). For...

2. The comment on `rebuildSegmentIndex()` says the engine "never scans segments per frame in O(n) loops", but `tick()` currently does linear scans over `segments` (e.g., `inCornerSegment()` and `resolveC...

---

### 16. code / spec / white

- **Count:** 2 comments
- **Severity:** nit
- **Preventability:** architecture
- **Affected Files:** src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**Representative Examples:**

1. The title color token (`COLOR_RED`) is set to `rgbm(248/255,113/255,113/255,1)` (Tailwind red-400), but the PR description/spec calls out `#EF4444` for the red used in the HUD (and you already define ...

2. The PR description says speed delta coloring is red when `current > target + 8` and white otherwise, but `drawApproachPanel()` uses `speedColor()` which returns green when `current <= target`. If the ...

---

### 17. ac_copilot_trainer / code / tests

- **Count:** 2 comments
- **Severity:** perf
- **Preventability:** guideline

**Representative Examples:**

1. ## Code Review

This pull request implements a major rebuild of the coaching HUD (issue #72), transitioning from lap-aggregate coaching to a live-frame engine. Key changes include the introduction of ...

2. ## Pull request overview

This PR delivers a full “Phase 5 HUD rebuild” for AC Copilot Trainer, replacing the previous lap-aggregate real-time hinting and status-gated HUD windows with a live-frame co...

---

### 18. codex / review / pull

- **Count:** 2 comments
- **Severity:** nit
- **Preventability:** guideline

**Representative Examples:**

1. 
### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `213e6f6d89`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...

2. 
### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `047826f0c4`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...

---

## Most Churned Files

Files with the most review comments:

- `src/ac_copilot_trainer/modules/hud.lua`: 33 comments
- `src/ac_copilot_trainer/modules/realtime_coaching.lua`: 18 comments
- `tests/test_phase5_rebuild_ete.py`: 14 comments
- `src/ac_copilot_trainer/ac_copilot_trainer.lua`: 7 comments
- `src/ac_copilot_trainer/modules/coaching_overlay.lua`: 6 comments
- `docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md`: 3 comments
- `tests/test_lua_runtime_smoke.py`: 3 comments
- `docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md`: 2 comments
- `tests/test_design_conformance.py`: 2 comments
- `src/ac_copilot_trainer/manifest.ini`: 1 comments

---

## Suggested Next Actions

### Proposed Cursor Rule Updates

**Automation Gates (lint/format/pre-commit):**

- Add check for: code / from / tokens

**Type Checking:**

- Add type check for: code / details / summary
- Add type check for: code / test / that
- Add type check for: code / brake / dedupe

**Repository Guidelines:**

- Document pattern: code / color / media
- Document pattern: code / idle / test
- Document pattern: code / hold / dedupe
- Document pattern: code / scans / frame
- Document pattern: ac_copilot_trainer / code / tests

**Architecture/Agent Rules:**

- Add agent rule: code / details / summary
- Add agent rule: copilot / instructions / target
- Add agent rule: corner / code / label
- Add agent rule: code / with / sourcery
- Add agent rule: code / spec / white

### Proposed CI/Pre-commit Changes

- Add automated checks for common formatting/import issues
- Add type checking to CI pipeline
- Add pre-commit hooks for common issues

### Proposed Documentation Updates

- Update AGENTS.md with common patterns
- Add examples to documentation
- Create troubleshooting guide

---

## Appendix: All Comment Clusters

### Cluster 4: code / details / summary

- Count: 33
- Severity: maintainability
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
_🛠️ Refactor suggestion_ | _🟠 Major_

**Actually consume the shared token table at runtime.**

`tokens` is imported on Line 12 but never read after that, so WINDOW_0 still renders from its own duplica...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Keep the idle title red.**

Line 256 switches `"ACTIVE SUGGESTION"` to `COLOR_LABEL_GREY`, but the active state uses `COLOR_RED` and the acceptance criteria call fo...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Unused legacy color tokens are acceptable but consider documenting the intent.**

Static analysis flags `COLOR_LABEL`, `COLOR_TITLE`, and `COLOR_BRAND` as unused. These ap...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Intentional shadow tokens for test compatibility.**

`COLOR_LABEL_GREY` and `COLOR_BRAND_GREY` are unused at runtime because `tokens.COLOR_LABEL_GREY` and `tokens.COLOR_BR...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Consider `_vm` to indicate intentionally unused parameter.**

`drawIdleState(vm)` doesn't use its argument. While passing it maintains API consistency with other draw func...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Empty `if` branch can be replaced with underscore local.**

The `if _pe07Touch then end` pattern avoids the previous global `_` issue but triggers Luacheck W542 (empty if ...
```

---

### Cluster 8: code / color / media

- Count: 20
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/realtime_coaching.lua, src/ac_copilot_trainer/ac_copilot_trainer.lua, tests/test_design_conformance.py, src/ac_copilot_trainer/modules/coaching_font.lua

**All Examples:**

```
### Telemetry footer never renders in idle state

**Medium Severity**

<!-- DESCRIPTION START -->
`drawTelemetryFooter` guards on `fadeAlpha < 0.01` and returns early, but `fadeAlpha` is only ever inc...
```

```
### Imported tokens table unused; values already diverge

**Low Severity**

<!-- DESCRIPTION START -->
`local tokens = coachingOverlay.tokens` is assigned but never read — no code references `tokens.X...
```

```
### Missing background panel during fade-out final frame

**Low Severity**

<!-- DESCRIPTION START -->
`M.draw` ignores the return value of `drawActiveSuggestion`. When `fadeAlpha` is just above 0.01 ...
```

```
### Unused shadow tokens exist only for test regex

**Low Severity**

<!-- DESCRIPTION START -->
Seven local color variables (`COLOR_BG`, `COLOR_BG_BORDER`, `COLOR_RED`, `COLOR_WHITE`, `COLOR_AMBER`, ...
```

```
### Dead code kept solely for test string matching

**Low Severity**

<!-- DESCRIPTION START -->
The `_pe07Touch` variable reads `vm.debriefText` into a local that is never used, followed by a no-op `...
```

```
### Panel background blinks during idle-to-active hint transition

**Medium Severity**

<!-- DESCRIPTION START -->
When the first hint arrives, `M.draw` switches from the idle branch (panel chrome at ...
```

---

### Cluster 10: code / details / summary

- Count: 11
- Severity: maintainability
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/ac_copilot_trainer.lua, src/ac_copilot_trainer/manifest.ini, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
_⚠️ Potential issue_ | _🟡 Minor_

**Use post-integration alpha on fade-in.**

When a hint becomes active from idle, `hasHint` is already true while `fadeAlpha` is still `0`. Line 93 snapshots that `0`...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
set -euo pipefail

fd -i 'coaching_font\.' | while read -r f; do
  echo "== $f...
```

```
_🧹 Nitpick_ | _🔵 Trivial_

**Inconsistent color token usage in delta display.**

The delta color logic mixes token usage with hardcoded values:
- Line 240: Uses `tokens.COLOR_RED` ✓
- Line 238: Uses h...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Keep the footer divider on the same chrome alpha as the active panel.**

`drawActiveSuggestion()` now keeps the panel chrome fully opaque while a hint is active, bu...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**Only persist auto-placement after all target windows moved.**

Line 1031 sets `state._autoPlaceChecked` before any lookup succeeds, and Lines 1059-1060 write the st...
```

```
_⚠️ Potential issue_ | _🟡 Minor_

**Keep the in-app version string in sync.**

`manifest.ini` is now `0.5.0`, but `src/ac_copilot_trainer/ac_copilot_trainer.lua`, Line 2 still exposes `v0.4.2`. The HU...
```

---

### Cluster 0: code / test / that

- Count: 8
- Severity: bug
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, tests/test_phase5_rebuild_ete.py, src/ac_copilot_trainer/ac_copilot_trainer.lua, docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md, docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md

**All Examples:**

```
`hud.lua` calls UI functions like `ui.drawRectFilled`, `ui.drawRect`, `ui.setCursor`, and `ui.windowSize()` without any availability/type checks. Other modules in this repo guard UI APIs (and sometime...
```

```
**suggestion (testing):** There’s no test for the HUD’s behavior when `realtimeView` is nil, even though always-visible tiles are part of the contract.

ETE-01 currently always passes a non-nil `realt...
```

```
**suggestion (testing):** autoPlaceOnce is only validated via grepping source; behavior (one-time move and persisted flag) is not exercised via Lua.

Given the existing `ac.storage`, `ac.getAppWindows...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

This line appears to be redundant. The `state._cachedApproachData` variable is calculated here using the legacy `approachHudData...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Restore `hud.reset` to avoid runtime reset crashes**

`modules/hud.lua` no longer defines `M.reset`, but teardow...
```

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The dimensions `132×456` do not align with the window sizes defined in `src/ac_copilot_trainer/manifest.ini` (`520,200` for `WIN...
```

---

### Cluster 16: code / from / tokens

- Count: 5
- Severity: nit
- Preventability: automation
- Affected Files: src/ac_copilot_trainer/modules/hud.lua

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The `tokens` table is imported from `coachingOverlay` but never used in this file. Furthermore, the `coachingOverlay` module its...
```

```
`tokens` is imported from `coachingOverlay.tokens` but never used; instead this file duplicates the token values in local constants. That undermines the “single source of truth” intent and risks visua...
```

```
`coachingOverlay.tokens` is assigned to `local tokens` but never used, while the file redefines “shadow” token constants that can drift from the canonical values (they already do: `PANEL_PAD_Y` is 18 ...
```

```
Despite importing `coachingOverlay.tokens` as a “single source of truth”, `hud.lua` still defines its own panel constants (e.g., `PANEL_PAD_Y = 18`) that already diverge from `coaching_overlay.lua`’s ...
```

```
## Code Review

This pull request implements a significant visual rewrite of the Active Suggestion and Approach panels to align with new design specifications (Issue #69). Key changes include the intr...
```

---

### Cluster 3: code / review / pull

- Count: 4
- Severity: maintainability
- Preventability: test
- Affected Files: N/A

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Docs-only vault SAVE updating AcCopilotTrainer session handoff and current focus to reflect PR #73 (Phase 5 HUD rebuild...
```

```
## Code Review

This pull request updates the project documentation in the 'Current Focus' and 'Next Session Handoff' files to reflect the completion and merging of Issue #57 (Phase 5 HUD redesign) an...
```

```
## Code Review

This pull request updates the project's internal documentation to reflect the completion of the Phase 5 HUD rebuild and the merge of PR #73. The updates include revised status reports,...
```

```
## Pull request overview

Updates the AcCopilotTrainer vault “SAVE” documentation to reflect the current `main` head after PR #73 merged and to align the handoff/current-focus notes with the remaining...
```

---

### Cluster 5: code / brake / dedupe

- Count: 4
- Severity: maintainability
- Preventability: typecheck
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua, tests/test_phase5_rebuild_ete.py

**All Examples:**

```
**suggestion (testing):** Live-frame engine tests don’t exercise all documented priority rules (PREPARE TO BRAKE, EASE OFF, approach labeling, and dedupe).

ETE-02..04 cover BRAKE NOW and the slower-t...
```

```
![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

The dedupe key is too coarse and can suppress critical coaching updates. Since the key only consists of `kind` and `cornerLabel`, a ...
```

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Include hint text/state in realtime dedupe key**

The dedupe key only uses `kind` and `cornerLabel`, so distinct...
```

```
The 600 ms dedupe key is only `(kind, cornerLabel)`, so different messages of the same kind for the same corner (e.g., `PREPARE TO BRAKE` → `BRAKE NOW`) will be suppressed for up to `DEDUP_HOLD_SEC`, ...
```

---

### Cluster 14: copilot / instructions / target

- Count: 4
- Severity: maintainability
- Preventability: architecture
- Affected Files: N/A

**All Examples:**

```
## Pull request overview

This PR rewrites the in-game HUD panels (WINDOW_0 active suggestion + WINDOW_1 approach telemetry) to match the Figma-spec layout for issue #69, addressing prior stacking/lay...
```

```
## Pull request overview

Copilot reviewed 6 out of 6 changed files in this pull request and generated 3 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instructio...
```

```
## Pull request overview

Copilot reviewed 6 out of 6 changed files in this pull request and generated 2 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instructio...
```

```
## Pull request overview

Copilot reviewed 11 out of 16 changed files in this pull request and generated 4 comments.





---

💡 <a href="/agorokh/ac-copilot-trainer/new/main?filename=.github/instruct...
```

---

### Cluster 17: code / idle / test

- Count: 4
- Severity: maintainability
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, tests/test_lua_runtime_smoke.py

**All Examples:**

```
**suggestion (testing):** Idle state test should assert on the actual placeholder copy, not just non-blank chrome

Right now the test only checks for the panel chrome and `ACTIVE SUGGESTION`, so it wo...
```

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Render telemetry footer during idle state**

`M.draw()` calls `drawTelemetryFooter(vm)` in both active and idle ...
```

```
`drawTelemetryFooter()` returns early when `fadeAlpha < 0.01`, but in the idle path `drawActiveSuggestion()` is never called so `fadeAlpha` stays 0. As a result, the telemetry footer won’t render in t...
```

```
Hey - I've found 3 issues, and left some high level feedback:

- In hud.lua you now maintain both coachingOverlay.tokens and a separate set of COLOR_* / PANEL_* constants; consider deriving the shadow...
```

---

### Cluster 6: corner / code / label

- Count: 3
- Severity: bug
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
![high](https://www.gstatic.com/codereviewagent/high-priority.svg)

There is a logic error in the corner label priority. Currently, if `nextBrake` is found (which is almost always true as it wraps aro...
```

```
_⚠️ Potential issue_ | _🟠 Major_

**In-corner coaching should override the next-corner label.**

`cornerLabel` is resolved from the next brake point first, and Line 235 only fills the current segment ...
```

```
### In-corner label override misattributes BRAKE NOW hints

**Medium Severity**

<!-- DESCRIPTION START -->
When the car is inside a corner segment, `cornerLabel` is overridden to the current corner's...
```

---

### Cluster 11: code / hold / dedupe

- Count: 3
- Severity: nit
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
`DEDUP_HOLD_SEC` is defined but there is no logic that uses it to hold/merge viewmodels, so the advertised 600ms anti-flicker dedupe isn’t actually implemented. Either remove this constant (and relate...
```

```
`lastView`/`lastEmittedAt` are written but not read anywhere, which makes the dedupe state dead code and harder to reason about. If dedupe was intentionally removed, consider deleting these fields; ot...
```

```
This block updates `lastEmittedAt`, but nothing consults it to suppress/freeze output for `DEDUP_HOLD_SEC`. If the intention is a 600ms dedupe, implement the time check here (e.g., keep returning `las...
```

---

### Cluster 15: code / with / sourcery

- Count: 3
- Severity: maintainability
- Preventability: architecture
- Affected Files: N/A

**All Examples:**

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

<details>
<summary>Reviewer's guide (collapsed on small PRs)</summary>

## Reviewer's Guide

Updates AcCopilotTrainer vault docs to reflect ...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Reworks the Assetto Corsa copilot HUD to match the new Figma spec by turning WINDOW_0 into a single full-window Active ...
```

```
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Phase 5 HUD rebuild replaces the old lap-aggregate coaching engine and widget-based HUD with a live-frame viewmodel eng...
```

---

### Cluster 1: fixed_size / geometry / size

- Count: 2
- Severity: maintainability
- Preventability: test
- Affected Files: docs/01_Vault/AcCopilotTrainer/00_System/Next Session Handoff.md, docs/01_Vault/AcCopilotTrainer/00_System/Current Focus.md

**All Examples:**

```
The text says FIXED_SIZE "must recover the previous 132×456 saved geometry", but FIXED_SIZE + manifest SIZE should override/ignore that persisted (bad) geometry and force the declared size. Consider r...
```

```
This sentence says FIXED_SIZE "recovers the 132×456 saved geometry", which reads as if the window will keep the persisted 132×456 size. If the intent is that FIXED_SIZE forces the manifest size and th...
```

---

### Cluster 2: consider / details / summary

- Count: 2
- Severity: maintainability
- Preventability: test
- Affected Files: N/A

**All Examples:**

```
Hey - I've left some high level feedback:

- Referencing the specific commit hash (`a1b2126`) in the docs may become stale after rebases or history rewrites; consider describing the state more generic...
```

```
Hey - I've reviewed your changes and they look great!

***

<details>
<summary>Sourcery is free for open source - if you like our reviews please consider sharing them ✨</summary>

- [X](https://twitte...
```

---

### Cluster 7: code / scans / frame

- Count: 2
- Severity: perf
- Preventability: guideline
- Affected Files: src/ac_copilot_trainer/modules/realtime_coaching.lua

**All Examples:**

```
![medium](https://www.gstatic.com/codereviewagent/medium-priority.svg)

The new implementation introduces O(N) scans of the `segments` table every frame (here and in the `findNextBrake` fallback). For...
```

```
The comment on `rebuildSegmentIndex()` says the engine "never scans segments per frame in O(n) loops", but `tick()` currently does linear scans over `segments` (e.g., `inCornerSegment()` and `resolveC...
```

---

### Cluster 9: code / spec / white

- Count: 2
- Severity: nit
- Preventability: architecture
- Affected Files: src/ac_copilot_trainer/modules/hud.lua, src/ac_copilot_trainer/modules/coaching_overlay.lua

**All Examples:**

```
The title color token (`COLOR_RED`) is set to `rgbm(248/255,113/255,113/255,1)` (Tailwind red-400), but the PR description/spec calls out `#EF4444` for the red used in the HUD (and you already define ...
```

```
The PR description says speed delta coloring is red when `current > target + 8` and white otherwise, but `drawApproachPanel()` uses `speedColor()` which returns green when `current <= target`. If the ...
```

---

### Cluster 12: ac_copilot_trainer / code / tests

- Count: 2
- Severity: perf
- Preventability: guideline
- Affected Files: N/A

**All Examples:**

```
## Code Review

This pull request implements a major rebuild of the coaching HUD (issue #72), transitioning from lap-aggregate coaching to a live-frame engine. Key changes include the introduction of ...
```

```
## Pull request overview

This PR delivers a full “Phase 5 HUD rebuild” for AC Copilot Trainer, replacing the previous lap-aggregate real-time hinting and status-gated HUD windows with a live-frame co...
```

---

### Cluster 13: codex / review / pull

- Count: 2
- Severity: nit
- Preventability: guideline
- Affected Files: N/A

**All Examples:**

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `213e6f6d89`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

```

### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `047826f0c4`
    

<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team ...
```

---

## Hook suggestions (not applied)

The following are **suggested** `settings.json` hook snippets for `preventability: automation` patterns.
Review and merge manually if appropriate.

### code / from / tokens
```diff
+ // Suggested: add PreToolUse guard for format/lint class feedback
+ // Pattern: code / from / tokens (5 occurrences)
```
