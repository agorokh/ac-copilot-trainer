# Ignore ad-hoc lv_font_conv outputs generated locally.
*.c
*.bin
*.tmp.c

# Racing Atelier Part A4 ships the generated LVGL C font bundle because the
# firmware now references these faces directly.
!font_saira_*.c
!font_spline_*.c
