// Template for secrets/wifi_secrets.h. Copy to wifi_secrets.h (gitignored)
// and fill in. Do NOT commit.
//
// NOTE: named wifi_secrets.h (not wifi.h) because Windows' case-insensitive
// filesystem makes #include "wifi.h" collide with the framework's WiFi.h.
#pragma once
#define WIFI_SSID     "your-ssid-here"
#define WIFI_PASSWORD "your-password-here"
