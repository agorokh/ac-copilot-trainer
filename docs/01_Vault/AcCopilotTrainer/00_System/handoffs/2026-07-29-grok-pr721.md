---
type: handoff
status: active
memory_tier: canonical
created: 2026-07-29
updated: 2026-07-29
last_updated: 2026-07-29T16:00:00Z
pr: https://github.com/agorokh/ac-copilot-trainer/pull/721
issue: https://github.com/agorokh/ac-copilot-trainer/issues/719
relates_to:
  - AcCopilotTrainer/03_Investigations/issue-719-treatment-receipt-2026-07-28.md
  - AcCopilotTrainer/03_Investigations/issue-625-boot-scoped-redesign-2026-07-28.md
  - AcCopilotTrainer/03_Investigations/issue-710-cycle-delivered-2026-07-28.md
  - AcCopilotTrainer/00_System/Current Focus.md
  - AcCopilotTrainer/00_System/Next Session Handoff.md
---

# Post-merge handoff — PR #721 / #719 treatment receipt

## Resume here

1. Local `main` is on merge `de5bd6b` (PR #721 squash). Feature branch deleted.
2. Issue **#719 CLOSED**. Classification: no scripts/migrations/env/deps flags.
3. Next operator-facing work for #625 remains the **physical 16-reboot A/B** — now with
   treatment-receipt instrumentation; regenerate plans as **v4** before any run.

## What was delivered

- **Launcher** records per-attempt + boot roll-up `perturbers` (report schema **v3**):
  `injected` / `not_observed` / `unavailable` via Toolhelp32 module snapshots
  (`entry_launcher.process_module_names`). Presence unions across samples; empty PID list does
  not wipe prior absence; pin one ACS PID per attempt until exit.
- **Analyzer** (plan/analysis **v4**): cross-checks observed vs planned arm; contradicted boots
  **excluded** (never re-labelled); salvage `*.arm_contradicted.*` discoverable when planned
  report missing; `--expect-perturbers` fail-fast for off inject / on STABLE miss.
- **Operator path:** plan commands emit `--expect-perturbers`; unique salvage keeps exclusive
  planned path free for re-run after settings fix + reboot; nonzero exit stops reboot chains.
- Vault live protocol updated for receipt verification (issue-625-boot-scoped-redesign).

## What remains

- **#625 physical A/B** still operator-gated (settings toggles + rig time). Receipt makes that
  investment trustworthy; it does not replace the run.
- Rig verification of live receipt sampling through `python -m tools.ac_harness.resilient_launch`
  was done during implementation; re-confirm on the merge tip before the 16-reboot campaign.
- Codex thrash on freze-as-absence vs STABLE-only was settled in code as: freze post-go-live OK
  for **off-arm** absence confirmation; **on-arm miss** remains STABLE-only; positive injection
  any verdict. Do not re-open without a process-lifetime schema.

## Blockers

- None for merge/sync. Physical A/B still blocked only on operator sign-off and rig schedule.

## Classification (post_merge_classify)

No post-merge classification flags (no migrations / env / deps / workflows automation).

## Ops notes

- No new env vars required.
- 64-bit Python required on Windows when using `--expect-perturbers`.
- Plan schema v3 refused — regenerate with `python -m tools.ac_harness.init_perturber_ab plan`.
