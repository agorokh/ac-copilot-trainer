---
type: handoff
status: active
memory_tier: canonical
created: 2026-08-29
updated: 2026-08-29
last_updated: 2026-08-29T19:54:00Z
issue: https://github.com/agorokh/ac-copilot-trainer/issues/750
relates_to:
  - AcCopilotTrainer/00_System/Current Focus.md
  - AcCopilotTrainer/00_System/handoffs/2026-08-29-190623Z-post-merge-pr770-c1-pressure-a91f02.md
  - AcCopilotTrainer/03_Investigations/issue-746-749-repeatability-and-thermal-gate-2026-08-10.md
  - AcCopilotTrainer/01_Decisions/cold-side-temperature-tagged-friction-refit-2026-08-28.md
---

# BLOCKED: #750 three-lap scientist still needs AG_PC

## Resume here

1. Keep #749, #750, #751, and #764 open together. Do not mint a new issue or split.
2. Off-rig #750 contract is on `main` at `ffa4482` (PR #766 merge `57f01a6`, pressure
   repair #770 merge `b636830`).
3. When Tailscale `pc` is `active` (not `offline, last seen 18d`) and SSH `arsen@100.75.251.87`
   works, run the retained three-lap scientist, then close the C1 block with #749's
   second-session plant-adoption proof.

## Re-probed 2026-08-29T19:51Z (this session)

- `gh issue view 750 --repo agorokh/ac-copilot-trainer --json state` → `OPEN`.
- `gh pr view 766 --repo agorokh/ac-copilot-trainer --json state,mergedAt` → `MERGED`
  `2026-08-29T15:13:04Z`.
- `tailscale status` → `100.75.251.87 pc … windows  active; relay "lax"; offline, last seen 18d ago`.
- `ping -c 3 100.75.251.87` → `100.0% packet loss`.
- `ssh -o ConnectTimeout=5 -o BatchMode=yes arsen@100.75.251.87` → `Operation timed out`.
- Prometheus `up{job="workstation-ops",service_id="windows-exporter"}` → `"0"`.
- Target health `down`; lastError `Get "http://100.75.251.87:9182/metrics": context deadline exceeded`.
- `last_over_time((up{…windows-exporter}==1)[30d:1m])` → empty (no successful scrape in 30d).
- LAN `192.168.4.26` still pings (MAC `90:48:46:c4:36:f6`, Texas Instruments). TCP 22 / 3389 /
  445 / 9182 timed out. That is not a usable AG_PC control path; do not treat the April-2026
  PC LAN address as the rig.

## Off-rig contract (observed, not a close)

`python -m pytest tests/test_alien_scientist.py` → **32 passed**. Flying-lap filter,
`insufficient_flying_laps` → `no_verdict`, and no-verdict ledger skip are on `main`.
That does **not** satisfy the issue's live `--laps 3` re-run of `plateau_rear_wing`.

## Unblock

Power on AG_PC (or recover Tailscale if the host is already on). Reboot is the documented
fix when `tailscaled` wedges with no adapter. Then run:

```bash
python -m tools.ac_harness.auto_alien \
  --car ks_porsche_911_gt3_r_2016 --track magione \
  --setup Copilot_Balanced_Fast --laps 3 --iterations 2 \
  --scientist --scientist-trigger pace_plateau
```

No new issue. No split. C1 stays one block.
