---
type: handoff
status: blocked
memory_tier: canonical
created: 2026-08-30
updated: 2026-08-30
last_updated: 2026-08-30T02:10:31Z
issue: https://github.com/agorokh/ac-copilot-trainer/issues/764
relates_to:
  - AcCopilotTrainer/00_System/Current Focus.md
  - AcCopilotTrainer/00_System/handoffs/2026-08-30-014457Z-c1-issue749-rig-offline-fa5727.md
  - AcCopilotTrainer/00_System/handoffs/2026-08-29-210200Z-c1-issue764-mcp-verified-4b119a.md
  - AcCopilotTrainer/01_Decisions/cold-side-temperature-tagged-friction-refit-2026-08-28.md
---

# BLOCKED: #764 reverified, C1 still awaits AG_PC power

## Resume here

1. Keep #749, #750, #751, and #764 open together. Do not mint a new issue or split C1.
2. Do not retry Wake-on-LAN alone. Wait until AG_PC is physically powered on and Tailscale reconnects.
3. Then run the retained #749 second-session plant-adoption proof and the #750 three-lap scientist proof together.

## #764 observed on current main

Current `origin/main` was `11b30c0d73432f78c04bf3e9657e430b4e2e91f8`. In a clean dependency
environment built from `.[dev,mining,knowledge,analytics]`, MCP resolved to `2.1.1` and the focused
protocol test passed:

```text
.venv/bin/python -m pytest -q tests/test_repo_knowledge/test_mcp_server_import.py
1 passed in 0.63s
```

The deprecated import still fails with the SDK's explicit v2 migration message:

```text
from mcp.server import MCPServer
works
import mcp.server.fastmcp
ModuleNotFoundError: No module named 'mcp.server.fastmcp'. This is mcp 2.x, where FastMCP was renamed to MCPServer
```

An in-process `mcp.Client` exercised the consumer path, not only the import:

```text
mcp=2.1.1
server=repo-knowledge
tools=query_ci_failures,query_decisions,query_file_patterns,query_review_history,query_similar_issues
call_is_error=False
content_blocks=1
```

Full local parity passed in a clean Tk-capable Python 3.14 environment with the same declared
extras: `4105 passed, 75 skipped`, 87.21% coverage, and all format, lint, security, secrets,
policy, agent, and CSP checks green.

## Fresh live reconciliation

At `2026-08-30T02:03Z`, the non-WoL probes still showed the external rig gate:

```text
tailscale status --json
HostName=AG_PC DNSName=pc.tail31ce1b.ts.net. Online=false LastSeen=2026-08-10T22:37:24.1Z LastHandshake=0001-01-01T00:00:00Z

tailscale ping -c 1 pc
no reply
ping "100.75.251.87" timed out

ssh -o BatchMode=yes -o ConnectTimeout=8 -o ConnectionAttempts=1 arsen@pc echo CONNECTED
ssh: connect to host pc port 22: Operation timed out
```

The four coupled issues were all still `OPEN`. PR #776 was `MERGED` at
`278fb1abcd1d2bd36651da1263dea75d52f1d0c5`, and its exact merge-commit CI run
`33284538053` was successful.

## CI failure investigation

Vault PR #778 merged at `11b30c0d73432f78c04bf3e9657e430b4e2e91f8`, but its PR `build` check
failed before tests because branch `codex/issue-749-rig-gated-recheck` is not an allowed prefix in
`scripts/ci_policy.py`. This session uses `vault/issue764-c1-recheck-20260830`, which is compliant.
The first local Python 3.11 parity attempt also lacked the optional `_tkinter` extension; the exact
failing rig-launcher test passed under the installed Tk-capable Python, and the clean full rerun then
passed. No product code changed because those environment/branch failures are outside C1's file
cluster and do not reproduce on the supported hosted CI path.

## Session notes

- No product code or procedure changed in this iteration.
- Tier-3 substrate tools were not exposed by this Codex runtime. Grounding used the newest Tier-2
  handoff, linked C1 nodes, current GitHub state, and live Tailscale probes.
- The full `autonomous-deliver` loop is Opus-only by its own runtime contract. This GPT-5.6 run
  followed the required orchestrate fallback and stopped only at the confirmed external host gate.
